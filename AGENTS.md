# AGENTS.md — csl-observatory

This file orients Codex and other non-Claude agent sessions. The section between the generated-block markers is machine-owned (regenerated from Uprava; hand edits there are overwritten) — add repo-curated content OUTSIDE the markers.

[generated-block: repo-context — do not hand-edit; regenerated by Uprava tools/gen_repo_context.py; content-hash: bfb9bbd7b8]
## Shared org context (generated — H1393 layer)

- Canonical Sanskrit-domain facts (encodings, key1/key2, corpus gaps, transcoder traps): read the [Sanskrit context primer](https://github.com/gasyoun/github-spine/blob/main/SANSKRIT_CONTEXT_PRIMER.md) before touching encodings, transliteration, or corpus data.
- Task routing: [CLAUDE.md](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/CLAUDE.md) — open the matching section; do not read the file end-to-end. Sections: Project Overview · Repo Category · GitHub Issue Conventions · Cross-Repo Coordination · Operational hazard notes.
- Danger facts (destroy-work-if-missed; full text + sources in [DANGER_FACTS.md](https://github.com/gasyoun/Uprava/blob/main/DANGER_FACTS.md)):
  - csl-orig + all Cologne upstreams are fenced: no direct commits/PRs; corrections only via the queued monthly batch-PR path
  - sanskrit-util iast_to_devanagari is BROKEN (9/9 basic words wrong) — use the composition to_slp1() -> slp1_to_devanagari() instead
  - git pull / rebase --autostash in a shared-contention clone can swallow another session's uncommitted work — prefer git fetch + a fresh worktree
  - Every subprocess git call in a test fixture must filter GIT_* env — an unfiltered git init --bare / git config can corrupt the CANONICAL repo's .git/config.
- Further operational hazard notes live in the org-private hub ([Uprava DANGER_FACTS.md](https://github.com/gasyoun/Uprava/blob/main/DANGER_FACTS.md); org members only).
- Cross-repo data edges (from [Uprava interlinks_edges.tsv](https://github.com/gasyoun/Uprava/blob/main/interlinks_edges.tsv)):
  - consumes → `*`: org-wide repo metrics (manifest.json) (live)
  - feeds → `csl-atlas`: correction_events_release.csv (50,953 events) (queued)
  - produces → `*`: pwg-scan-index-campaign — PWG scan-index campaign registry + e-text candidate queue + scan-target audit (data/pwg_scan_index_tracker/) (live)
  - ← from `ext:sanskrit-lexicon-scans` (feeds): pwg-scan-index-campaign — 37 PWG/PWK page-scan link-target repos (~11.2 GB), each serving app1/app2 at sanskrit-lexicon-scans.github.io/<dir>/ (live)
- Gitignored/off-git canonical assets are registered as pointer rows in the org-private [shadow-assets registry](https://github.com/gasyoun/Uprava/blob/main/SHADOW_ASSETS_POINTERS.md).

[/generated-block]
