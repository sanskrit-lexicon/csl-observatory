# Project Objective: Phase L0 — first convention-fingerprint cladogram of 35 CDSL dictionaries (real, validated fingerprint → distance → tree → paper). Pipeline: docs/handoffs/ARCHITECTURE.md.

## ➡️ Next Steps (Queue)

- [ ] **Human gate (Stage 2)**: fill the 461 `unknown` cells in `data/L0/convention_fingerprint.csv`. Breakdown of the 461:
  - 288 = Patel/philological dims (1-8 + 16) × the 32 present dicts → need the **Patel 2016 PDF** or manual annotation.
  - 90 = the 3 missing-source dicts (KNA, KOW, AMAR) × 30 dims.
  - ~83 = dim-specific gaps among the 21 auto dims (e.g. dim15 only 13/32 cite; dim18 only 5/32 mark verb class; dim22 22/32 have Skt tokens; dim29 7/32 had measurable etym length).
- [ ] Decide sourcing for **KNA / KOW / AMAR** — per-dict repos exist in the org but they are absent from `../csl-orig/v02`.
- [ ] Review the **SHS etym=none anomaly** (SHS derives from WIL, which is etym=full).
- [ ] When `cells_unknown` is acceptably low → run **Stage 3 (distance)**. Stage 3 MUST drop the constant dims **15, 18** (and any all-unknown dims) before computing distances — they carry zero signal.

## 🚧 Current Work-In-Progress (WIP)

- [ ] (none active) — Stage 2 real re-extraction is complete and parked at the human-annotation gate.

## 🧠 Dev Notes & Hypotheses

- **2026-05-27**: The 2026-05-18 Stage 2 output was degenerate. The old `s2_fingerprint.py` wrote a constant (`DIMS[i].options[0]`) down every column for all 35 dicts to game the "40% filled" acceptance bar — every dictionary identical, so any tree would be a meaningless star. Rebuilt it as a real streaming extractor over `../csl-orig/v02/<code>/<code>.txt` (32 of 35 dicts cloned locally).
- Extractor design: samples ≤4000 entries/dict (conventions are global, so first-N is fine); 21 auto dims; 9 left for annotation (Patel 1-7, sandhi 8, MBh-edition 16). Margin-based confidence capped at 0.9. A **variance check flags constant columns** instead of hiding them.
- Result: 589/1050 cells (56%), **19 informative dims**, **2 flagged constants (15, 18)**. Validated against known facts for WIL / MW / PWG / VCP / SKD — all strong signals correct (see EXECUTION_LOG 2026-05-27 block).
- External inputs **unchanged as of 2026-05-27** (confirmed with M.G.): no Patel 2016 PDF, no classical manuals, no Matomo access. So Patel dims stay unauto-fillable.
- Note: KCH (Kochergina) is NOT in the 35-dict L0 subset.
- **2026-05-27 preview validation**: built the first REAL convention cladogram (`scripts/L0/preview_tree.py` → `data/L0/preview/`) on the 19 informative dims × 32 dicts, missing-aware Gower + Jaccard, UPGMA + NJ. **All 6 known lineages came out tighter than the global mean (0.433)**: VCP/SKD=0.000, AP90/AP=0.210, CAE/CCS=0.222, Petersburg-German=0.353, Wilson=0.370, MW72/MW=0.389. Nearest-neighbour pairs recover AP90↔AP, VCP↔SKD, PW↔SCH, CAE↔SHS. So the 19 auto dims already carry real phylogenetic signal even before the Patel dims are added. Surprises to revisit after annotation: YAT↔STC (d=0.059), MW grouping with GRA, BHS near AP90. NOT the gated Stage 3/4 (preflight needs cells_unknown=0).

- **2026-05-27 YAT↔STC chase**: investigated why YAT (Yates 1846 Skt-Eng, a Wilson digest) and STC (Stchoupak 1932 Skt-Fra, a Petersburg digest) were nearest convention-neighbours (d=0.059). Verdict: **genuine, not artifact** — both are minimalist bilingual glossaries (0% `<ls>` citations, 0% etymology, no cognate tags, abbreviated grammar, segmented headwords). **Methodological lesson: the convention fingerprint measures editorial REGISTER, which converges across unrelated lineages (homoplasy) — convention-distance ≠ descent.** This argues for combining it with the lemma-overlap Jaccard (which *does* capture descent: WIL→YAT was 91% containment in sanhw1). Needs a stated caveat in the paper. The chase also found + fixed a real bug: `{%...%}` italic-gloss markup was miscounted as a foreign-language tag, inflating dims 21 & 27 for every bilingual dict. Fixed; re-ran; 6/6 lineages still cohere; Wilson group tightened 0.37→0.30, Cappeller 0.22→0.11. Diagnostic kept at `scripts/L0/diag_compare.py`.
- Known under-detection (not yet fixed): YAT lightly numbers senses with mid-line `1.` (e.g. `{#(aH)#} 1. {%m.%}`) which the arabic-numbering regex (line-start only) misses → YAT dim11=unnumbered is slightly off. Low impact.

- **2026-05-27 homoplasy quantified (tanglegram)**: `scripts/L0/tanglegram.py` compares the convention UPGMA tree vs the sanhw1 lemma-overlap UPGMA tree over their **30 common dicts** (intersection drops BOR/AE — not in sanhw1 — plus KNA/KOW/AMAR). Result: the two signals are **significantly but weakly congruent** — Mantel r=0.223 (p=0.009, 999 perms; r²≈0.05), Robinson-Foulds 52/56 = only **2 of 28 clades shared** (random baseline 56±1), leaf-order crossings 202/435≈0.46 (≈random 0.5). So convention ≠ descent: complementary signals, not redundant. Biggest topology movers = the specialized indexes (INM 26, BHS 23, MCI 23, PUI 22) — distinctive content but generic formatting — plus MW (20) and AP90/AP (18). YAT/STC example: lemma tree glues YAT to (WIL,SHS) and STC to MW72; convention tree glues YAT↔STC → their connectors cross. Artifacts: tanglegram.png, homoplasy_metrics.json, common_{convention,lemma}.newick in data/L0/preview/. **This is Paper-1 material: the quantified case for the multi-signal method.**

## ✅ Completed (Recent only)

- 2026-05-28: **Patel annotation scaffold built** to unblock Phase L0 Stage 2 human gate. `scripts/L0/patel_annotation_scaffold.py` emits `data/L0/patel_annotation_scaffold.csv` (315 rows = 35 dicts × 9 Patel-schema dims [1–8 + 16]) with up to three real exemplar entries per (dict × dim) cell sampled from `../csl-orig/v02/<code>/<code>.txt`, plus four annotator-input columns (`annotator_value`, `annotator_label_if_new`, `annotator_confidence`, `annotator_notes`). 32/35 dicts have local source; KNA/KOW/AMAR are flagged `source_available=no`. 224/315 rows carry at least one exemplar; the 91 empty are honest gaps (the feature does not occur in the first 4000 entries of that dict — sparsest is dim 5 verb-anusvāra at 9/35, then dim 8 compound boundary at 15/35, then dim 16 MBh references at 18/35). Spot-check shows the WIL vs YAT contrast on dim 2 surfaces immediately: WIL `aMSuBartf` (single t) vs YAT `aMSuBarttf` (doubled tt). Companion guide: `docs/handoffs/PATEL_ANNOTATION_GUIDE.md` (workflow + per-dim glosses + honest-gaps section). The merge step `merge_patel_annotations.py` is stubbed in the guide but not yet implemented — to be written when the first round of annotations returns.
- 2026-05-28: **Self-review pass over §3.5 + §5.2.1** caught and fixed six issues in `docs/articles/article_1_methods.md`: (a) Cappeller editions ordered chronologically (German 1887 then English 1891); (b) BHS abbreviation now spelled with its acronym for consistency with INM/PUI/MCI; (c) the cryptic "Monier-Williams 1899, where the reverse holds" expanded to "distinctive house style places it peripherally in convention space while its near-universal lemma overlap places it centrally in lemma space"; (d) added a methodological parenthetical in §5.2.1 explaining why AP-AP90 (one of the 3 shared clades in §3.5) is absent from the 7-pair joint-test table — the published inheritance-edges file requires directed containment ≥ 0.85 that AP90→AP doesn't reach (likely a normalisation artefact across the two Apte editions), so the topology-level test of §3.5 is strictly more inclusive than the pair-level test of §5.2.1; (e) §5.2 forward-reference sharpened to §5.2.1; (f) §3.5 P3 closing parenthetical rewritten with em-dashes for flow.
- 2026-05-28: **Extractor tightening (s2_fingerprint.py)** — two surgical regex fixes that propagated through the whole chain. (a) `etym_E` regex broadened from literal `\.E\.` to `(?:^|[\s.¦])E\.\s+\{#` so it catches both WIL's `.E. {#...#}` and SHS's bare ` E. {#...#}` (same Nirukta convention, one fewer period); SHS's etymology was 100% missed before. (b) `num_arabic` regex gained `[\s\}]\d{1,2}\.\s+[A-Z{]` so it catches mid-line sense markers like YAT/SHS `{#(aH)#} 1. {%m.%}` and `ind. 1. NO, ... 2. A ...`. **Downstream impact**: Mantel r 0.223→**0.228** (p 0.009→**0.006**), Robinson-Foulds 52/56→**50/56**, shared clades **2/28→3/28** (new: SHS↔WIL), WIL→SHS conv Gower 0.167→**0.053** (overtakes Cappeller as the strongest joint signal). Joint passers 8→**7**: SHS-YAT dropped from 0.278 to 0.389 because the fix correctly upgraded SHS to "etymology=full" (matching its WIL parent) while YAT still has none — a controlled counterexample within the Wilson lineage. YAT-STC convergent-minimalism distance also dropped from 0.059 to **0.000** (identical across all 17 co-known dims). §3.5 and §5.2.1 prose + Table 5.2.1 updated to reflect new numbers; the "iff same-author direct revision" framing softened to "where editorial register is preserved across the lineage" (covering both same-author revisions and same-tradition register-preserving descent like WIL→SHS).
- 2026-05-28: **Paper-1 §5.2.1 *Dictionary pairs that pass the joint inheritance test* drafted** (~700 words + Table 5.2.1) into `docs/articles/article_1_methods.md`. Defines joint pass as containment ≥ 0.85 AND conv Gower ≤ Q1 (0.333). 7 of 22 candidate inheritance pairs pass (updated 2026-05-28 from initial 8 after the extractor tightening above). Added §5 Results scaffolding; fixed the §3.1 "Section 4 (below)" → "Section 5 (below)" reference. Computation: `scripts/L0/joint_inheritance_table.py` → `data/L0/preview/joint_inheritance_table.csv`.
- 2026-05-28: **Paper-1 §3.5 *Inheritance detection: two complementary signals* drafted** (~530 words + ~100-word figure caption) into `docs/articles/article_1_methods.md`. Slots before the limitations subsection (renumbered §3.5→§3.6); cross-references sharpened; status/date bumped to 2026-05-28. Headline framing: convention recovers descent **iff same-author direct revision** (the (CAE,CCS) and (AP,AP90) clades); elsewhere the signals diverge — joint use is the strong inheritance claim. The methods-doc NEXT-STEPS line for Phase L0 marked partial-done (preview only; full L0 still gated).
- 2026-05-27: Quantified convention↔descent homoplasy via tanglegram (Mantel r=0.22 p=0.009; only 2/28 clades shared). Complementary signals; specialized indexes move most.
- 2026-05-27: Chased YAT↔STC surprise → genuine convergent minimalism (not artifact); fixed `{%...%}`-as-lang-tag bug in dims 21/27; re-ran, lineages still cohere.
- 2026-05-27: Built + validated the first real convention cladogram (preview). 6/6 known lineages recovered as cohesive. Outputs in data/L0/preview/.
- 2026-05-27: Rebuilt + ran + validated Stage 2 real fingerprint extraction; committed `2247803`; EXECUTION_LOG.md appended with corrected block.
