# Project Objective: csl-observatory = the GitHub/organization observatory for the sanskrit-lexicon / CDSL ecosystem — measure repositories, issues, PRs, commits, contributors, and digitisation-work health. Scope is fixed by `docs/BOUNDARY_RULES.md` (human decision, 2026-06-03): dictionary-content research lives in `csl-atlas`, standards work in `csl-standards`.

## ➡️ Next Steps (Queue)

- [ ] One in-scope org finding still on the table (raw data already in `observatory/site/src/data/`): **org velocity/health timeline** (`timeseries_annual/monthly.csv` — issues/PRs/commits/unique-authors per year+month). Follow the established pattern: offline script in `scripts/`, `reports/<name>.md`, `src/data/<name>.csv`, then a site page/section whose JS mirrors the script.
- [ ] Follow up on the **ORCID identity gap** surfaced by the bus-factor finding: all 17 human contributors lack an ORCID in `people_summary.csv`. Tracking issue #1 already invites contributors to self-identify.
- [ ] Possible org-hygiene action items flagged by the repo-health audit (not observatory code, but worth an issue/runbook): add licenses to the 41 license-less repos, normalise the 21 `NOASSERTION` ones, standardise default branch (46 still on `master`), and archive the 6 `temp_*`/`test_*`/legacy repos.

## 🚧 Current Work-In-Progress (WIP)

- [ ] (none active) — both shipped findings (bus-factor, repo-health) are complete end-to-end on `main`; tree clean except untracked `.claude/` (local config).

## 🧠 Dev Notes & Hypotheses

- **Boundary decision (2026-06-03)**: `csl-observatory` was narrowed to GitHub/org observability. The dictionary-content pipelines (L0 convention cladogram, forensic MW↔PWG lineage, lexico/R2 sense work, Article 21, sanhw1) were **moved to `../csl-atlas`** — confirmed present there with live history (PR #33). Do not re-add dictionary research here; admission test + pointers in `docs/BOUNDARY_RULES.md` and `docs/DICTIONARY_STRUCTURE_MOVED.md`. Pre-boundary plans archived as `docs/*LEGACY_BROAD_METRICS.md`.
- **Data pipeline**: `scripts/pull_data.py` (GraphQL fetch) → `scripts/compute_metrics.py` (alias-merge onto canonical logins via `scripts/contributors_map.json`) → site CSVs in `observatory/site/src/data/`. `scripts/retry_via_clone.py` is the fallback for repos GraphQL can't serve (bare-clone `--filter=blob:none`, metadata only). `scripts/contributor_stats.py` is the **live-API** leaderboard; `scripts/bus_factor.py` is **offline/reproducible** over the committed CSVs.
- **Observable site** (`observatory/site/`, Framework v1): `npm run build` / `npm run dev` from that dir. Build output (`dist/`) and caches (`.observablehq/`, `src/.observablehq/`) are gitignored — commit only `src/*.md` and `src/data/*.csv`. Reactive `${}` values render as `<observablehq-loading>` placeholders in static HTML and hydrate client-side from FileAttachment data; a clean build is the compile-time signal.
- **Bus-factor headline** (current data): 65/76 repos have bus factor 1; funderburkjim alone = 51.8% of all contributions; core trio = 97.6%; Gini 0.86; 1/2/3 people cover 50/80/90% of work. Page math in `community.md` mirrors `scripts/bus_factor.py` so site and `reports/bus_factor.md` stay in sync.
- **Repo-health headline** (current data): 41/76 repos (53%) have no license at all, 21 are `NOASSERTION`, only 14 recognised SPDX; 46/76 default to `master`; 5 lack a description; 6 disposable `temp_*`/`test_*`/legacy repos are live; 0 stale (all pushed within ~3 weeks); only 5 fully clean. The org's risk is hygiene + concentration, not abandonment. `repo-health.md` site page mirrors `scripts/repo_health.py`.
- **Taxonomy-adoption headline** (current data): 5,324 issues (89 PRs excluded); 89% carry a type label, 83% severity, 79% milestone, **63% fully conformant** (1 type + 1 severity + milestone, per org CLAUDE.md). Adoption curve: 11–25% conformant 2014–18 → 92% peak 2025 → 39% dip 2026 (recent backlog, un-milestoned). 324 over-typed; 999 carry one of **54 distinct stray labels** (`Documentation` 195, `Correction submission` 175, `help wanted` 92, many `domain:*` — normalise/fold/delete candidates). Section on `/coverage` mirrors `scripts/taxonomy_adoption.py`.
- **Finding pattern (established, repeatable)**: offline script over a committed `src/data/*.csv` → `reports/<name>.md` + `src/data/<name>.csv` → Observable page/section (new page registered in `observablehq.config.js`, or a section added to an existing page), with `${}` KPI cards + Plot charts whose JS recomputes the same numbers as the script (cross-check + no drift). Three findings shipped this way: bus-factor (Community page), repo-health (new Repo Health page), taxonomy-adoption (section on the Issue Taxonomy page).

## ✅ Completed (Recent only)

- 2026-06-05: **Issue-taxonomy adoption & conformance shipped end-to-end** — `scripts/taxonomy_adoption.py` (offline, over `issues.csv`) → `reports/taxonomy_adoption.md` + `src/data/taxonomy_adoption.csv` (commit `9606570`); adoption section added to the existing `/coverage` (Issue Taxonomy) page (commit `daef4b0`). Build clean: 8 pages, 22 links. Third in-scope finding under the boundary.
- 2026-06-05: **Repository-health audit shipped end-to-end** — `scripts/repo_health.py` (offline, over `repos.csv`) → `reports/repo_health.md` + `src/data/repo_health.csv` (commit `a0ba200`); new **Repo Health** site page `src/repo-health.md` + nav entry in `observablehq.config.js` (commit `c9563b0`). Build clean: 8 pages, 22 links. Second in-scope finding under the boundary.
- 2026-06-05: **Tracked `observatory/site/package-lock.json`** (lockfileVersion 3) for reproducible site builds — commit `f35d828`. (Resolved the open queue question.)
- 2026-06-05: **Refreshed `.ai_state.md`** from stale Phase L0 content to the org-observatory scope — commit `884518f`.
- 2026-06-05: **Wired the bus-factor finding into the Observable site** (`community.md`) — replaced the rough top-3-share heuristic with the canonical `bus_factor.csv`: KPI cards (bus-factor-1 repos, people-for-half, core-trio share, Gini) + per-repo top-share chart coloured by single-point-of-failure status. Verified with `npm run build` (7 pages, 19 links, no errors). Commit `ce27110`.
- 2026-06-05: **Bus-factor / contributor-concentration finding** — `scripts/bus_factor.py` (offline, over `observatory/site/src/data/contributors.csv`) → `reports/bus_factor.md` + `observatory/site/src/data/bus_factor.csv`. First in-scope finding under the new boundary. Commit `7fefa5e`.
- 2026-06-05: Housekeeping — gitignored `observatory/site/src/.observablehq/` build cache (`4f5c1c1`); tracked `scripts/retry_via_clone.py` (`05611ce`). All pushed to `origin/main`.
- 2026-06-04/03: **Boundary cleanup** merged via PR #14 (`047eb94`) + `docs: update boundary status` (`8b000c9`) — narrowed observatory to org scope; dictionary-research scripts/data/docs moved to `csl-atlas`.
