# Project Objective: csl-observatory = the GitHub/organization observatory for the sanskrit-lexicon / CDSL ecosystem — measure repositories, issues, PRs, commits, contributors, and digitisation-work health. Scope is fixed by `docs/BOUNDARY_RULES.md` (human decision, 2026-06-03): dictionary-content research lives in `csl-atlas`, standards work in `csl-standards`.

_Created: 08-05-2026 · Last updated: 03-07-2026_

## ➡️ Next Steps (Queue)

- [ ] **Phase-2 rethink queued tracks (MG decisions 2026-07-03: scholars-first audience; all four visual forms; active monitor).** Roadmap Workstream G in [`docs/OBSERVATORY_ROADMAP.md`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/docs/OBSERVATORY_ROADMAP.md). G1 (lifecycle) + G2 (delta monitor) shipped 2026-07-03 (Fable 5, `claude-fable-5`, commits `869c313`/`1fcc41d`). Remaining four run from Uprava handoffs, priority order: [H089 capture–recapture](https://github.com/gasyoun/Uprava/blob/main/handoffs/H089_obs_capture_recapture.md) (errors REMAINING per dict; candidate Axx) · [H090 network page](https://github.com/gasyoun/Uprava/blob/main/handoffs/H090_obs_network_page.md) · [H091 story page](https://github.com/gasyoun/Uprava/blob/main/handoffs/H091_obs_story_page.md) · [H092 external reach](https://github.com/gasyoun/Uprava/blob/main/handoffs/H092_obs_external_impact.md) (traffic-API step gated on MG token, GTD @DO).

- [x] **A13 agent fix pass DONE 2026-07-02** (Opus 4.8 (`claude-opus-4-8`), branch `docs/a13-iij-fixpass`, PR pending): executed sections A–E of [`article/A13_review_fable5.md`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/article/A13_review_fable5.md). **A** numbers: abstract 6400+→"over 5,400 issues and pull requests across 76 repositories" (per `manifest.json` 5,413/5,324-excl-PR), 392600+→"over 390,000", Gode 2021→**1961** + PD chain restated (Joshi 1963 → sixty-year post-mortem term → 2023), both 41,6 %→**41.6 %** reconciled as one figure. **B** bibliography: §7.1 → single alphabetised IIJ author-date **## References** (16 published works); Böhtlingk 2007→Brückner & Zeller 2007; Kretov & Leonchenko→Kretov, Gasūns & Leonchenko; uncited 2019–24 conference talks → `[^presentations]` footnote; §7.2 → **## Appendix: Dictionaries at CDSL**; `refs.bib` two "Reference TBD" stubs (malten1997/kapp2009, both uncited) removed. **C** IIJ format: all 60 numbered headings → text markdown headings, §5.2.1/5.2.2 reordered + de-numbered, 4–6 keywords added, title→H1. **D** garbles: AI alove→alone, doen't→doesn't, Hardvard→Harvard, Atharhaveda→Atharvaveda, 21^th^→21^st^, "last millennia"→"last two centuries", hostage grammar, typoe(s)→typo(s), Sanskit→Sanskrit, §5.1.4 truncated German table ellipsised + `&#13;` garble stripped. **E** build: `article/indo-iranian-journal.csl` added (author-date, Chicago-18 base), narrative build lines in `article/README.md`, **`article/00-report-narrative.docx` produced** (fonts-embedded PDF NOT built — no xelatex locally; command documented), root mirror `gasuns-cologne-30-report.md` re-synced, BOM-free. **Flagged for M.G. in PR (could not resolve without private knowledge):** Jachertz 1983 full citation (absent from 1997 report; WebSearch "title" was unverifiable), which work (Gasūns 2024) binds to. **4 M.G. gates unchanged** (byline/ORCID/email + "Zealots's", private-quote permissions §3.2/§3.5.3/§4.2.1, four sharp passages, A14 decoupling) — left in manuscript, listed in PR body.

- [x] **P6 — Tier A PRs audited 2026-06-29.** VCP #20 DONE (closed, pUrbb=0 confirmed). SKD #13: 10/14 done (PR #2864 merged); 3 lines (42288, 54089, 92343) need editorial judgment from Andhrabharati — already flagged in issue. csl-apidev #24 DONE (closed). csl-websanlexicon #60 fixed in open PR #2872 (waiting Jim). csl-apidev #10 (`<pic>` path): details in COLOGNE #286 — blocked by TLS timeouts, re-check next session.
- [ ] **Awaiting maintainer**: csl-orig #1788 (MW sup→rev, Andhrabharati to confirm); csl-ldev #11 (SKD scanned-print variant, maintainer sign-off); PWK #12 (bracket form decision needed).
- [ ] **COLOGNE #286**: network retry result unknown — re-run triage.
- [x] **csl-devanagari #38**: PR #45 (`codex/fix-devanagari-tags`) updated 2026-06-29 with two fixes: (1) XML tag preservation via `transliterate_text_preserving_xml_tags`; (2) `ACCENTED_DICTS={'acc','gra'}` so all non-Vedic dicts use `slp1` instead of `slp1_accented` (fixes the `/`→accent corruption in BOR, SKD, VCP). 7/7 tests pass. Awaiting Dhaval merge + server-side regeneration of v02/ outputs.
- [x] **csl-ldev #12 (SKD Sāyaṇa)**: PR csl-orig#2873 (`fix/skd-sayana-retroflex`) — 165 occurrences fixed (163 standalone + 2 commentator compounds BAzyakftsAyaNaH/tadBAzyesAyaNaH). 22 compounds (rasAyanaH etc.) correctly left unchanged. Awaiting Jim/Dhaval merge.
- [ ] Open PRs to merge (Jim/Dhaval): csl-orig#2866/2867/2868/2869/2870/2871/2872, csl-pywork#62, csl-websanlexicon#74, csl-apidev#61.
- [ ] ORCID registration into `scripts/contributors_map.json` for the 7 named contributors + identify the 9 unknown logins is tracked by issue #20.
- [ ] Org-hygiene: standardise default branch (46 still on `master`); archive last 2 temp repos (with funderburkjim).
- [ ] The seven hygiene issues are now FILED (#15–#21, all on Tooling Roadmap project #9) — the remaining work is the maintainers actually doing them (licenses, branch rename, archiving, ORCIDs, taxonomy cleanup). The ⚠ care notes on #15/#17/#19 still need human judgement before execution.
- [ ] ORCID registration into `scripts/contributors_map.json` for the 7 named contributors + identify the 9 unknown logins is tracked by issue #20 (`reports/contributor_identity.md`).
- [ ] Org-hygiene from the repo-health audit: **licenses DONE** (41 license-less + 21 NOASSERTION resolved — see RH1/RH3 item below) and **4 of 6 temp/legacy repos archived**. STILL OPEN: standardise default branch (46 still on `master`) and archive the last 2 temp repos (with funderburkjim).

- [x] **RH1/RH3 rollout — DONE 2026-06-19/20 (PR #54 + #55 both merged to main, deploys green).** Full record: `docs/RH1_LICENSE_ROLLOUT_LOG.md` + memory `project-rh-license-archive-policy`. **RH1 COMPLETE**: ~36 repos licensed across 7 batches (code/tooling no-license→GPL-3.0; dictionary no-license→CC-BY-SA-4.0; NOASSERTION normalization; MWS/Wil-YAT relicensed off CC-BY-NC-SA-3.0+MIT→CC-BY-SA-4.0 per MG; mixed code+data dual layout = root LICENSE CC-BY-SA-4.0 + `licenses/GPL-3.0.txt` + README note; correction/source data; infrastructure incl. 3rd-party NOTICEs for sanskrit-fonts/csl-homepage). Counts: none 41→6, NOASSERTION 21→0, recognised 14→70 (dashboard refreshed via `scripts/refresh_repo_licenses.py`, PR #55). **RH3**: archived santamlegacy/temp_corrections_acc/temp_corrections_ae/test_cologne_push (`scripts/rh3_archive.py`); **temp_corrections_ap90 + temp_corrections_mw LEFT FOR funderburkjim** (each has one open active scholarly-question issue). **Jim pinged 2026-06-21** — a polite per-issue note posted as @gasyoun (no AI footer; light apology for the recent automated flurry since Jim was unhappy about it) asking whether to migrate the thread into the relevant dict repo or keep the repo open; ball in his court. Once he migrates/closes the issues, re-run `rh3_archive.py` (it blocks on open issues by design). **Reusable tooling added** (all retry the flaky network + recheck-after-timeout): rh1_apply_license / rh1_apply_dual / rh1_delete_file / rh1_put_file / rh1_state / rh1_inspect_noassertion / rh1_infra_probe / rh1_mixed_probe / rh3_archive / refresh_repo_licenses. **KEY GOTCHAS**: GitHub's repo-health audit (`repos.csv`) is stale+mislabeled → always get live state via `rh1_state.py` before mutating; "NOASSERTION" ≠ "no license" (inspect the actual text — that's how MWS/Wil-YAT's intentional NC license was caught); a 2nd root `LICENSE*` file (e.g. LICENSE-CODE) collides with GitHub's detection glob → put the code license under `licenses/`. OBS-T data stays CC-BY-4.0.

## 🚧 Current Work-In-Progress (WIP)

- [x] **OBS-T VALIDATION (Phase 7, PR #61 merged 2026-06-29).** All AI-doable phases complete. Gold annotation 390-row `validation/gold_sample.csv` (accuracy 0.29; grammar/meta F1 ~0.90; ~66 encoding↔orthography disputed rows). Error sample 120-row `validation/error_sample.csv` (0/120 found-error rate). Paper draft `reports/obs_t_paper_draft.md` (~6000 words). Paper revision ✅ (2026-06-30), Zenodo DOI ✅ minted 2026-07-01, venue ✅ LREC-COLING. **ONLY REMAINING GATE**: the human IAA — (1) review ~66 borderline rows; (2) second annotator fills `gold_component_2` for Cohen κ.

- [x] **OBS-T error-typology track verified current (2026-06-26).** PR #33 merged (`081d57d`). Two-axis form: **52,498 events**, 43 dicts, 208 correctors, 64.3% derived. Remaining: **human IAA (κ) only** — paper revision ✅, Zenodo DOI ✅ ([10.5281/zenodo.15834721](https://doi.org/10.5281/zenodo.15834721)), venue ✅ LREC-COLING (all 2026-07-01). Optional: neural baselines (needs torch).
- [ ] (prior) observatory build-out complete; all four findings + synthesis + identity worksheet shipped on `main`; hygiene issues filed; project #9 categorised.

## 🧠 Dev Notes & Hypotheses

- **✅ RESOLVED in Phase 8 (two-axis reframe, branch `obs-t-twoaxis`).** Author chose "two clean axes". Implemented: `error_component` is now the LOCATION axis (derived-only: headword/grammar/citation/sense/markup/crossref/meta; join-failures = `unattributed`; untagged git text → sense; no more orthography/encoding as locations) and a new `edit_type` column is the EDIT-TYPE axis (spelling/diacritic/case/spacing/punctuation/digit/transposition, from edit-ops, all events). Results: LOCATION (derived) sense 53% / headword 22% / markup 12% / citation 10%; EDIT-TYPE spelling 33% / punctuation 20% / spacing 19% / diacritic 11% / case 9%. H1 restated = micro-edit dominance (median dist 2, **66% ≤2 chars**, minor-edit rate 68%) holding across locations; H2 location×dict V=0.415 (bootstrap [0.41,0.42], minus-PW 0.437); H3 location trends headword falling, markup/meta rising; classifier baseline jumped 0.388→**0.594**. All scripts (typology/rigor/robustness/gold-guide/page) updated; site builds clean. **Docs pass 2026-06-12** (branch `obs-t-docs`): design spec §0 two-axis final state, datasheet two-axis labels, README + reports/README OBS-T sections, schema + script-dict add error_component/edit_type/crosswalks/split, and new `docs/CODEX_REVIEW_PROMPT.md` (full external-review prompt). The historical confound note below is kept for provenance.

- **⚠️ HISTORICAL — component-axis confound (Phase 7i, 2026-06-11), now RESOLVED above.** The silver validation (`obs_t_silver.py`) showed the empirical-cluster fallback agrees with the headword-join only **0.1%** on their overlap — because they measure DIFFERENT AXES: the join assigns component by LOCATION (headword typo → `headword`), the heuristic by edit TYPE (`typo` → `orthography`). So the inferred fallback (34% of corpus) filed type-axis values (orthography/encoding) into the location-axis component column. Evidence: orthography 38.3% of ALL labels but 12.8% derived; encoding 2.6% all / **0.0% derived**. On derived (location) labels the real typology is **sense 40.5% > headword 21.7% > orthography 12.8% > markup 11.8% > citation 10.0%** — NOT orthography-dominated. Consequence: the H1 "surface vs meaning" framing conflated sense-LOCATION with meaning-EDIT and must be restated. **Recommended fix (Phase 8)**: two orthogonal axes — LOCATION component (derived-only: headword/grammar/citation/sense/markup/crossref/meta; join-failures = `unattributed`) × EDIT-TYPE (the existing ERRANT/OCR/textcrit crosswalk, where orthography/encoding/diacritic belong). This turns one muddled column into two clean axes (a stronger paper) but reworks attribution + all downstream stats/H1/page. Branch `obs-t-robustness` (Phases 7f campaigns, 7h robustness, 7i silver — committed, not pushed).

- **OBS-T design decisions (2026-06-11, all human-chosen by Gasūns)**: ONE comprehensive paper, NLP/CL venue (released language resource + baselines); full 2014–2026 span across 5 data layers (cfr.tsv form era · csl-orig git diffs · history.txt+printchange · csl-corrections batches · observatory metrics backdrop); **canonical typology = lexicographic microstructure** (classify by damaged dictionary component via the `csl-orig` `<L>` XML-tag join — the headline contribution), with ERRANT/OCR/textual-criticism(Katre) as crosswalk columns; IAST canonical (NFC display, **NFD for diacritic-level edit ops**); temporal train/dev/test split; full real names alias-merged; CC-BY-4.0. OBS-T complements OBS-Q: OBS-Q = who/when/latency from git; OBS-T = what-was-wrong/where/how from correction *content*.
- **OBS-T boundary note**: error *content* leans toward csl-atlas territory, but the object of analysis is corrections/commits/issues over source text → in scope for the observatory per `docs/BOUNDARY_RULES.md`. The *dictionary-structure interpretation* (microstructure) cross-links to csl-atlas rather than living here. Keep generators here; if structure analysis grows, consider an atlas bridge like `reports/obs_rc_atlas_bridge.md`.
- **OBS-T Phase 1 gotchas**: cfr.tsv old/new mix Devanagari+IAST+Latin and the NEW cell embeds inline commentary (`वेदना - no "र"...`) → split heuristically (inferred, review-sample later). Edit ops over NFD inflate retroflex/diacritic distance (ṭ = t+◌̣, so d→ṭ = dist 2); intended for diacritic granularity but the OSA tie-break trace can pick the less-intuitive indel+sub order — refine in the typology phase if needed. Self-contained Deva→IAST transliterator (stdlib only) lives in `scripts/build_correction_events.py`; verified on vedranā→vedanā, ghaṭ, kriyā, duḥkhabhāk. Only 1,580/24,441 form rows carry a parseable "Corrected M/D/YYYY" latency.
- **Boundary decision (2026-06-03)**: `csl-observatory` was narrowed to GitHub/org observability. The dictionary-content pipelines (L0 convention cladogram, forensic MW↔PWG lineage, lexico/R2 sense work, Article 21, sanhw1) were **moved to `../csl-atlas`** — confirmed present there with live history (PR #33). Do not re-add dictionary research here; admission test + pointers in `docs/BOUNDARY_RULES.md` and `docs/DICTIONARY_STRUCTURE_MOVED.md`. Pre-boundary plans archived as `docs/*LEGACY_BROAD_METRICS.md`.
- **Data pipeline**: `scripts/pull_data.py` (GraphQL fetch) → `scripts/compute_metrics.py` (alias-merge onto canonical logins via `scripts/contributors_map.json`) → site CSVs in `observatory/site/src/data/`. `scripts/retry_via_clone.py` is the fallback for repos GraphQL can't serve (bare-clone `--filter=blob:none`, metadata only). `scripts/contributor_stats.py` is the **live-API** leaderboard; `scripts/bus_factor.py` is **offline/reproducible** over the committed CSVs.
- **Observable site** (`observatory/site/`, Framework v1): `npm run build` / `npm run dev` from that dir. Build output (`dist/`) and caches (`.observablehq/`, `src/.observablehq/`) are gitignored — commit only `src/*.md`, `src/data/*.csv`, and the SEO statics `src/{robots.txt,sitemap.xml,observatory-card.png}`. Reactive `${}` values render as `<observablehq-loading>` placeholders in static HTML and hydrate client-side from FileAttachment data; a clean build is the compile-time signal.
- **SEO/static-file gotcha (2026-06-30)**: `observable build` only emits files a **page references** — root statics like `robots.txt`, `sitemap.xml`, and the og:image card (referenced only via absolute-URL meta tags) are NOT copied to `dist/`. Fix: `scripts/postbuild.mjs` wired as npm `postbuild` copies them after build (cross-platform, runs locally + in the Pages CI). SEO `<head>` is a `head({title,data,path})` function in `observablehq.config.js`. **Front-matter gotcha**: Framework's `normalizeFrontMatter` (frontMatter.js) WHITELISTS keys (`title/sidebar/toc/index/keywords/draft/sql/head/header/footer/pager/style/theme`) and silently drops everything else — so a per-page `description:` in `.md` front matter NEVER reaches the head function (`data` only carries title+toc). Per-page descriptions therefore live in a `PAGE_DESCRIPTIONS` route→string map in the config, looked up by `path`; `data.title` still drives og:title. Regenerate the card with `python scripts/make_social_card.py` and the sitemap with `python scripts/make_sitemap.py` after branding/page changes (committed artifacts, not built in CI).
- **Bus-factor headline** (current data): 65/76 repos have bus factor 1; funderburkjim alone = 51.8% of all contributions; core trio = 97.6%; Gini 0.86; 1/2/3 people cover 50/80/90% of work. Page math in `community.md` mirrors `scripts/bus_factor.py` so site and `reports/bus_factor.md` stay in sync.
- **Repo-health headline** (current data): 41/76 repos (53%) have no license at all, 21 are `NOASSERTION`, only 14 recognised SPDX; 46/76 default to `master`; 5 lack a description; 6 disposable `temp_*`/`test_*`/legacy repos are live; 0 stale (all pushed within ~3 weeks); only 5 fully clean. The org's risk is hygiene + concentration, not abandonment. `repo-health.md` site page mirrors `scripts/repo_health.py`.
- **Taxonomy-adoption headline** (current data): 5,324 issues (89 PRs excluded); 89% carry a type label, 83% severity, 79% milestone, **63% fully conformant** (1 type + 1 severity + milestone, per org CLAUDE.md). Adoption curve: 11–25% conformant 2014–18 → 92% peak 2025 → 39% dip 2026 (recent backlog, un-milestoned). 324 over-typed; 999 carry one of **54 distinct stray labels** (`Documentation` 195, `Correction submission` 175, `help wanted` 92, many `domain:*` — normalise/fold/delete candidates). Section on `/coverage` mirrors `scripts/taxonomy_adoption.py`.
- **Tooling Roadmap project #9 structure** (org project, node id `PVT_kwDOAGGnOc4BXy9n`): a `Category` single-select field (field id `PVTSSF_lADOAGGnOc4BXy9nzhUwIcU`; options `Finding`=`95d773e8`, `Action`=`8524d383`) splits the observatory items into **Findings (#22–#25)** = the four analysis reports, and **Actions (#15–#21)** = the seven hygiene follow-ups. To get the two-column board: open a board view → **Group by → Category** (UI-only; the API can set the field + values but cannot configure a view's grouping). Verified 11/11 by reading values back off the board. **GraphQL gotcha**: per-item calls were unbearably slow under the TLS-timeout flakiness; the winning approach was **3 batched/aliased requests** (fetch all item-ids → one aliased `updateProjectV2ItemFieldValue` mutation → one read-back) — far fewer calls to get through a flaky network. Also: `gh project item-add` throws intermittent "unknown owner type"; use the `addProjectV2ItemById` GraphQL mutation instead.
- **Velocity-timeline headline** (current data): 9,877 commits / 5,324 issues opened over 2014–2026, but the busiest year (2021) drew only **11 distinct authors** — volume-per-person, not a growing base (reinforces bus-factor). PRs barely register: 89 in 13 years, 59% of them in 2026 (first PR 2019) — the org runs on direct commits + issues. Open-issue backlog peaked at 1,742 in 2025 (csl-orig correction wave) then fell to 913 in 2026 — correction-campaign signature. **NB**: `unique_authors` in `timeseries_annual/monthly.csv` is per-repo and must NOT be summed for an org total (double-counts); recompute distinct authors from `commits.csv` (author_login/date). `scripts/velocity_timeline.py` does this; the Activity-page authors chart was fixed to match.
- **Finding pattern (established, repeatable)**: offline script over a committed `src/data/*.csv` → `reports/<name>.md` + `src/data/<name>.csv` → Observable page/section (new page registered in `observablehq.config.js`, or a section added to an existing page), with `${}` KPI cards + Plot charts whose JS recomputes the same numbers as the script (cross-check + no drift). Four findings shipped this way: bus-factor (Community page), repo-health (new Repo Health page), taxonomy-adoption (Issue Taxonomy page), velocity-timeline (Activity page). **PowerShell gotcha**: a literal `&` in a `git commit -m @'...'@` here-string breaks parsing (git sees the message as pathspecs) — write "and" or use `git commit -F <file>`.

## ✅ Completed (Recent only)

- 2026-07-03: **Phase-2 rethink G1+G2 shipped** (Fable 5, `claude-fable-5`, direct to main). **G1 issue-lifecycle track** (commit `869c313`): [`scripts/issue_lifecycle.py`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/scripts/issue_lifecycle.py) (offline over `issues.csv`) → [`reports/issue_lifecycle.md`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/reports/issue_lifecycle.md) + 4 CSVs + new `/lifecycle` page (survival heatmap, backlog age pyramid, latency band, per-repo dots). Headlines: median close **6 d** but p90 **349 d**; **23%** of issues still open at 1 year; **620/913** open issues are 4+ years old; **178 silent** (0 comments); 2014 cohort still ~45% open after 4 years. Time-to-first-response = API-gated extension (no comment timestamps offline). **G2 delta monitor** (commit `1fcc41d`): [`scripts/monitor_deltas.py`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/scripts/monitor_deltas.py) compares working-tree snapshot vs HEAD (backlog/silent/stale/repos/contributors/bus-factor/conformance) → [`reports/monitor_digest.md`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/reports/monitor_digest.md) + `$GITHUB_STEP_SUMMARY`; wired into `refresh-observatory.yml` BEFORE the refresh commit (so HEAD = previous snapshot); alert path tested with a mutated `issues.csv`. Also: `data_index.py` now catalogs the 4 lifecycle CSVs + previously-missing `pwg_citation_coverage.json` (50 files OK); sitemap 21 urls; build 20 pages / 62 links; smoke + repo-health regression green. Roadmap Workstream G added; H089–H092 minted in Uprava.
- 2026-07-02: **Fable window S3 — decisions-master re-adjudication + A13 go/no-go** (Fable 5, `claude-fable-5`). [`docs/CROSS_REPO_DECISIONS.md`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/docs/CROSS_REPO_DECISIONS.md) fully refreshed against live repo state (6 read-only verification sweeps, Sonnet-tier Explore agents, over MWS/csl-atlas/VisualDCS/WhitneyRoots/csl-apidev/csl-corrections/IndologyScholars/BookIndex/RuWritingStyles/SanskritKaraoke/CommentaryStrategies/csl-observatory): **12 of the 14-06 items closed** (token misread, apidev #26 posted, R2+PR#88 applied, D2 decided, A4/A7 approved, KRM consistent, Salt Q6 live, clean-URL settled, F2/F5 decided, SK PR#9 merged); survivors re-ranked with per-item Fable verdicts (recommend/decide/park) — headline re-rank: **ONE second-annotator recruit now tops Tier 1** (unblocks A12 κ + A16 G5 + A44 IRR + IndologyScholars IRR at once); new concrete recommendations: Pāṇini `<ls>` cites → ashtadhyayi.com sūtra URLs (agent-doable, kills the scan-scheme blocker), WhitneyRoots `dātta`→collapse to `datta`, H5 divaraTa via `/cologne-correction-queue`, B2 reassigned agent-side, VisualDCS adapter + MWS W2 G5 passes moved to agent-ready. **A13 verdict: GO — conditional** ([`article/A13_review_fable5.md`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/article/A13_review_fable5.md)): submit to IIJ decoupled from A14; blockers = 1 agent fix session (numbers/bibliography/IIJ format/CSL) + 4 M.G. decisions (quotes permissions, byline, sharp passages, decoupling) + email submission.
- 2026-07-01: **OBS-T (A12) Zenodo DOI minted + venue chosen.** Concept DOI [10.5281/zenodo.15834721](https://doi.org/10.5281/zenodo.15834721) (CC-BY-4.0, `obs-t-data-v1.0.0`) minted; wired into `reports/obs_t_paper_draft.md` §10 + footer, `CITATION.cff` (`identifiers`), and README citation (commits `1751ef9`/`655f0d2`). Venue decided: **LREC-COLING** (paper footer + `Uprava/ARTICLES.md` A12 + GTD updated). A12 is now one gate from submission — the human IAA (Cohen κ) second-annotator pass is the only blocker left.
- 2026-07-01: **Org-wide social cards + dictionary landing pages (cross-repo, cluster point — tooling lives outside this repo per boundary).** Audited all 14 org `github.io` sites: only observatory/atlas/guides had cards. Added a shared `cdsl-card.png` + default og:image (`_config.yml`) to the org root [`sanskrit-lexicon.github.io`](https://github.com/sanskrit-lexicon/sanskrit-lexicon.github.io) and extended its **domain-root `robots.txt`** (authoritative for the whole host) to advertise the observatory + atlas + guides sitemaps. csl-app: og:image + Twitter card ([PR #48](https://github.com/sanskrit-lexicon/csl-app/pull/48) merged). Built a reusable SEO/UX landing-page generator and rolled `index.html` out to **21 dictionary repos** (MWS/PWG 404-fix + 19 more), each PR-merged + Pages-enabled + live-verified. **Gotcha:** GitHub's default Jekyll build *fails* on dictionary repos (large data → "Page build failed"); each needs an empty **`.nojekyll`** to serve static HTML. **Generator + card + rollout scripts committed centrally** to [`sanskrit-lexicon.github.io/tools/dict-pages/`](https://github.com/sanskrit-lexicon/sanskrit-lexicon.github.io/tree/main/tools/dict-pages) (commit `87fe834`), co-located with the shared card. **Org-root landing page** rebuilt as a styled hub ([`index.html`](https://github.com/sanskrit-lexicon/sanskrit-lexicon.github.io/blob/main/index.html), commits `a924b82`/`34a4368`) — grouped directory linking all 21 dict pages + the app/guides/atlas/observatory sites; generated from the same `DICTS` table via `gen_dict_page.py --hub`, live-verified (25 cards). **Mobile-verified** (measured `scrollWidth`≤`innerWidth`, no overflow; the apparent clipping in screenshots was a headless-Chrome ~484px min-viewport crop artifact, not a real bug) and hardened the grid to `minmax(min(100%,260px),1fr)` for ultra-narrow (<300px) screens (commit `3c3a26d`). Cross-repo rollup logged in [`Uprava/GTD_NEXT_ACTIONS.md`](https://github.com/gasyoun/Uprava/blob/main/GTD_NEXT_ACTIONS.md). Per-dict metadata is agent-supplied from standard scholarship — maintainers should sanity-check less-common entries. **CDN warmed** for all 22 pages (hub + 21 dicts, all sharing the one `cdsl-card.png`); platform-debugger priming (FB/LinkedIn) stays an MG follow-up — no API token for programmatic re-scrape. **Domain-root sitemap** added (commit `5680ea6`): [`/sitemap.xml`](https://sanskrit-lexicon.github.io/sitemap.xml) index → [`/dictionaries.xml`](https://sanskrit-lexicon.github.io/dictionaries.xml) (22 urls: hub + 21 dict pages) + the guides/observatory/atlas sitemaps; advertised in the domain-root `robots.txt`. Generated via `gen_dict_page.py --sitemap`. Live-verified. **Full coverage reached (audit-driven):** an audit cross-referencing Cologne dict codes vs org repos found 10 dictionaries/reference works without pages; extended the generator (+ new "Reference works, glossaries & indexes" group) and rolled out to **VCP, BHS, BOP, VEI, IEG, INM, MCI, ACC, KRM, FRI** — now **31 dictionary landing pages** + hub (35 cards) + sitemap (32 urls), all live-verified. Metadata pulled from the CDSL homepage. `.nojekyll` shipped in each branch so first build serves statically (no build-fail).
- 2026-06-30: **Site SEO + discoverability foundation shipped** (commits `f9ea6a5`, `265345f`; pushed; Pages auto-deploys on `observatory/site/**`). `observablehq.config.js` now injects a per-page `<head>()` — canonical, meta description (per-page front-matter `description:` with a site default), Open Graph, Twitter `summary_large_image` — for origin `https://sanskrit-lexicon.github.io/csl-observatory/`. Added a 1200×630 social card (`src/observatory-card.png`, `scripts/make_social_card.py`, PIL), `sitemap.xml` (19 urls, `scripts/make_sitemap.py` parses the config page list) + `robots.txt`, and `scripts/postbuild.mjs` (npm `postbuild`) to emit the three root statics into `dist/`. UX consistency: header/package "12 years"→"13 years" to match the homepage. Roadmaps: OBSERVATORY_ROADMAP **E6** + ROADMAP **PR5** (both `active`). **Per-page descriptions DONE** (commit `bfd4982`): all 19 pages emit a distinct meta/og description via a `PAGE_DESCRIPTIONS` route map in the config — NOT front matter (see gotcha below). Build clean (19 pages, 58 links); statics + tags verified in `dist/`.
- 2026-06-27: **Tier C P4 — `cologne-bug-triage` sweep complete.** 84 open bug issues triaged across 22 repos (62 agents). ~24 comments posted; ~10 new Tier A bugs found (added to roadmap); ~8 confirmed already-fixed; ~18 skipped (active threads / no source data); 9 network failures logged for re-run. Findings digest: [`docs/bug-triage-findings.md`](docs/bug-triage-findings.md). AGENT_ROADMAP updated.
- 2026-06-27: **Tier C P3 — `cologne-question-research` sweep complete.** ~130 question issues across org. ~73 comments posted (concrete data only, collapsible `<details>`); ~57 skipped. Findings digest: [`docs/question-research-findings.md`](docs/question-research-findings.md). Policy: no "inconclusive" posts.
- 2026-06-27: **Bot-noise policy established.** Phase 3.5 gate in both skills (no post without concrete data), `<details>` collapsible wrapper on all agent comments, `question-research-findings.md` + `bug-triage-findings.md` as non-GitHub digest store.
- 2026-06-26: **OBS-T PR/report preflight verified.** GitHub PR #33 is merged;
  `reports/obs_t_typology.md` is already generated from committed data (current
  two-axis 52,498-event version). Checks passed: OBS-T regression, deterministic
  baselines, site visualization smoke. No scoring run was attempted because the
  gold/error validation sheets remain human-annotation gated.

- 2026-06-26: **A12 manuscript refreshed from released OBS-T outputs.** Updated
  `paper-obs-t-error-typology.md` and the README summary away from the stale
  50,953-event / 210-corrector snapshot and aligned the manuscript with
  `reports/obs_t_typology.md`, `reports/obs_t_rigor.md`, and
  `reports/obs_t_baselines.md`. Remaining A12 blocker is human annotation / second
  annotator for validation and κ, not data drift.

- 2026-06-26: **A14 metric-definition audit resolved.** The article now explicitly
  keeps the 2026-05-07 archived REST/GraphQL snapshot as its basis, updates the
  captured commit total to the retry-recovered 3,979 value, and warns against
  replacing it with 2026-06 transformed CSV row counts (`76 / 5,413 / 9,877 / 209`)
  because those are a different basis and `209` is not distinct contributors.

- 2026-06-25: **Stale data catalog refresh fix implemented.** Audit found `observatory/site/src/data/data_index.csv` byte counts stale and `.github/workflows/refresh-observatory.yml` not regenerating/checking the catalog before refresh commits. Regenerated the current public data catalog (45 rows) and hardened the refresh workflow to run `python scripts/data_index.py` plus `python scripts/data_index.py --check` before `Commit data refresh`. Verification passed: `python scripts/data_index.py --check`; `python scripts/refresh_observatory.py --check-only --skip-site-build`; `python -m compileall -q scripts observatory`; `python scripts/repo_health_regression.py`; `python scripts/obs_t_regression.py`; `python scripts/site_visualization_smoke.py`; `npm.cmd run build` from `observatory/site` (18 pages, 51 links); `git diff --check` (exit 0; only LF-normalization warning for regenerated `data_index.csv`).

- 2026-06-20: **Refresh check-only/catalog repair implemented.** Fixed stale public `data_index.csv` byte counts for `repo_health.csv`/`repos.csv`; changed `scripts/refresh_observatory.py --check-only` so it writes no manifest/summary by default, while refresh mode still writes the default report artifacts and explicit `--manifest`/`--summary` still work. Verification passed: `python scripts/data_index.py --check`; `python scripts/refresh_observatory.py --dry-run --check-only --skip-site-build`; `python scripts/refresh_observatory.py --check-only --skip-site-build`; `python scripts/refresh_observatory.py --check-only --skip-site-build --manifest reports/tmp_check.json` (then removed temp file); `npm run build` from `observatory/site`. Confirmed no untracked refresh report files remained.

- 2026-06-19: **Audit repair pass implemented on `rh1-license-rollout`** (repo-only; no external org mutations). Fixed Dependabot auto-merge to validate before merge and hold semver-major updates; made refresh commits/pushes happen before Pages deploy and fail loudly; hardened `observatory/fetch.py` + `check_coverage.py` so attempted repos and fetch failures cannot silently disappear from `repos.csv`; made `build_people.py` derive its repo universe from `data/repos.csv`; added `manifest.json` to the public data catalog and regenerated `data_index.csv`; made RH1 helper scripts exit nonzero on partial failures and updated dual-license wording to `licenses/GPL-3.0.txt`; updated `CHANGELOG.md`. Verification passed: `python -m compileall -q scripts observatory`; `python scripts/data_index.py --check`; `python scripts/refresh_observatory.py --check-only --skip-site-build`; `repo_health_regression.py`; `obs_t_regression.py`; `site_visualization_smoke.py`; `npm run build` (18 pages, 51 links); `git diff --check` (only line-ending normalization warnings). `scripts/rh1_infra_probe.py` is tracked in this branch and was left untouched.

- 2026-06-17: **Landed the Codex maintainer-track working tree + recorded RH1/RH3 decisions** (branch `codex/readme-typology-20260612`, not pushed). The branch carried ~56 modified + 34 new files from a separate Codex session (maintainer dashboards: ops-command/repository-risk/metadata-readiness/taxonomy-triage/community-continuity/workflow-health/repo-metadata/reproducibility/obs-t-maintenance; scripts: workflow_health/repo_metadata_snapshot/data_index/refresh_observatory/check_workspace/_gh + regression+smoke guards; decision packets; regenerated OBS-T two-axis data; DATA_LICENSE.md). **All weekly checks pass**: repo_health_regression OK, site_visualization_smoke OK (6 pages/30 plots), site builds clean (18 pages, 50 links), repo_metadata.csv 76 rows = repos.csv. **Commit split** (HEAD was already LF; 270k working-tree churn was CRLF illusion + genuine data regen): `ca62dc2` .gitattributes eol=lf alone, `6f573f1` content, `853d0ac` RH1/RH3 decision recording. `.claude/settings.local.json` gitignored. **MG approved RH1 license policy + RH3 archive list** — see the gated-rollout next-step above. Nothing pushed (not requested).

- 2026-06-11: **OBS-T Phase 3 — microstructure component attribution** (the canonical typology). `scripts/attribute_components.py`: assigns each event an `error_component` (headword/grammar/citation/sense/markup/crossref/meta/encoding/orthography). **git layer 26,512 = 100% positionally derived** (component read off the changed line's XML tags); **form layer 12.9% derived** via a csl-orig headword join, rest empirical-cluster fallback (inferred). Overall 50,953 events, **58% derived / 42% inferred**, evidence-labeled. Outputs: `correction_events_typed.csv`, `obs_t_component.csv` (component×layer×dict), `correction_events_typed.meta.json`. **Three data findings forced design changes**: (1) cfr cells are **mixed-encoding** — PW (largest) is **Harvard-Kyoto**, not Devanagari/SLP1, so Phase 1 got an HK→IAST transliterator (`normalize_to_iast`; HK z=ś,S=ṣ,R=ṛ,T=ṭ) and re-ran; (2) cfr "L code" is free-text `[L=5590] [p= 1-299]`; (3) more importantly the 2014-era sequential `<L>` ids have **drifted** vs today's csl-orig (cfr L=4477→utkaṇṭhā, current 4477=utkalaṃ), so the join uses the **stable headword** (+ corrected value + first token) as key, not the lcode. Added `--merge-only` to `reconstruct_git_events.py` to rebuild the all-layers table after a Phase-1 rerun without re-mining git. The low form link rate is a reportable finding (legacy correction data only partly machine-linkable). Phase 3.1 = fuzzy headword match to raise it.

- 2026-06-11: **OBS-T Phase 2 — git-diff reconstruction (2019→2026)**. `scripts/reconstruct_git_events.py`: mines correction-classified `../csl-orig` v02 commits, parses unified diffs into old/new line pairs, attributes each to its record (`<L>` code + `<k1>`, SLP1→IAST headword only) from hunk context, reuses OBS-Q `classify()`/resolver + Phase-1 edit-op machinery. **26,512 git events from 1,243 commits (287 bulk reformats skipped), 2019-11-04→2026-05-30**; merged `correction_events_all.csv` = **50,953 events** (24,441 form + 26,512 git). BOM-free, verified (proper lcodes/IAST headwords/correctors). **Two perf fixes were required**: (1) `classify()` lets large reformat/re-encode commits through as "corrections" → cap at MAX_PAIRS_PER_COMMIT=250 (top ~12 commits had 400k–20M changed lines, a clear cliff above normal fixes); (2) `subprocess.run(capture_output=True)` buffered a bulk commit's multi-GB diff into RAM (hit ~2GB) BEFORE the cap could act → rewrote `parse_show` to **stream via Popen line-by-line and kill the proc on cap** (memory now bounded ~31MB). Outputs: `correction_events_git.csv`, `correction_events_all.csv`, `correction_events_git.meta.json`.

- 2026-06-11: **OBS-T design spec + Phase 1 event table** (new error-typology track). Wrote `docs/ERROR_TYPOLOGY_DESIGN.md` (full spec: 5 data layers, unified event schema, microstructure typology + crosswalks, statistics catalogue, NLP resource plan, 7-phase plan). Built `scripts/build_correction_events.py` (stdlib-only): parses `../CORRECTIONS/cfr.tsv`, splits dirty NEW cell, self-contained Devanagari→IAST, edit-op trace over NFD (Damerau/OSA), alias-merge correctors, submit→corrected latency → `observatory/site/src/data/correction_events.csv` (**24,441 events, 0 malformed, 2014-03-18→2019-07-21**) + `.meta.json` envelope + `data/schema/correction-event.schema.json`. Verified BOM-free, transliteration correct, sample rows clean. Empirical clusters: typo 18,519 · ocr-print 1,793 · capitalization 1,473 · iast-diacritic 884 · markup 765 · as-number 493 · reference 251. Not yet committed (awaiting user review).

- 2026-06-06: **Dashboard deployed live + refresh automated** (autonomous, Phase 6). GitHub Pages enabled with the Actions source (`build_type=workflow`); new `.github/workflows/deploy.yml` builds the Observable site from the committed snapshot and publishes on push-to-main affecting `observatory/site/**` (commit `8cf663d`). Deploy run succeeded; **live + verified** at https://sanskrit-lexicon.github.io/csl-observatory/ (KPI cards + Key findings render, no 404). Asset links are relative (`./_observablehq/`), so the `/csl-observatory/` subpath needs no base-path config. Fixed `refresh-observatory.yml`: dropped the dead `data/L0` copy block, added a **Regenerate findings** step (runs the 5 finding scripts after the data copy), commits site data + reports, pinned `upload-pages-artifact@v3` + `deploy-pages@v4` (the `@v5` refs didn't exist — latent bug since it had never run); `refresh.yml` made manual-only so the two refreshers don't race (commit `75577d5`). **Decision noted**: data was only 4 days old (2026-06-01 snapshot, root `data/` == site data) and the dual fetch pipeline is fragile under the session's TLS flakiness, so deployed on the current snapshot and folded refresh into CI rather than forcing a manual re-fetch.
- 2026-06-05: **Tidy + write-up batch** (autonomous) — `reports/README.md` index of the 5 findings + synthesis (commit `715cbbf`); polished `reports/synthesis.md` to publishable (abstract + byline + citation, same commit); `docs/ARCHIVE.md` consolidating the 3 legacy broad-metrics archives + 12 dictionary-research "Moved" stubs (commit `134981e`); refreshed root `README.md` to authoritative numbers + Findings section + reports/project-#9 links (commit `eee4b1e`); current-status banner on stale `STATUS.md`. All dictionary docs confirmed already 5-line "Moved" stubs (content in csl-atlas) — left in place, indexed not deleted.
- 2026-06-05: **Filed the four findings as roadmap issues** (human-authorised) — #22 bus-factor, #23 repo-health, #24 taxonomy-adoption, #25 velocity-timeline; each `documentation`/`minor` → Developer Experience, linking its report + live site page + headline numbers + actionable follow-ups (#15–#21) + synthesis. All four added to org **Tooling Roadmap project #9** (verified via GraphQL `projectItems`). Findings tracker on the roadmap = #22–#25; actionable hygiene work = #15–#21.
- 2026-06-05: **Filed the seven hygiene issues** (human-authorised) — #15–#21 in `csl-observatory`, each with one type + one severity + one milestone per the tooling taxonomy, each a trackable checklist linking to its source report. All seven added to org **Tooling Roadmap project #9** (verified via GraphQL `projectItems`). `docs/hygiene_issues_draft.md` marked FILED (commit `e374273`). **Network note**: GitHub API threw persistent TLS handshake timeouts; `gh project item-add` also threw intermittent "unknown owner type" — worked around with retry wrappers + the `addProjectV2ItemById` GraphQL mutation (bypasses the gh owner-resolution bug). Filing was robust once wrapped in retries; treat both as transient/known and don't stop on them.
- 2026-06-05: **Surfaced the four findings on the site overview** (`index.md` "Key findings" section, inline-computed from the finding CSVs) + added the missing Repo Health page to the index nav (commit `699d42c`). Build clean: 8 pages, 24 links.
- 2026-06-05: **Autonomous batch (one "go")** — three artifacts shipped + pushed: (1) **contributor-identity worksheet** `scripts/contributor_identity.py` → `reports/contributor_identity.md` + csv (commit `e81bfec`) — 0/16 authors have a registered ORCID, 7 named await registration, 9 to identify; (2) **synthesis report** `reports/synthesis.md` tying all four findings together (commit `8618f76`); (3) **drafted (not filed) org-hygiene issues** `docs/hygiene_issues_draft.md` — 7 ready-to-file issues with taxonomy labels + care notes (commit `99a7e3b`). One transient SSL push failure on the last commit, succeeded on retry. Filing the issues + ORCID registration are left as human-authorised steps.
- 2026-06-05: **Org-wide velocity & health timeline shipped end-to-end** — `scripts/velocity_timeline.py` (offline, over `timeseries_annual.csv` + `commits.csv`) → `reports/velocity_timeline.md` + `src/data/velocity_timeline.csv` (commit `b8d5258`); "Org-wide velocity & health" section added to the `/activity` page + fixed its double-counting annual-authors chart (commit `a1187e5`). Build clean: 8 pages, 23 links. Fourth (final planned) in-scope finding.
- 2026-06-05: **Issue-taxonomy adoption & conformance shipped end-to-end** — `scripts/taxonomy_adoption.py` (offline, over `issues.csv`) → `reports/taxonomy_adoption.md` + `src/data/taxonomy_adoption.csv` (commit `9606570`); adoption section added to the existing `/coverage` (Issue Taxonomy) page (commit `daef4b0`). Build clean: 8 pages, 22 links. Third in-scope finding under the boundary.
- 2026-06-05: **Repository-health audit shipped end-to-end** — `scripts/repo_health.py` (offline, over `repos.csv`) → `reports/repo_health.md` + `src/data/repo_health.csv` (commit `a0ba200`); new **Repo Health** site page `src/repo-health.md` + nav entry in `observablehq.config.js` (commit `c9563b0`). Build clean: 8 pages, 22 links. Second in-scope finding under the boundary.
- 2026-06-05: **Tracked `observatory/site/package-lock.json`** (lockfileVersion 3) for reproducible site builds — commit `f35d828`. (Resolved the open queue question.)
- 2026-06-05: **Refreshed `.ai_state.md`** from stale Phase L0 content to the org-observatory scope — commit `884518f`.
- 2026-06-05: **Wired the bus-factor finding into the Observable site** (`community.md`) — replaced the rough top-3-share heuristic with the canonical `bus_factor.csv`: KPI cards (bus-factor-1 repos, people-for-half, core-trio share, Gini) + per-repo top-share chart coloured by single-point-of-failure status. Verified with `npm run build` (7 pages, 19 links, no errors). Commit `ce27110`.
- 2026-06-05: **Bus-factor / contributor-concentration finding** — `scripts/bus_factor.py` (offline, over `observatory/site/src/data/contributors.csv`) → `reports/bus_factor.md` + `observatory/site/src/data/bus_factor.csv`. First in-scope finding under the new boundary. Commit `7fefa5e`.
- 2026-06-05: Housekeeping — gitignored `observatory/site/src/.observablehq/` build cache (`4f5c1c1`); tracked `scripts/retry_via_clone.py` (`05611ce`). All pushed to `origin/main`.
- 2026-06-04/03: **Boundary cleanup** merged via PR #14 (`047eb94`) + `docs: update boundary status` (`8b000c9`) — narrowed observatory to org scope; dictionary-research scripts/data/docs moved to `csl-atlas`.
