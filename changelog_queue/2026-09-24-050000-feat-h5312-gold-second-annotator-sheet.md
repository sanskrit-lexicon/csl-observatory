- feat(obs-t): H5312 — blind second-annotator sheet + decisions ingest so the gold sample's κ can finally flow through the harness. [`validation/second_annotator_sheet.html`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/validation/second_annotator_sheet.html) (390/390 gold rows, built by new [`scripts/obs_t_second_sheet.py`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/scripts/obs_t_second_sheet.py) from the pre-registered blind input `validation/gold_sample_blind.json`) presents each correction with BOTH typology axes as annotation targets — axis A location (8 chips, fills `gold_component_2`), axis B edit-type (9 chips, recorded) — with localStorage persistence, filter bar and a decisions.json download; the first annotator's labels are physically absent from the payload (leak-checked: no annotation column, no pre-selected chip, zero retired-axis label values in the rendered HTML). [`scripts/obs_t_gold.py --ingest`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/scripts/obs_t_gold.py) applies such a decisions file fail-closed (schema/sheet_id/vocabulary/row-id validation, clobber guard on existing `gold_component_2`, `--sheet` redirect for sandbox runs, never touches `gold_component`/`notes`); `--score` now resolves the sheet's pre-migration hex event_ids through the H1494 `event_id_crosswalk_v1.csv` (213/390 recovered — previously the auto-join returned `?` for every row and accuracy read 0.0 by construction) and reports the unresolved count instead of hiding it. Own-data canary [`scripts/obs_t_gold_roundtrip_check.py`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/scripts/obs_t_gold_roundtrip_check.py) round-trips [`validation/fixtures/second_annotator_fixture.json`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/validation/fixtures/second_annotator_fixture.json) (marked fixture:true) sheet → decisions → ingest → κ in a throwaway sandbox with four negative controls (bad label, unknown row_id, clobber-without-force, missing sheet) all refusing for the right reason, and asserts the real instrument is untouched. Two pre-existing defects surfaced, NOT fixed here (tracked follow-ups): the sheet's `gold_component` column still carries the retired one-axis labels (`encoding`×184, `orthography`×87 — H1385's pass-A location labels live only in `validation/component_passA.json`), so `--score` now flags loudly that any κ against it is not interpretable until that column is re-based (a human decision); and 177/390 sheet event_ids resolve by neither crosswalk nor unique tuple-join, so full re-keying needs its own repair unit
