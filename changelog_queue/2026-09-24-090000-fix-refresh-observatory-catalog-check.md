- fix: [`.github/workflows/refresh-observatory.yml`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/.github/workflows/refresh-observatory.yml)'s scheduled run had failed every Monday since 2026-09-07 at the "Validate data catalog" step (`python scripts/data_index.py --check` exiting 1 with `missing catalog entries: error_recapture_calibrated.csv, error_recapture_lowm.csv`). Root cause: PR [#207](https://github.com/sanskrit-lexicon/csl-observatory/pull/207) (H3986) committed both CSVs from [`scripts/lowm_estimators.py`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/scripts/lowm_estimators.py) but never added their `CATALOG` entries to [`scripts/data_index.py`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/scripts/data_index.py) — the files were never renamed or dropped, the catalog manifest was simply never told about them. Added the two missing `Entry` rows and regenerated [`observatory/site/src/data/data_index.csv`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/observatory/site/src/data/data_index.csv); `--check` now passes. This unblocks the weekly refresh, which regenerates the data the README.md "Headline numbers" table's report-sourced rows (contribution concentration, most active repo, peak years, dominant work type) read from — those rows had been stuck on the stale 76-repo snapshot since the cron started failing.
