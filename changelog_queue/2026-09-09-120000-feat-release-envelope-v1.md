- feat(release-envelope): adopt release-envelope-v1 for the OBS-T correction-event corpus — [`data/manifest/envelopes/v1.13.3.envelope.json`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/data/manifest/envelopes/v1.13.3.envelope.json) pins `correction_events_release.csv`/`.meta.json` (git-lfs oid digests) and the `correction_events_final.csv` source input by commit self-pin, per Uprava `docs/SPEC_RELEASE_ENVELOPE_V6_PORTFOLIO_2026.md` (kosha `data-v0.5.0` reference implementation). [`scripts/envelope_check.py`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/scripts/envelope_check.py) re-derives every declared digest from bytes on disk (LFS-pointer-aware) and exits 0 only on all-pass — 8/8 PASS against the current `v1.13.3` tree, and the pre-existing `scripts/obs_t_regression.py` release gate still passes independently. Additive only: no canonical store touched, one release adapted (H4257)
