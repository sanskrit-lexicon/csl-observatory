- test(recapture): H5221 — generative acceptance test for the recapture control suite. `--selftest` in [`scripts/recapture_sensitivity.py`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/scripts/recapture_sensitivity.py) gains four checks that test the analytic path against the mechanism's *definition* rather than against itself: an event-by-event simulation (4 settings × 200,000 sites, per-error find-and-fix in era 1, survivors found in era 2) that the joint, closed-form and sequential cells must match within 4 SE (worst 1.39), with a power check that rejects a no-removal survival model at ≥ 39.7 SE; textbook-Chapman quartiles from site-by-site capture that `run_cells` must reproduce (worst 1.83 SE); and exp(log-likelihood) against the simulated frequency of the observed table (worst 0.91 SE). New [`scripts/recapture_mutation_check.py`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/scripts/recapture_mutation_check.py) re-applies the three review-3 survivors named in [`reports/recapture_sensitivity.md`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/reports/recapture_sensitivity.md) §9 item 6 (era-2 survival exponent in both derivations, Petersen for Chapman, negated log-likelihood) plus two companions: all five now FAIL the selftest; before this change the three named ones passed. No published estimate, CSV row or §9a disclosure changed; §9 item 6 records the new test and says plainly that the other seven survivors are unnamed and not re-tested
