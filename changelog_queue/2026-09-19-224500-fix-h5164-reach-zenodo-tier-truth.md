- fix(reach): the `/reach` Zenodo tier reports record-verified state instead of hardcoded BLOCKED boilerplate — `external_reach.py` flips the CSV tier (`blocked`→`measured`), section heading, headline reading and closing bullet on the verified record 21965649 (concept `10.5281/zenodo.21346705`, minted 2026-08-16, logged-out 200 re-check 19-09-2026, downloads 10 / views 70, cache `2026-09/`); `/reach` heading made status-conditional the same way; generator now emits the dated `_Created · Last updated` header and writes the CSV with LF endings per `.gitattributes` (H5164)
