- fix(H5424): restore the removed weekly cron on [`.github/workflows/refresh.yml`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/.github/workflows/refresh.yml) (Mondays 06:00 UTC, 3h offset from `refresh-observatory.yml`'s 03:00 slot, still sharing the `observatory-data` concurrency group so the two pipelines queue rather than race) — MG ruling: "I don't know why the cron was removed, bring it back, is still show it, no need to freeze". No removal commit was found in git history (the schedule block was dropped outside a tracked commit sometime between the 2026-05-07 bootstrap and the 2026-06-01 snapshot). Ran the pipeline once via `workflow_dispatch` to produce a fresh `data/summary.json`. New [`scripts/update_headline_numbers.py`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/scripts/update_headline_numbers.py), wired as the pipeline's last step, regenerates the README.md summary line and STATUS.md headline table from `data/summary.json` + `data/contributors.json` (human, non-bot count) + `data/manifest.json`/`correction_events_release.csv` (OBS-T corpus) — replacing hand-typed numbers that had drifted from the committed snapshot (76 vs 78 repos, 5,413 vs 5,482 issues+PRs, 16 vs 48 contributors, 50,953 vs 52,498 OBS-T events)
