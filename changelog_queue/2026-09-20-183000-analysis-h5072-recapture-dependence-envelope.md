- analysis(recapture): H5072 — bound the recapture conclusions under dependent correction sources. New [`scripts/recapture_sensitivity.py`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/scripts/recapture_sensitivity.py) + [`reports/recapture_sensitivity.md`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/reports/recapture_sensitivity.md), preregistered ahead of its own numbers in [`reports/recapture_sensitivity_prereg.md`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/reports/recapture_sensitivity_prereg.md) (separate earlier commit, so the ordering is checkable in history). Every violation of the *independence between the two eras* enters the Chapman estimate as ONE dependence factor γ — closure and linkage error explicitly do NOT, and are outside this envelope — and a two-list table cannot identify γ: all three observed counts are spent on (N, p1, p2), the fit is exact at every γ (residual 1.8e-12) and the likelihood discriminates by 0.15 log-units across a fourfold range of N̂, far below the conventional 2-unit threshold (a weak monotone slope, not exact flatness; exact non-identification holds for the unseen cell under the conditional formulations). Consequences, all exact inversions rather than simulations: a **12.8% departure from independence exhausts pw's entire 95% CI**; the **pw/mw ordering by remaining error sites reverses at γ = 0.590**, and control C puts the sequential-removal mechanism at γ = 0.583 when sites average 1.5 errors — the flip is reachable by a mechanism `error_recapture.md` already concedes, not by a hypothetical. 76/76 controls PASS (independent-source recovery: bias −1.4%, coverage 93.5% against a now two-sided gate; heterogeneity: γ = 1 + CV² reproduced to machine precision by two mixing families whose cells are built by integration rather than by substituting the identity, nominal-95% coverage collapsing to 20% at CV = 0.50; sequential removal sized for the first time, γ 0.98 → 0.58, and exactly 0 in the one-error-per-site corner where the design cannot estimate at all; and a new control E showing the two mechanisms' γ values must NOT be multiplied — the product can invert the sign of the net bias). Sampling is exact (Beta-recursion binomial, validated against numpy at five parameter settings), stdlib-only per repo convention. §6 reports a bounded negative result: per-site correction counts are **more dispersed than any zero-truncated Poisson–Gamma of that mean can be** (up to 4.0× over that family's variance ceiling, 7 of 8 era-dictionary cells), so no latent Gamma CV can be fitted — but a bounded two-point Poisson mixture does match those moments, and clustered events reproduce them with γ = 1, so the counts leave homogeneous catchability untested rather than refuted. Zero-recapture non-identification preserved unchanged (26 dictionaries). An independent Codex Astra (`gpt-6-astra`) logic critic reviewed the first version and returned FAIL on ten defects; all are fixed, each pinned by a new `--selftest` invariant, and §9a records every deviation from the preregistration — including the one that changes a published verdict (the fragility rule, restored to its literal form, now flags cae). No published estimate is altered: [`reports/error_recapture.md`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/reports/error_recapture.md) gains one cross-reference paragraph and nothing else, and the two new CSVs are registered in `data_index.py`
