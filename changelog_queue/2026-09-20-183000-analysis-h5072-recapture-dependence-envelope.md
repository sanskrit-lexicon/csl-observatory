- analysis(recapture): H5072 — bound the recapture conclusions under dependent correction sources. New [`scripts/recapture_sensitivity.py`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/scripts/recapture_sensitivity.py) + [`reports/recapture_sensitivity.md`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/reports/recapture_sensitivity.md), preregistered ahead of its own numbers in [`reports/recapture_sensitivity_prereg.md`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/reports/recapture_sensitivity_prereg.md) (separate earlier commit, so the ordering is checkable in history). Every independence violation the published report names — sequential occasions, correlated catchability — enters the Chapman estimate as ONE dependence factor γ, and a two-list table cannot identify it: all three observed counts are spent on (N, p1, p2), the fit is exact at every γ (residual 1.8e-12) and the likelihood is flat to 0.15 units across a 16× range of N̂. Consequences, all exact inversions rather than simulations: a **12.8% departure from independence exhausts pw's entire 95% CI**; the **pw/mw ordering by remaining error sites reverses at γ = 0.590**, and control C puts the sequential-removal mechanism at γ = 0.583 when sites average 1.5 errors — the flip is reachable by a mechanism `error_recapture.md` already concedes, not by a hypothetical. 30/30 preregistered controls PASS (independent-source recovery: bias −1.4%, coverage 93.5%; heterogeneity: γ = 1 + CV² reproduced, nominal-95% coverage collapsing to 20% at CV = 0.50; sequential removal sized for the first time, γ 0.98 → 0.58, and exactly 0 in the one-error-per-site corner where the design cannot estimate at all). Sampling is exact (Beta-recursion binomial, validated against numpy at five parameter settings), stdlib-only per repo convention. §6 reports a negative result worth keeping: per-site correction counts are **more dispersed than any zero-truncated Poisson–Gamma of that mean can be** (up to 4.0× over the family's variance ceiling, 7 of 8 era-dictionary cells), so no latent CV can be fitted — two readings (heavy-tailed heterogeneity vs within-site event clustering) that the events CSV's timestamps and authorship could separate, flagged as the next step. Zero-recapture non-identification preserved unchanged (26 dictionaries). No published estimate is altered: [`reports/error_recapture.md`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/reports/error_recapture.md) gains one cross-reference paragraph and nothing else, and the two new CSVs are registered in `data_index.py`
