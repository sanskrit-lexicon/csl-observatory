# Cologne codebase improvement map — prior-art status sweep, 22 verified gaps, ranked roadmap (September 2026)

_Created: 02-09-2026 · Last updated: 02-09-2026_

Executed as [Uprava handoff H3884](https://github.com/gasyoun/Uprava/blob/main/handoffs/H3884-Fable_csl-observatory_cologne-codebase-improvement-map-2026-09_02.09.26.md) (Fable 5.1, 🟡2 medium — Cologne codebase improvement map 2026-09). Answer to "how to improve the Cologne code?" built on prior art, not around it. Every earlier improvement item the org already has (the 50 items of H3487 / GENERATION_MANUAL / DECISIONS_NEEDED / REFRESH / the open COLOGNE and csl-apidev issues / the Cologne-lane handoffs) was **status-verified against the current tips** first; only the residue was re-audited, and every new finding survived a three-lens refutation pass before it entered this map. Executed by Claude Fable 5.1 (`claude-fable-5-1`) as Claude Code workflow run `wf_455b00eb-e36` (13 subagents, all `claude-fable-5-1`: 4 batched status verifiers over the 50 prior-art items, 5 merged finders, 2 batched three-lens refuters, 1 synthesiser at xhigh effort, 1 completeness critic; measured duration 59м 1с; 1,063,750 subagent tokens; 439 tool calls). Predecessor: [CODEBASE_ADVERSARIAL_AUDIT_2026-08.md](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/docs/CODEBASE_ADVERSARIAL_AUDIT_2026-08.md) — [Uprava handoff H3487](https://github.com/gasyoun/Uprava/blob/main/handoffs/H3487-OxAlpha_csl-observatory_cologne-codebase-adversarial-audit_25.08.26.md) (OxAlpha, 🟡2 medium — Cologne codebase adversarial audit 2026-08).

**Read-only pass.** No repo other than this one was modified; nothing was executed against production or live data; `gh` was used only for `run list`, `run view --log-failed`, `pr view` and `issue list`. **Privacy note:** finding B13 concerns submitter e-mail addresses committed to a public repository. No address is reproduced anywhere in this document; counts only.

**How to read it.** §2 is what is already done (do not redo). §3 is the 50-item status table. §4 is the 22 confirmed findings. **§5 is the critic's corrections — read it before launching anything from §6**, because it re-scopes ranks 1, 3, 4, 5, 7 and 9 and corrects the e-mail count. §6 is the ranked roadmap with launchable missions, §7 the quick wins, §8 the decisions a human should make, §9 what was dropped and why, §10 what was not covered.

## 1. Clone manifest (audited tips)

| Repo | Local tip audited | origin/main at run time | Role |
|---|---|---|---|
| [csl-pywork](https://github.com/sanskrit-lexicon/csl-pywork/tree/e460545) | `e460545` | `aefbdac` | generation toolchain (Python/shell, Mako) |
| [csl-orig](https://github.com/sanskrit-lexicon/csl-orig/tree/dee5a03) | `dee5a03` | `30b2ae7` | dictionary data + guard scripts + Actions |
| [csl-websanlexicon](https://github.com/sanskrit-lexicon/csl-websanlexicon/tree/2750327) | `2750327` | `02b45aa` | Mako source of the public web faces (local clone sits on branch `h3636-getword-data-twin`) |
| [csl-apidev](https://github.com/sanskrit-lexicon/csl-apidev/tree/e5ed207) | `e5ed207` | `58b54a0` | flat-root API/display PHP twin |
| [csl-inflect](https://github.com/sanskrit-lexicon/csl-inflect/tree/14cd802) | `14cd802` | `same` | verb/noun inflection generator + PHP lookup |
| [csl-corrections](https://github.com/sanskrit-lexicon/csl-corrections/tree/e363439) | `e363439` | `same` | correction-form intake + nightly reporting (public) |
| [COLOGNE](https://github.com/sanskrit-lexicon/COLOGNE/tree/ece273d) | `ece273d` | `same` | monorepo of offline tooling (iast/, stardict/, issues/) |
| [csl-observatory](https://github.com/sanskrit-lexicon/csl-observatory/tree/d2dea7a) | `d2dea7a` | `same` | this repo — cross-repo health dashboards |
| [cologne-stardict](https://github.com/sanskrit-lexicon/cologne-stardict/tree/0590b83) | `0590b83` | `same` | StarDict/Babylon export |
| [csl-atlas](https://github.com/sanskrit-lexicon/csl-atlas/tree/3f9fe64) | `3f9fe64` | `same` | Node/StarDict export + atlas |
| [sanskrit-util](https://github.com/sanskrit-lexicon/sanskrit-util/tree/a2c4714) | `a2c4714` | `same` | shared transliteration package |
| [csl-json](https://github.com/sanskrit-lexicon/csl-json/tree/c824e1e) | `c824e1e` | `same` | JSON export (Python 2 driver) |
| [hwnorm1](https://github.com/sanskrit-lexicon/hwnorm1/tree/4a80a35) | `4a80a35` | `same` | headword normalisation |
| [csl-app](https://github.com/sanskrit-lexicon/csl-app/tree/a830144) | `a830144` | `same` | app |
| [csl-homepage](https://github.com/sanskrit-lexicon/csl-homepage/tree/4165574) | `4165574` | `same` | homepage |
| [csl-sqlite](https://github.com/sanskrit-lexicon/csl-sqlite/tree/65e9b8d) | `65e9b8d` | `same` | sqlite builds |

**Stale-clone hazard (critic).** Four local clones were behind origin when the finders read them: csl-pywork +3 (the fail-closed PR #84 already merged), csl-websanlexicon +3, csl-apidev +4 (0.4.1/0.4.2, `listhierClass.php`, `basicdisplay.php`, `dictinfo.php`), csl-orig +4 (PR #2894 added 20 lines to `check_generate_dict.sh`, #2895). Line numbers in §4 are valid at the **local** tips; §5 re-anchors the ones that moved. Before acting on any item, re-read the cited lines with `git show origin/main:<path>` (no checkout).

## 2. Already done — do not redo (16 of 50 prior-art items)

| Item | Evidence at tip |
|---|---|
| G2 | csl-orig PR #2894 merged 2026-08-29: .githooks/pre-commit:29,48,58,73 + scripts/check_encoding.py (staged blobs) + check_deletions.sh; install-hooks.sh:42 sets core.hooksPath; guard-backstop.yml backstops; issue #2852 closed. |
| G3 | csl-pywork PR #87 (aefbdac) build_query_index.py writes lines(off,t) table; websan PR #153 querymodel.php:57-83 open_query_index + :144 LIKE on index; p95 162.8→45.5 ms. Live effect awaits Cologne regeneration (REFRESH). |
| D4 | Same PRs as G3 (pywork #87 / websan #153); measured p95 drop recorded. |
| G4 | csl-apidev PR #155 twin-drift.yml:51-56 diffs four PHP twins on every push; drill PR #160 proved it fires. One-directional (apidev checks websan) and PHP-only — XML tables not covered (see ranked #7). |
| G5 | websan PR #150 transcoder.php:40-44,189,195,348-353 raise instead of silent identity; apidev PR #156 byte-identical copy. |
| G6 | apidev PRs #159/#161 fixed HTTP status semantics (0.4.2); issue #153 remains OPEN (updated 2026-09-02) — maintainer to close, agents never close issues. |
| D1 | websan .htaccess:12-29 mod_expires + Cache-Control headers. |
| D2 | defer on script tags in webtc templates; mobile1 deliberately excluded. |
| D3 | APCu transcoder cache present and SAME in both twins. |
| G7 | apidev PR #162 basicdisplay.php:637-640 `$this->row .=` and dictinfo.php:60-66; websan PR #154 mirrors. |
| G8 | csl-pywork PR #85 (fcef81d): refresh_csl.sh FAILED aggregation, regenerate-hwnorm1-sqlite.sh:79,102,118 `git status --porcelain`, init_query.py:61-63 junk-row guard, debug cutoff removed. |
| G9 | apidev PR #157 dalglobClass.php:103-113,127-128 prepared statements; basicadjust.php:382-390 `LIKE :pat`; parm.php:112 quote-char strip; websan PR #151. Residual dead `unused_get3b` at dalglobClass.php:284 (quick win). |
| G10 | csl-orig PR #2895 deleted reorg1.sh; README tombstone. |
| G11 | apidev PR #158 download_hwnorm1c_sqlite.sh:22 `set -euo pipefail`, :55-58,:83-109,:119-123,:126-128 checked steps. |
| AP4 | Commits a2cc5f4/b82622b/82a5b17 (2019) resolved the code; issue #4 still OPEN — maintainer to close. |
| CE3 | csl-pywork ffe847f made digentry.py canonical; census doc lines 21/40 still say otherwise (quick win: update the census). |

## 3. Prior-art status sweep — 50 items

Counts: done 16, open 19, partial 13, superseded 2. Sources of the item IDs: G/D = GENERATION_MANUAL + DECISIONS_NEEDED gap specs; PW = csl-pywork issues; AP = csl-apidev issues; AR = COLOGNE ARCHITECTURE_REVIEW; CL = COLOGNE issues #480-#487; CO = csl-corrections issues; CE = csl-observatory CROSS_REPO_DECISIONS; SH = SHARED_CODE; HO = Uprava handoffs H3485/H3486/H3488/H3851; REFRESH = the Cologne regeneration plan.

| ID | Item | Status | Evidence |
|---|---|---|---|
| G1 | Fail-closed validation in generation pipeline | **partial** | csl-pywork origin/main aefbdac: e596cb3 2026-08-29 'fix(generation): fail-closed validation in the generation pipeline (G1, H3631) (#84)' MERGED; v02/makotemplates/pywork/redo_xml.sh:11 'make_xml_status=$?', :15 'xmlint_status=$?', :21-25 'if [ $make_xml_status -ne 0 ] \|\| [ $xmlint_status -ne 0 ]; then printf "\033[31mSTOP redo_xml.sh ..."; exit 1', :28-33 gates redo_postxml_status; v02/utilities/xmlvalidate.py:25 'sys.exit(1)' after 'Problem validating'; v02/generate_dict.sh:39-45 fail_exit() helper + per-stage status checks at :50-55,59-64,69-71,75,77,81-86,88,93,96,139,143,164,167-176; make_xml.py writes fileout+'.tmp' then os.replace (P4) and 'sys.exit(1)' on malformed records (P3); .g |
| G2 | Hook hardening: staged-blob validation + deletion guard + install-by-default | **done** | csl-orig origin/main 30b2ae7: abb58ed 2026-08-29 'hooks: harden commit guard — staged blobs, deletion guard, hooksPath install, CI backstop (H3632)' via PR #2894 MERGED 2026-08-29 (merge 5810603); legacy hooks/pre-commit deleted ('fatal: path hooks/pre-commit exists on disk, but not in origin/main'); .githooks/pre-commit:29 'bash scripts/check_deletions.sh --cached', :48 'git diff --cached --name-status --diff-filter=ACMRT', :58 'python3 scripts/check_encoding.py --staged "${v02_files[@]}"', :73 check_generate_dict.sh; scripts/check_encoding.py:24 docstring 'validates the STAGED blobs (git show :v02/...)', :43 'def staged_bytes(relpath)', :110 'if nLEND == 0:' fail-closed, :130 'sys.exit(1)' |
| G3 | webtc2 search index replacing per-request query_dump.txt scan (perf D4; H3485) | **done** | csl-pywork origin/main aefbdac: aefbdac 2026-08-29 'feat(webtc2): generation-time search index built in the webtc2 stage (G3, H3633) (#87)' MERGED; v02/makotemplates/pywork/webtc2/redo.sh 'set -e; python3 init_query.py ...; python3 build_query_index.py query_dump.txt query_dump.sqlite3; mv query_dump.txt query_dump.sqlite3 ../../web/webtc2/'; webtc2/build_query_index.py:41-42 'CREATE TABLE meta(k TEXT PRIMARY KEY, v TEXT)' / 'CREATE TABLE lines(off INTEGER PRIMARY KEY, t TEXT)', :43 dumpsize meta, :49 "t = line.rstrip('\r\n').replace('-', '')" (byte-offset line table, NOT FTS5; no index on t); init_query.py:27-30 keysanskrit='' (P10); tests/test_h3633_webtc2_index.py (141 lines); v02/invento |
| G4 | Twin-drift CI guard apidev <-> websanlexicon | **done** | csl-apidev origin/main 58b54a0: 11a559c 2026-08-28 'feat(twins): sync nybj + APCu transcoder cache from websanlexicon; add twin-drift CI guard (H3634, audit G4/A24+A25) (#155)' MERGED; .github/workflows/twin-drift.yml:22-27 on push/PR to main + workflow_dispatch, :41-45 checks out sanskrit-lexicon/csl-websanlexicon@main into websan/, :51-56 guarded twins basicadjust.php, basicdisplay.php, getword_data.php, utilities/transcoder.php, :60 'cmp -s', :69-70 '::error::Twin drift detected ... run v02/apidev_copy.sh' + 'exit 1'; :14-17 allowlisted deliberate drift parm.php, dispitem.php, security_headers.php, dal.php, servepdf*, dictinfo. Acceptance proof: PR csl-apidev#160 'DRILL: injected twin dri |
| G5 | Transcoder error surfacing (no @ on cache write, fail loud on missing FSM XML) | **done** | csl-websanlexicon origin/main 02b45aa: 8bb61e1 2026-08-28 'fix(transcoder): surface FSM failures ... (H3635, W11+W12) (#150)' MERGED; v02/makotemplates/web/utilities/transcoder.php:40-44 'if (!file_exists($filein)) { ... error_log("transcoder: FSM XML missing: $filein"); return; }', :189 'if (file_put_contents($cachefile,serialize($entry)) === false) {' (no @), :195 error_log('transcoder: cannot write FSM cache file ... APCu-only caching') warned once per process (:193-197), :348-353 'if (!$fsm) { error_log("transcoder: no FSM for $fromto; failing loud"); throw new RuntimeException("transcoder: FSM XML missing or unparsable for language pair $fromto"); }'. csl-apidev origin/main 58b54a0: 150 |
| G6 | HTTP semantics pass on apidev endpoints (A19, A10, A12) | **done** | csl-apidev origin/main 58b54a0: cde9591 2026-08-28 'fix(http): truthful HTTP semantics on apidev endpoints - error envelopes, status codes, destructure guards (H3636, audit G6/A8-A12+A19+A22) (#159)' MERGED (14 files, +182/-43); e5ed207 (#161) 'api0/getsuggest serves 404 on zero matches'. A19: getwordXmlClass.php +'public $status;' and '$this->status = $ans["status"];' (cde9591 diff), getword_xml.php:22 http_response_code(500) in catch, :31 'http_response_code($temp->status)', :39 http_response_code(400); api0/getsuggest.php:22 500, :30 '$result["status"]', :34 http_response_code(404). A10: exit(1) sites replaced by throws — listhierClass.php:47 'throw new RuntimeException("listhier: no reco |
| D1 | Cache-Control/mod_expires for static assets | **done** | csl-websanlexicon origin/main 02b45aa: v02/makotemplates/web/.htaccess:12-23 '`<IfModule mod_expires.c>` ExpiresActive On ... "access plus 1 week"' for js/css/images/fonts, :25-29 '`<IfModule mod_headers.c>` `<FilesMatch "\.(js\|css\|gif\|png\|jpe?g\|woff2?\|otf)$">` Header set Cache-Control "max-age=604800, public"'; header comment notes AllowOverride required on host. Audit §7 marks D1 FIXED via PR #83 (pre-pin); unchanged since 45b0d30. csl-apidev origin/main 58b54a0 has no .htaccess ('fatal: path .htaccess does not exist in origin/main') — apidev static caching not covered (api-mode, allowlisted drift). |
| D2 | Render-blocking JS | **done** | csl-websanlexicon origin/main 02b45aa: v02/makotemplates/web/webtc/indexcaller.php:12-14,125 script tags carry 'defer'; webtc1/index.php:12-18 'defer' (incl. keyboard.js, transcoderJson.js); webtc2/index.php:10-11,185-186 'defer'; mobile1/index.php:13-20 no defer (CDN jQuery, deliberately left per PERFORMANCE_AUDIT_2026-07.md). Audit §7 marks D2 FIXED via PR #83; no commits touching these files since pin 45b0d30 besides querymodel/transcoder/basicdisplay changes. |
| D3 | Transcoder FSM APCu cache (websan PR #83) + propagation to apidev twin (A25) | **done** | csl-websanlexicon origin/main 02b45aa: v02/makotemplates/web/utilities/transcoder.php:158-160 'if (function_exists("apcu_fetch")) { $entry = apcu_fetch($key,$ok);', :166-171 serialized file cache fallback then apcu_store, :185-186 apcu_store on build, :189 file cache write. csl-apidev origin/main 58b54a0: 11a559c 2026-08-28 (#155) 'sync nybj + APCu transcoder cache from websanlexicon' — utilities/transcoder.php +143/-48; at HEAD :158 apcu_fetch, :160, :170-171, :185-186 apcu_store, :189 file_put_contents; cmp of apidev utilities/transcoder.php vs websan twin at tips = SAME (both 543 lines); basicdisplay.php:382-383 nybj branch present (A24) and basicdisplay/basicadjust/getword_data twins SAM |
| D4 | webtc2 querymodel.php linear scan replaced by index | **done** | Same delivery as G3: csl-websanlexicon origin/main 02b45aa 5a491a8 (#153) querymodel.php:57-83 open_query_index() with staleness/schema/ext guards, :144-149 'SELECT off FROM lines WHERE off >= ? AND t LIKE ? ORDER BY off LIMIT 2000', :288/:465 index consulted in both matchkey and smatchkey paths, flat fgets scan kept as fallback; csl-pywork origin/main aefbdac build_query_index.py:41-49 lines(off INTEGER PRIMARY KEY, t TEXT) + meta dumpsize, webtc2/redo.sh builds and ships query_dump.sqlite3 alongside query_dump.txt. Timing: tests/webtc2_parity/transcript-MW-parity-timing.log 'p95 ms page-1: 162.8 (old-scan) -> 45.5 (new-index)', CHANGELOG PWG 156.4->86.7 ms. Audit §7 at 45b0d30 said 'D4 STI |
| D5 | CDN (parked) | **open** | csl-websanlexicon origin/main 02b45aa: no CDN configuration in v02/makotemplates/web/.htaccess (only mod_expires/mod_headers blocks :12-29), no commits mentioning CDN in 45b0d30..02b45aa (log: 02b45aa, 5a491a8, bc1bf8f, 28406b1, 8bb61e1, deaa28e); gh issue search 'CDN' in csl-websanlexicon returns nothing relevant (apidev hits #16/#33/#23 are unrelated: dalGlob refactor, GitHub security notice, jquery.cookie); Uprava handoffs grep 'CDN' hits only unrelated repos (Systema, IndologyScholars, csl-atlas) plus H1512/H088/H225 (dependency/UX contexts, not a CDN rollout). PERFORMANCE_AUDIT_2026-07.md and CODEBASE_ADVERSARIAL_AUDIT §7 both list D5 as parked/unchanged. Vendored JS remains self-hosted |
| D6 | MW homonym gap-fill get1_mwalt_prev / keydoc.sqlite fast path dev-gated in dal.php | **open** | csl-websanlexicon origin/main 02b45aa: v02/makotemplates/web/webtc/dal.php:39-43 'if (isset($_REQUEST["dev"]) && ($_REQUEST["dev"] == "yes")) { $this->devFlag = True; }', :50 '$this->keydoc_file = "$sqlitedir/keydoc.sqlite";', :93 'if (($this->keydoc_file) && file_exists($this->keydoc_file) && $this->devFlag) {' — keydoc fast path still gated on ?dev=yes; 'git log 45b0d30..origin/main -- v02/makotemplates/web/webtc/dal.php' empty (no commits since pin). csl-apidev origin/main 58b54a0 dal.php:36-39 same devFlag gate, :47 keydoc_file, :91 'if ($this->keydoc_file && file_exists($this->keydoc_file) && $this->devFlag)' — same gate (dal.php is allowlisted deliberate drift in twin-drift.yml:16). Au |
| G7 | skd `<mark>` H/P markers dropped (A11) + dictinfo year fallback for dicts with no older scan (A20) | **done** | csl-apidev origin/main=58b54a0 (local HEAD e5ed207 lags). git log 6f1b720..origin/main: 4547c30 2026-08-29 "fix(display): render skd `<mark>` H/P markers; version=1 fallback for dicts with no older scan (H3637, audit G7/A11+A20) (#162)" [basicdisplay.php 2 +-, dictinfo.php 5 ++++-]. basicdisplay.php:637-640@origin/main: `} else if ($el == "mark"){ // skd. n = H,P  $n = $attribs['n'];  $this->row .= "<strong>($n) </strong>";` (was `$row .=` at e5ed207:640). dictinfo.php:60-66@origin/main: `if ((isset($_REQUEST['version'])) &&($_REQUEST['version'] == '1')) { // dicts with no older scan (abch,acph,acsj,nmmb,fri) fall back ... $this->year = isset(self::$dictyear_older[$this->dictupper]) ? self::$d |
| G8 | v02 bookkeeping: refresh_csl.sh failure aggregation, untracked-aware regen gate, init_query junk row, make_xml debug cutoff (P11/P14/P15/P18) | **done** | csl-pywork origin/main=aefbdac (local HEAD e460545 = pin). git log e460545..origin/main: fcef81d 2026-08-28 "fix(v02): G8 bookkeeping — refresh_csl aggregation, untracked-aware regen gate, junk row, debug cutoff (H3638) (#85)". v02/refresh_csl.sh@origin/main: per-repo `if git -C "$BASE/$repo" pull; then echo "OK   $repo"; else echo "FAIL $repo"; FAILED="$FAILED $repo"; fi`, lines 27-30 `if [ -n "$FAILED" ]; then echo "refresh_csl: FAILED pulls:$FAILED"; exit 1; fi`. v02/regenerate-hwnorm1-sqlite.sh@origin/main:79,102,118 `if [ -z "$(git status --porcelain)" ]; then` (replaces git diff --quiet; with git add -A). make_xml.py@origin/main: grep "debug stopping\|1000000" empty (cutoff removed). i |
| G9 | Parameterize interpolated SQL in dalglobClass get1 + basicadjust ls_matchabbr LIKE; strip quote chars from parm keys (A13/A14/W4) | **done** | csl-apidev origin/main=58b54a0. b913637 2026-08-28 "fix(sql): parameterize dalglob get1 + basicadjust ls_matchabbr; strip quote from parm keys (H3639, A13+A14) (#157)" — latest commit on dalglobClass.php (git log origin/main -- dalglobClass.php). dalglobClass.php@origin/main:103-113 get(): `try { $stmt = $this->file_db->prepare($sql); $stmt->execute($params); } catch (PDOException $e) { return $ansarr; }`; :127-128 `$sql = "select * from {$this->tabname} where key=:key"; $ansarr = $this->get($sql,array(':key'=>$keynorm));`. basicadjust.php@origin/main:382-390 `$sql = "select * from $table where $fieldname LIKE :pat"; ... $stmt->execute(array(':pat'=>$key1));` with PDOException catch. parm.ph |
| G10 | csl-orig reorg/: delete broken reorg1.sh / reorg1_all.sh, tombstone in README (O8/O9) | **done** | csl-orig origin/main=30b2ae7 (local HEAD dee5a03 = pin; files still on disk locally only because the worktree lags). git log dee5a03..origin/main: 553c2f0 2026-08-29 "reorg: delete broken reorg1.sh/reorg1_all.sh, tombstone in README (H3640)" [reorg/README.txt +4, reorg1.sh -8, reorg1_all.sh -8]; 30b2ae7 Merge PR #2895 h3640-drain. `git show origin/main:reorg/reorg1.sh` -> "fatal: path exists on disk, but not in 'origin/main'". reorg/@origin/main holds only README.txt: "TOMBSTONE 29-08-2026: reorg1.sh and reorg1_all.sh (deleted; see git history and PR for H3640) ...". No later commit re-adds them (30b2ae7 is tip). REGISTRY_ARCHIVE.md: H3640 Done 29-08-2026; stale handoffs/H3640*.claim remains |
| G11 | download_hwnorm1c_sqlite.sh hardening: set -euo pipefail, empty-tag guard, checksum/unzip verify, integrity_check, transactional staging (A23) | **done** | csl-apidev origin/main=58b54a0. f83b476 2026-08-28 (#158, H3641). download_hwnorm1c_sqlite.sh@origin/main (132 lines): :22 `set -euo pipefail`; :51-52 curl -f --retry 3 \|\| die; :55-58 `if [ -z "$LATEST_TAG" ]; then die "no tag_name ..."`; :83-109 sha256 verify or `unzip -t`; :119-123 `PRAGMA integrity_check`; :126-128 `mv "$STAGE_DIR/hwnorm1c.sqlite" simple-search/hwnorm1/hwnorm1c.sqlite; echo "$LATEST_TAG" > "$TAG_FILE"` (state file written only after verified move). No later commit on this file. REGISTRY_ARCHIVE.md: H3641 Done 28-08-2026. PR #158 MERGED on main. |
| REFRESH | Refresh-script modernization plan (csl-pywork #53): Python 3 selective driver with dry-run/no-push/state/manifest | **partial** | csl-pywork origin/main=aefbdac. v02/redo_xampp_selective.py added 7bd24d1 2026-07-14 "(csl-pywork#53, AR1) (#68)"; `#!/usr/bin/env python3`; argparse flags --base --indic-base --state-file --since --dict --dry-run --no-push --skip-pull --strict-clean --allow-dirty --stop-after --manifest; :100 `git_output(["status","--porcelain"], repo_dir)`; :252-253 `run(["python3","make_babylon.py",d,"0"/"1"], cwd=stardict_dir)`, :293 `run(["python3","json_from_babylon.py",d])` — python2 calls gone from driver. v02/redo_xampp_selective.sh = `set -e; exec python3 "$HERE/redo_xampp_selective.py" "$@"`. v02/readme_selective.md and tests/test_redo_xampp_selective.py present. Gaps: no flock/lockfile in driver; |
| CE2 | Code-duplication census 'Template lags deployment' warnings (transcoder.py, updateByLine.py, parseheadline.py, redo.sh, php endpoints) | **superseded** | csl-observatory@d2dea7a reports/code_duplication_census.md (last fa62983 2026-07-12) line 37 warning; JSON canonical_is_modal=False for all five. Re-diff at current tips shows the flag is a measurement artifact — the template is AHEAD of the modal cohort, not behind: (a) transcoder.py: only csl-pywork@origin/main copy is v02/distinctfiles/mw/pywork/transcoder.py (blob 1faee9dc, last 72d381b 2026-06-05); modal 53-copy cluster = per-dictionary issue-work copies (AMAR/transcoder.py, AP/issues/issue5/transcoder.py, BEN/verbs01/, CAE/verbs01/ = blob aee61772, dated 2020-2026) — diff 40 lines: template uses `open(filedbg,'w',encoding='utf-8')`/`is None`, modal uses `codecs.open`/`== None`. (b) upd |
| PW52 | csl-pywork #52 .gitattributes line-ending policy (also csl-websanlexicon, csl-apidev, csl-orig, csl-inflect, COLOGNE) | **partial** | csl-pywork@origin/main aefbdac .gitattributes = `* text=auto` + `*.tsv/*.sh/*.yml/*.txt/*.py text eol=lf` (added 0dcd85d 2026-04-08). csl-orig@origin/main 30b2ae7 .gitattributes blob 66d2bd2 `* text=auto` + eol=lf (added 947cd78 2026-04-06). csl-websanlexicon@origin/main 02b45aa `* text=auto` + eol=lf (added e8277b1 2026-04-09). csl-inflect@14cd802 .gitattributes `* text=auto eol=lf` + per-ext (added 14cd802 2026-07-27 #28). csl-apidev@origin/main 58b54a0 .gitattributes (added 1dd1b02 2026-04-08) = `*.txt text eol=lf / *.tsv / *.sh / *.yml / *.txt (duplicate line) / *.py` — NO `* text=auto` catch-all, `*.txt` listed twice. COLOGNE@ece273d (=origin/main): `git ls-tree origin/main .gitattribut |
| PW74 | csl-pywork #74 changelog case-duplicate guard (CHANGELOG.md vs changelog.md) | **open** | csl-pywork@origin/main aefbdac tree: only CHANGELOG.md (no lowercase twin); `git ls-tree -r --name-only origin/main \| sort -f \| uniq -di` empty. History: 5e9f5c4 2026-07-09 (#65) dropped dup; 34b2181 2026-07-29 re-dropped it (recurred once). Workflows at origin/main: ci.yml, codeql.yml, dependabot-auto-merge.yml, readme-guard.yml, xml-parse.yml — none mention changelog or case collision; .pre-commit-config.yaml only check-merge-conflict. Issue #74 OPEN (documentation,minor; no comments; suggests CI `git ls-files \| sort -f \| uniq -di` guard or fold into #52). csl-apidev has a "changelog duplicate-entry guard (CI)" (PR #131, 2026-07-30) but that checks duplicate bullet entries, not filenam |
| AP4 | csl-apidev #4 cleanup of apidev functions (delete unused getword_data_html, delete get1_basic, rename getword_data_html_raw) | **done** | csl-apidev@origin/main 58b54a0. Commits 2019-08-07 (git log --grep): a2cc5f4 "remove get1_basic method. It is not used."; b82622b "change getword_data_html_raw to getword_data_html"; 82a5b17 "getword_html_data -> getword_data_html". Current tree: `git grep get1_basic origin/main -- '*.php'` empty; `git grep getword_data_html origin/main` shows only the later-renamed `getword_data_html_adapter` (getword_data.php:88 `public function getword_data_html_adapter(...)`, callers getword_data.php:54, getwordXmlClass.php:98, basic_xml_html.php:13-15) — no `_raw` suffix, no unused `getword_data_html` remains. Issue #4 still OPEN (2019-08-07, minor,tech-debt; 3 comments, last 2019-08-07 by funderburkjim |
| AP16 | csl-apidev #16 refactor dalGlob (dalglobClass.php + sample/dalglob1.php display) | **partial** | csl-apidev@origin/main 58b54a0. dalglobClass.php log: b913637 2026-08-28 (H3639, parameterized get1); 9ea43a9 2023-10-06 "dalglobClass minor edit"; 1914b71 2023-01-14; no structural refactor. Callers: dalglob.php, sample/dalglob1.php, api1/salt_multidictClass.php, app/entry.php, getword_batch.php/lookup (git grep -l -i dalglob origin/main). lookup/index.php, lookup/lookup.js, lookup/dictmeta.js, lookup.css, getword_batch.php, doc/roadmap_lookup.md present at origin/main (PR #63 MERGED 2026-07-03 Wave 1; PR #130 Wave 3 2026-07-28). Issue #16 body (2020-07-17): changes for hwnorm2 db (hwnorm2#4) in dalglobClass.php and dalglob1.php display, "still problems with the scrolling in the lower panel |
| AP44 | csl-apidev #44 deflate repo (stop committing 25MB hwnorm1c.sqlite; GitHub Releases) | **partial** | csl-apidev@origin/main 58b54a0. Solution shipped: 23c6d1a 2026-04-11 "Stop tracking hwnorm1c.sqlite and it to .gitignore"; .gitignore@origin/main:18-22 `.csl_sqlite_release_tag`, `*.sqlite.zip`, `*hwnorm1c.sqlite`, `simple-search/hwnorm1/hwnorm1c.sqlite`; `git ls-tree -r origin/main \| grep -i sqlite` empty (no sqlite tracked). Releases: gh api csl-sqlite/releases/latest tag 2026-08-09-05-51-03 with hwnorm1c.sqlite.zip (10.6MB) + keydoc_glob1.sqlite.zip + per-dict zips; download_hwnorm1c_sqlite.sh hardened (G11). History bloat remains: .git = 223M, count-objects size-pack 222.62 MiB (3967 in-pack objects); `git rev-list --objects --all` shows >=8 hwnorm1c.sqlite blobs of 26.0-26.7 MB each (d |
| AR1 | P1 Python 2 legacy tools (make_babylon.py, updateByLine_python2.py, issue10/suggest.py+levenshtein.py): port or retire | **partial** | COLOGNE HEAD ece273d (2026-08-30). ARCHITECTURE_REVIEW.md:35-56@ece273d names the four files. py_compile at HEAD: enhancements/code/updateByLine_python2.py, enhancements/issue10/levenshtein.py, enhancements/issue10/suggest.py, stardict/make_babylon.py all fail `SyntaxError: Missing parentheses in call to 'print'` (unchanged since 2019-2024; git log --since=2026-05-01 touches no file in stardict/, enhancements/issue10/, enhancements/code/). What did land: docs/TOOLING_MANUAL.md:39@ece273d `stardict/ … 🔴 legacy Python 2, broken traps`, :43 `enhancements/issue10/ … 🔴 legacy Python 2`, :98-106 Python-version table, :235 `updateByLine_python2.py — the Python-2 legacy twin. Do not use for new work |
| AR2 | P1 Duplicated/broken transliterator: iast/transcoder.py path bug + stardict/transcoder.py copy | **open** | iast/transcoder.py:45-47@ece273d unchanged since 2022: `transcoder_dir =os.path.dirname(os.path.abspath(__file__))` / `transcoder_dir = os.path.dirname(transcoder_dir) ## parent` / `transcoder_dir += "/data/transcoder"` (resolves to COLOGNE/data/transcoder, which does not exist); iast/slp1_iast.py:9 `transcoder.transcoder_set_dir('')` works around it. stardict/transcoder.py:41-42@ece273d `transcoder_dir =os.path.dirname(os.path.abspath(__file__))` / `transcoder_dir += "/data/transcoder"`; stardict/data absent; duplicate file still present (ls stardict/ = make_babylon.py redo.sh transcoder.py). git log --since=2026-05-01 -- iast/ stardict/: no commits. taxonomy_proposals.csv human_decision=co |
| AR3 | P1 Flask prototype issues/issue445/serve_api.py (CORS(*), debug=True, f-string table name, whole-table regex scan) | **open** | issues/issue445/serve_api.py@ece273d (last commit 1f08095 2025-03-30, no commits since): :12 `CORS(app)  # Enable CORS for all routes`; :16-17 `DB_FILE = "temp_pwg_10.sqlite"` / `TABLE_NAME = "temp_pwg_10"`; :46-48 `cursor.execute(f"SELECT key, lnum, data FROM {TABLE_NAME}")` + `rows = cursor.fetchall()`; :51-58 `re.compile(key_pattern)` Python-side filter; :64-68 generic `except Exception as e: return jsonify({"error": str(e)}), 500`; :94 `app.run(debug=True)`. Only change is a label: TOOLING_MANUAL.md:500 `issues/ — archival workspaces (plus one live prototype)`; taxonomy_proposals.csv human_decision=promote-or-sunset for issues/issue445/*. CodeQL workflow exists (.github/workflows/codeql. |
| AR4 | P1 No dependency manifest / env pins (pyproject or requirements) | **partial** | At HEAD ece273d: pyproject.toml, requirements*.txt, setup.py all absent (ls). Deps are only listed in prose: docs/TOOLING_MANUAL.md:108 `There is no requirements.txt/pyproject.toml yet`, :111 lists lxml, indic_transliteration, flask, flask_restx, flask_cors. ruff.toml@ece273d present (`target-version = "py311"`, `select = ["E9","F63","F7"]`) and .pre-commit-config.yaml present; .github/workflows/ci.yml installs ruff ad hoc. ARCHITECTURE_REVIEW.md:108-126 and Near-Term Backlog :316 ask for pyproject/requirements; not delivered. |
| AR5 | P2 Copied per-issue logic in issues/issueNNN; no shared cologne_tools package | **open** | No cologne_tools/ or scripts/ directory at HEAD ece273d (ls). issues/ tree unchanged: git log --since=2026-05-01 -- issues/ empty. ARCHITECTURE_REVIEW.md:128-145 and roadmap Phase 3 :291-296 ask for a shared package; Near-Term Backlog :321 `cologne_tools`. Only governance landed: taxonomy_proposals.csv human_decision=archive-no-refactor for 288 rows (issues/issue2*-4* etc.) and promote-or-sunset for issue441/442/443/445; TOOLING_MANUAL.md:500-522 labels issues/ archival. csl-observatory docs/SHARED_CODE.md:207 counts levenshtein.py 6 copies across VCP+COLOGNE (cross-repo duplication still live). |
| AR6 | P2 No tests, no CI (golden tests with fixtures/mini_dict.txt) | **partial** | CI exists at HEAD ece273d: .github/workflows/ci.yml (ruff E9/F63/F7 syntax lint under ruff.toml excludes enhancements/, iast/, stardict/; change-file format validator; UTF-8 validator; YAML lint), .github/workflows/codeql.yml (python; triggers fixed to main by PR #473 merged), dependabot-auto-merge.yml, .pre-commit-config.yaml; .ai_state.md Completed lists org-baseline hygiene (CI, CodeQL, pre-commit, dependabot). Tests absent: no tests/ dir, no tests/fixtures/mini_dict.txt, no pytest config (ls, grep). ARCHITECTURE_REVIEW.md:147-168 asks for golden tests of updateByLine.py, xmltag.py, ea.py, transcoder; Near-Term Backlog :317 `tests/fixtures/mini_dict.txt`. taxonomy human_decision=test-and- |
| AR7 | P2 Fragile shell wrappers (../../../cologne/csl-orig relative paths) and plain-HTTP curl without checksum | **open** | xmltag/redo_one.sh:4@ece273d and eascii/redo_one.sh:4@ece273d `filein="../../../cologne/csl-orig/v02/${dictlo}/${dictlo}.txt"` (no existence check, no env override). enhancements/code/dictionary_init.sh:41,44,47@ece273d `curl -o "$DICT"web1.zip http://s3.amazonaws.com/sanskrit-lexicon/blobs/"$DICT"web1.zip` etc. — plain http, no -fSL, no checksum; last commit 20ad9d9 2019-02-04. git log --since=2026-05-01 -- eascii/*.sh xmltag/*.sh enhancements/code/*.sh iast/install_local.sh localinstall/ stardict/redo.sh: none. Only docs: TOOLING_MANUAL.md:82 `Directory layout assumption (load-bearing)`, :255-262 and :291-296 document the sibling-path requirement. taxonomy human_decision=harden-ops for 29  |
| AR8 | P2 XML handled by regex/string replace (xmltag.py, make_md.py) instead of a parser | **open** | xmltag/xmltag.py:35@ece273d `regex = re.compile(r'<(.*?)>')`; makemd/make_md.py:57-58@ece273d `text = text.replace('<body>', '')` / `text = text.replace('</body>', '')`. Last code commit to xmltag/ is ed2c442 2026-05-04 (chgtag.py, pre-review); makemd/ untouched since 2019-2024. ARCHITECTURE_REVIEW.md:190-209 problem stands; no lxml/ElementTree adoption in these files (grep lxml xmltag/ makemd/ → none; lxml used only in enhancements/code/xmlvalidate.py). No issue or handoff targets this specifically (gh issue list, Uprava grep: none). |
| AR9 | P3 Positional sys.argv CLIs, no argparse/--help | **open** | enhancements/code/updateByLine.py:159-161@ece273d `filein = sys.argv[1]` / `changein = sys.argv[2]` / `fileout = sys.argv[3]`; makemd/make_md.py:121@ece273d `dictcode = sys.argv[1]`. `grep -rn argparse iast xmltag eascii enhancements/code makemd stardict` at HEAD → zero hits. No commits to these files since 2026-05-01. ARCHITECTURE_REVIEW.md:211-230 unaddressed; TOOLING_MANUAL documents the positional usage (e.g. :34-35, :261) rather than changing it. |
| CL480 | Slice 2 #480 mark-generated: 86 generated report files (eascii/eadata, all_ea.txt, easummary*.tsv, xmltag/*.txt) — mark as generated, document regeneration | **partial** | gh issue 480 OPEN (created 2026-08-25, 0 comments, body 'Approved as proposed (M.G., 19-07-2026) … this issue tracks implementation. Tracking: #478'; 86 rows). No .gitattributes linguist-generated, no header marker, no README-per-dir change at HEAD ece273d: git log --since=2026-05-01 -- eascii/ xmltag/ shows only pre-review 2026-05-04 regenerations (d93aa52 all_xmltags.txt, ed2c442 chgtag_*.txt). Regeneration commands ARE documented: docs/TOOLING_MANUAL.md:255-262 (eascii: `sh redo_one.sh mw`, `sh redo_all.sh`, `sh catall.sh > all_ea.txt`, easummary.py) and :291-296 (xmltag: `sh redo_one.sh mw`, `sh redo_all.sh`, `sh catall.sh > all_xmltags.txt`), plus pre-existing eascii/readme.txt and xmlt |
| CL481 | Slice 3 #481 archive-no-refactor: 248 files under issues/issueNNN — archive without refactoring | **partial** | gh issue 481 OPEN (2026-08-25, 0 comments, 248 rows). Policy ratified and documented: taxonomy_proposals.csv human_decision=archive-no-refactor for 288 rows (248 in this slice), PR #488 merged 2026-08-25; TOOLING_MANUAL.md:500 `issues/ — archival workspaces (plus one live prototype)`, :589 `Legacy / archival — do not build on`. In-tree nothing moved or marked: git log --since=2026-05-01 -- issues/ is empty; no issues/README.md or archive/ dir at HEAD ece273d (ls). human_decision_fill_2026-08-25.md slice table: #481 open. |
| CL482 | Slice 4 #482 test-and-package: 21 live scripts (eascii/*.py, enhancements/code/*.py, iast/*, makemd/make_md.py, xmltag/*.py) — tests + packaging | **open** | gh issue 482 OPEN (2026-08-25, 0 comments, 21 rows). At HEAD ece273d: no tests/, no pyproject.toml/requirements.txt, no package __init__ (ls, grep). ci.yml runs only ruff E9/F63/F7 with ruff.toml excluding enhancements/, iast/, stardict/ — so 13 of the 21 slice files are not even syntax-linted in CI. taxonomy_proposals.csv human_decision=test-and-package 16 rows (+5 in iast/ readme/xml data). No commits to any of these code paths since 2026-05-04 (git log --since=2026-05-01). |
| CL483 | Slice 5 #483 port-or-retire: 4 Python-2-only files (updateByLine_python2.py, issue10/levenshtein.py, issue10/suggest.py, stardict/make_babylon.py) | **open** | gh issue 483 OPEN (2026-08-25, 0 comments, 4 rows). All four still present and py2-only at HEAD ece273d: py_compile → `SyntaxError: Missing parentheses in call to 'print'` for each; no deprecation header, no move. Docs only: TOOLING_MANUAL.md:39,43,98-106,235,436 (`Port or retire is the …` verdict stated, not executed); ruff.toml excludes their dirs. No commits since 2026-05-01 to any of the four. |
| CL484 | Slice 6 #484 promote-or-sunset: issues/issue441-443, issue445 (Flask API), autocomplete/listsanhw1.py, issue10/hw1list.py — decide and act | **open** | gh issue 484 OPEN (2026-08-25, 0 comments, 5 rows). taxonomy_proposals.csv human_decision=promote-or-sunset copied verbatim from proposed_action (H3484 fill) — i.e. the fork 'promote OR sunset' itself is not yet decided per path. Files unchanged at HEAD ece273d: issues/issue445/serve_api.py last commit 1f08095 2025-03-30; issue441/442/443 and enhancements/autocomplete/listsanhw1.py, enhancements/issue10/hw1list.py no commits since 2026-05-01. TOOLING_MANUAL.md:42 autocomplete `🟡 data ships, script legacy`, :500-522 issue445 called the 'one live prototype' but no promote/sunset action. No handoff picks this up (Uprava grep for issue445/promote-or-sunset in open handoffs: none). |
| CL485 | Slice 7 #485 consolidate-shared: enhancements/code/updateByLine.py + stardict/transcoder.py — dedupe into a shared module | **open** | gh issue 485 OPEN (2026-08-25, 0 comments, 17 rows per issue body; CSV consolidate-shared = 2 rows: enhancements/code/updateByLine.py, stardict/transcoder.py). stardict/transcoder.py still present and diverged from iast/transcoder.py (stardict/transcoder.py:41-42 vs iast/transcoder.py:45-47 differing dir logic; xrange at stardict/transcoder.py lines 169,340 per TOOLING_MANUAL.md:102-104). No shared package at HEAD ece273d; no commits to stardict/ or enhancements/code/ since 2026-05-01. csl-observatory docs/SHARED_CODE.md:207 shows the same duplication pattern cross-repo (levenshtein.py 6 copies). |
| CL486 | Slice 8 #486 harden-ops: 23 shell/ops files (eascii+xmltag redo_*.sh/catall.sh, dictionary_init.sh, iast/install_local.sh, localinstall/**, stardict/redo.sh) — harden | **open** | gh issue 486 OPEN (2026-08-25, 0 comments, 23 rows). No shell file changed: git log --since=2026-05-01 -- '*.sh' localinstall/ is empty at HEAD ece273d. Evidence of unhardened state: xmltag/redo_one.sh:4 and eascii/redo_one.sh:4 hard-coded `../../../cologne/csl-orig/v02/...` without check; enhancements/code/dictionary_init.sh:41,44,47 plain `http://s3.amazonaws.com/...` curl without -fSL/checksum. ci.yml has no shellcheck job. TOOLING_MANUAL.md:82 documents the layout assumption as 'load-bearing' rather than removing it. |
| CL487 | Slice 9 #487 document-owner / keep-maintained: api/*.md, aws/, misc/, xsswork/readme.txt, readme_new_dict_addition.md — assign owner + status | **partial** | gh issue 487 OPEN (2026-08-25, 0 comments, 32 rows: document-owner 6 + keep-maintained 10 + others). Status verdicts exist: TOOLING_MANUAL.md:465 `api/ — REST API design spec (2020, not code)` (cheat-sheet 🟡 design doc), :479 `aws/ — S3 upload notes and bucket manifests` (🟡 manifests useful), :523 `xsswork/ and misc/ — notes and reference artifacts` (⚪ archival), :539 `Adding a new dictionary to CDSL` supersedes readme_new_dict_addition.md content. No owner assignment anywhere: no .github/CODEOWNERS at HEAD ece273d (ls: No such file), no OWNER/status header added to api/readme.md, api/getword.md, api/listhier.md, api/servepdf.md, xsswork/readme.txt, readme_new_dict_addition.md (git log --sin |
| CE1 | Lift true-clone helpers (levenshtein.py, util_mw.py, util_dump_lines.py, unixify.py, compare.py) into csl-pyutil or sanskrit-util | **open** | csl-pyutil/csl_pyutil/__init__.py:1-30@c66ce13 public API = render_review_sheet / anatomy / evidence / RU_UI_STRINGS / integrity_tripwire only ("from csl_pyutil import anatomy, evidence"); csl-pyutil/CHANGELOG.md@c66ce13 latest 0.22.0 (2026-08-21) split_layout, no helper lift; csl-pyutil issues (gh issue list, all states): #27 OPEN H2891 tripwire, #21/#20/#19/#18/#1 CLOSED — none about helper lift. sanskrit-util/py/pyproject.toml:7@a2c4714 version = "0.10.0"; only levenshtein there is private `def _levenshtein(a: str, b: str, *, max_dist: int) -> int:` at sanskrit-util/tools/KeySwap/plugins/offline_fuzzy/fuzzy_lookup.py:88 (plugin, not package export); gh issue list sanskrit-util --search le |
| CE3 | digentry.py headless family (193 copies, no canonical template home) | **done** | csl-pywork/v02/makotemplates/pywork/digentry.py EXISTS at e460545; `git log --follow`: ffe847f 2026-07-13 14:13:08 +0300 "H787: add canonical digentry.py to makotemplates (headless family regains a head) (#67)" (one day after the 12-07-2026 census). md5 b3bb9d71446f621fbbe03c4b9c82354a identical to all 11 leaf copies on disk: COLOGNE/issues/issue{422,423,424,425,426,427,428,429,431,432}/digentry.py@ece273d and csl-corrections/app/correction_response/issue91/digentry.py@e363439. csl-observatory/reports/template_deployment_reconcile_H787.md@d2dea7a documents the reconcile; SHARED_CODE.md:32@d2dea7a row 3 names csl-pywork/v02/makotemplates/pywork/ as canonical for digentry. Stale text: reports/ |
| SH1 | sanskrit-util adoption across the 19 hand-rolled transliteration/normalisation sites | **partial** | Re-grep of the 19 census sites for sanskrit_util\|sanskrit-util (refs count): WhitneyRoots/reader/reader.js 2, WhitneyRoots/src/utils/linguistics.js 6, WhitneyRoots/scripts/sanskrit_util.py 2 (shim, vendors 0.4.1), csl-atlas/src/lib/lookup-normalize.js 4, csl-atlas/scripts/lib/dict-normalize.mjs 3, SanskritSpellCheck/detectors/slp1util.py 15, SamudraManthanam/web/app/services/morph_service.py 1 → 7 delegate; still 0 refs: BookIndex/src/utils/linguistics.js, GRA/vn/gra-dev/web/webtc1/transcoderjs/transcoder3.js, SamudraManthanam slug.py + html_to_canonical.py, RussianRamayana/web/transliterate_filenames.py, VCP/meld_regex/convert_transliteration.py, csl-observatory/scripts/obs_t_translit_chec |
| CO357 | csl-corrections #357 build_correction_viz.py KeyError 'pwg'; SVGs carry no freshness signal | **open** | gh issue 357 sanskrit-lexicon/csl-corrections: OPEN, created 2026-08-13T10:55:07Z, comments [], labels []. Bug still reproducible at e363439: scripts/build_correction_viz.py:102-103 `dicts = sorted(per, key=lambda d: -(sum(per[d].values()) * 1000 / entries[d]))`; data/derived/dict_entry_counts.tsv has no `pwg` row (rows ap, ap90, … measured 2026-07-07) while data/derived/correction_loci.tsv has 1 row with dict=pwg (`grep -c -P '^pwg\t'` = 1). `git log -- scripts/build_correction_viz.py` = single commit bc94f31 2026-07-07 (H294, #291); docs/img/*.svg last commit bc94f31 2026-07-07, no 'generated' stamp inside any SVG (grep -l -i generated docs/img/*.svg → none). Workflows at HEAD: changelog-l |
| CO358 | csl-corrections #358 correction_loci.tsv stale by 21 875 rows / no regeneration wiring | **partial** | gh pr 356 csl-corrections: MERGED 2026-08-15T06:56:20Z by gasyoun, "data(derived): refresh correction_loci.tsv — 21 875 rows still batch_pending after batch_20260804 shipped" → commit 7b87b83 2026-08-15. TSV at e363439 batch column: batch_20260529 22131, batch_20260804 21875, batch_20250418 12371, …, batch_pending 141 (all 141 dict=pw = batch_pending/dictionaries/pw/change_pw_h3152_link_splitting.txt, PR #366 merged 2026-08-20, 141 lines) — data currently consistent with queue dirs (batch_20260529/20260626/20260712/20260804 + batch_pending/dictionaries/pw only). gh issue 358: still OPEN, created 2026-08-13T11:25:03Z, comments []. Wiring gap unchanged: .github/workflows = changelog-lint.yml,  |
| HO3485 | H3485 — websanlexicon D4 SQLite FTS search PR for webtc2 | **superseded** | Uprava/handoffs/H3485-OxAlpha_csl-websanlexicon_websanlexicon-d4-ftsearch-pr_25.08.26.md@98c37cd2f: mission = replace webtc2 flat-file scan with SQLite FTS index + build script + fallback, one PR; .claim = "OxAlpha (opencode/z-ai/glm-5.3-flash)\t2026-08-31T11:19Z"; handoffs/README.md:164 still `🟡 spec filled at mint (25-08-2026) {effort: medium}`; FRUIT_LAUNCH_STATUS_01.09.26.md:54 "🟡 queued, nongated". The D4 goal shipped under a different handoff: csl-websanlexicon PR #153 MERGED 2026-08-29T10:23:03Z "feat(webtc2): consult generation-time search index, flat file as fallback (G3, H3633)" — body: "**D4** still open per memo §7 → closed by this PR pair"; files v02/makotemplates/web/webtc2/que |
| HO3488 | H3488 — cologne hygiene consolidation sweep (fork-sync trio, sanskrit-util re-vendor, SAST gap list) | **open** | Uprava/handoffs/H3488-OxAlpha_multi_cologne-hygiene-consolidation-sweep_25.08.26.md@98c37cd2f: three deliverables (fork-sync verdict via tools/cologne_fork_sync_check.py; re-vendor table + one PR per consumer via tools/cologne_sanskrit_util_sync.py; SAST gap list + ≤5 Semgrep PRs); .claim = "OxAlpha (opencode/z-ai/glm-5.3-flash)\t2026-08-31T11:19Z"; README.md:162 `🟡 spec filled at mint (25-08-2026) {effort: medium}`; FRUIT_LAUNCH_STATUS_01.09.26.md:56 "🟡 queued, nongated". No H3488-attributed evidence: Uprava/tools/reports has only EVIDENCE_SYNC_SANSKRIT_UTIL_H3345_26-08-2026.txt (selftest of sync tool, H3345); `gh search prs --owner sanskrit-lexicon "sanskrit-util" --created >=2026-08-25` → |
| HO3851 | H3851 — csl-orig red branch repair: Generate and Sync Stardict Files | **open** | Uprava/handoffs/H3851-Sonnet_csl-orig_red-branch-repair-generate-and-sync-stardict-files_01.09.26.md@98c37cd2f: created 01-09-2026, Goal/Acceptance/Evidence sections are unfilled template placeholders; README.md:133 `🟡 spec filled at mint (01-09-2026) {effort: medium} {launch-box: any}`; NEXT_TASK_QUEUE.md:88,116 exec claims Sonnet 5 (claude-sonnet-5) 2026-09-01T20:16Z (pid28720) and 2026-09-02T03:06Z (pid16108); CHANGELOG.md:30 H3850 closed as DUPLICATE of H3851; docs/FAILSWEEP_LATEST.md:34 slug-dup warning. Workflow still red: `gh run list --workflow "Generate and Sync Stardict Files"` → failure 2026-08-13T04:54:56Z (NYBJ slp1, run 31668532366, failed step "Run transformation scripts"), fa |
| HO3486 | H3486 — csl-corrections AllvsPWG 99-item correction-queue pass | **open** | Uprava/handoffs/H3486-OxAlpha_csl-corrections_allvspwg-correction-queue-pass_25.08.26.md:29@98c37cd2f: one /cologne-correction-queue pass over drdhaval2785/SanskritSpellCheck Allvs_2026/PWG/AllvsPWG-priority.txt (99 candidates), survivors to csl-corrections batch_pending/dictionaries/pwg/, pass report with 99/99 verdicts, no csl-orig writes; .claim = "OxAlpha (opencode/z-ai/glm-5.3-flash)\t2026-08-31T11:19Z"; README.md:163 `🟡 spec filled at mint (25-08-2026) {effort: medium}`; FRUIT_LAUNCH_STATUS_01.09.26.md:55 "🟡 queued, nongated"; NEXT_UP.md:117-122 still lists the starter line. Deliverable absent at csl-corrections e363439: `ls batch_pending/dictionaries/` → only `pw/` (change_pw_h3152_li |

## 4. Confirmed findings — 22 (survived the three-lens refutation)

Severity is the finder's; `votes` = refuter verdicts. Line numbers are at the local tips of §1; see §5 for re-anchoring.

### B1. SQL injection: $_GET['word'] reaches interpolated sqlite query via transcoder passthrough

- **Where:** [csl-inflect](https://github.com/sanskrit-lexicon/csl-inflect/tree/14cd802) · [web/getWord.php:15-24,139,147](https://github.com/sanskrit-lexicon/csl-inflect/blob/main/web/getWord.php#L15) · category `security` · severity **high** · effort medium · prior art: new (none)
- **Claim:** The public lookup endpoint builds SQL by string interpolation from the GET parameter; the only preprocessing (preprocess_unicode_input, transcoder_processString) does not strip or escape double quotes, and the FSM transcoder emits any unknown character verbatim, so a crafted `word` value terminates the literal and appends arbitrary SQL executed via PDO::query.
- **Evidence:** web/getWord.php:15-24@14cd802: `if (isset($_GET['word'])) { $keyin = $_GET['word']; } ... $keyin1 = preprocess_unicode_input($keyin,$filterin); $key = transcoder_processString($keyin1,$filterin,"slp1");` -- web/getWord.php:139@14cd802: `$sql = "select * from `$table` where `key`=\"$key\"";` and :147 `$sql = "select * from `$table` where `stem`=\"$stem\" and `model`=\"$model\"";` -- web/utilities/transcoder.php:214-218@14cd802: `if (! $isubs) { $result .= $c; ...}` ("Default condition. emit the character" :255-259) -- web/getWord.php:409-421 preprocess_unicode_input only decodes `%uXXXX` -- web/dal.php:83@14cd802: `$result = $this->file_db->query($sql);` -- same pattern web/dal.php:120: `$sql = "select * from {$this->dict} where key='$key' order by lnum";` (also :126,:179,:408). PDO sqlite driver; no prepare/bindValue anywhere in web/ (grep).
- **Failure scenario:** GET getWord.php?word=x%22%20or%201=1%20--%20 : transcoder passes `"`, ` `, `=`, `-` through, query becomes select * from `t` where `key`="x" or 1=1 -- " returning the whole table; UNION/attach-style payloads read any table in the attached sqlite files (dal.php:31 ../sqlite/db, :39/:41 ../MWScan/2020/web/sqlite, ../../mw/web/sqlite). index.php's init_inputs_key (:99-114) strips `$ % # < > = ( )` but not `"`, and getWord.php applies no filter at all.
- **Proposed fix:** Switch every `$this->file_db->query("...$var...")` to prepared statements ($this->file_db->prepare('select * from `'.$table.'` where `key`=?')->execute([$key])) with `$table`/`$this->dict` validated against a whitelist (the keys of $dbinfo at dal.php:29); reject `word` values containing characters outside the slp1/IAST/Devanagari sets before transcoding.
- **Refuter votes:** [{"refuted": false, "confidence": "high", "reason": "CORRECTNESS: at csl-inflect@14cd802 web/getWord.php:15-24 `$keyin=$_GET['word']` -> preprocess_unicode_input (only %uXXXX) -> transcoder_processString($keyin1,$filterin,'slp1') where default $filterin='SLP2SLP' (:402-408) returns input unchanged (from==to); :139 `$sql = \"select * from `$table` where `key`=\\\"$key\\\"\";` executed verbatim by web/dal.php:83 `$this->file_db->query($sql)`; no prepare/bind anywhere under web/. Caller web/main.js:103-106 builds `getWord.php?word=`+escape(word). PRIOR-ART: nothing on csl-inflect web security in 

### B2. Correction form shows thank-you page regardless of write outcome; response endpoint has unchecked fopen, CORS *, no size/rate limit

- **Where:** [csl-corrections](https://github.com/sanskrit-lexicon/csl-corrections/tree/e363439) · [app/correction_form_response.php:2,8-19,29-32](https://github.com/sanskrit-lexicon/csl-corrections/blob/main/app/correction_form_response.php#L2) · category `silent-failure` · severity **medium** · effort medium · prior art: new (none)
- **Claim:** User-submitted corrections are lost silently: the form posts into a hidden iframe whose onload unconditionally redirects to the thank-you page, so a 500 (e.g. unwritable correction_response/ directory -> fopen false -> PHP 8 TypeError on fwrite) or an empty response still tells the user the correction was recorded. The endpoint also accepts unbounded POST bodies from any origin (CORS *) into an append-only TSV that the daily GitHub Action commits verbatim.
- **Evidence:** app/correction_form.php:111-116@e363439: `<iframe name="hidden_iframe" ... onload="if(submitted) {window.location='correction_form_thankyou.php';submitted=false;}">` + `<form action="correction_form_response.php" method="post" target="hidden_iframe"` -- app/correction_form_response.php:29-32@e363439: `$fileout = "correction_response/cfr-$filedate.tsv"; $fp = fopen($fileout,"a"); fwrite($fp,"$out\n"); fclose($fp);` (no `if ($fp === false)`, no flock, no return status) -- :2 `header("Access-Control-Allow-Origin: *");` -- :8-19 `$entry_dict = ...$_POST['entry_dict']` ... only `\n`/`\t` replaced, no length cap, no CSRF token, no rate limit -- contrast app/correction_form.php:11-41 which does `preg_replace('|[^A-Za-z0-9]|','',...)` + length caps on the GET side only.
- **Failure scenario:** Cologne host rotates permissions or disk fills: fopen returns false, PHP 8 raises TypeError: fwrite(): Argument #1 must be of type resource, response is 500 with an HTML error page; iframe onload fires anyway, user sees correction_form_thankyou.php; the scholar's correction never reaches cfr-`<date>`.tsv and no log/alert exists. Separately any third-party page can POST megabyte bodies that the cron job commits into daily/`<date>`/ and files as a GitHub issue (issue creation step at workflow :40-61).
- **Proposed fix:** In correction_form_response.php: `$fp = fopen(...); if ($fp === false || flock(...) === false) { http_response_code(500); echo 'ERR'; exit; }`, cap each field (e.g. 2000 chars) and total body, drop the `*` CORS header (same-origin form), optionally a simple per-IP token bucket file. In correction_form.php replace the iframe onload redirect with a fetch() POST that checks `response.ok` before navigating to thank-you and shows an error otherwise.
- **Refuter votes:** [{"refuted": false, "confidence": "medium", "reason": "CORRECTNESS: csl-corrections@e363439 correction_form_response.php:28-32 `$fp = fopen($fileout,\"a\"); fwrite($fp,\"$out\\n\"); fclose($fp);` — no false check, no flock, no error surfaced; caller correction_form.php:111-116 posts into a hidden_iframe and then unconditionally shows the thank-you page, so a failed append (permissions, full disk, PHP8 TypeError on fwrite(false)) is invisible to the submitter and to the maintainer. PRIOR-ART: greps of docs/PIPELINE_AUDIT_CSL_ORIG_CORRECTION_QUEUE_06-08-2026.md, correction-workflow.md, BATCH_RUN

### B3. fetch_yesterday_cfr.sh curl lacks -f: 404 HTML is saved and parsed, workflow red on every no-submission day (11 of last 20 scheduled runs)

- **Where:** [csl-corrections](https://github.com/sanskrit-lexicon/csl-corrections/tree/e363439) · [fetch_yesterday_cfr.sh:18,23-30](https://github.com/sanskrit-lexicon/csl-corrections/blob/main/fetch_yesterday_cfr.sh#L18) · category `ci` · severity **medium** · effort trivial · prior art: new (none)
- **Claim:** The daily fetch does not distinguish 'no corrections yesterday' (Cologne returns 404) from a real failure: curl without -f writes the 404 HTML page to cfr-`<date>`.tsv, `[ -s ]` passes, cfr_adj.py exits 1 on 'tab-parts should be 8, but is 1', and the workflow fails. The job fails closed (good) but is red more than half the time, so a genuine breakage (host down, format change) is indistinguishable from a quiet day and will be ignored.
- **Evidence:** fetch_yesterday_cfr.sh:18@e363439: `if ! curl -o "$OUTPUT" "$URL"; then` (no `-f`/`--fail`, no `-sS`) -- :23-30 `if [ -s "$OUTPUT" ]; then ... python3 ../../cfr_adj.py ... else ... exit 1` -- cfr_adj.py:29-34@e363439: `if len(parts)!= 8: print("# of tab-parts should be 8, but is",len(parts)) ... exit(1)` -- .github/workflows/fetch-daily-corrections-from-cologne.yml:24@e363439 `./fetch_yesterday_cfr.sh` (no `continue-on-error`, no `permissions:` block, no `concurrency:`) -- `gh run view 33582426561 --log-failed`: "Downloading .../cfr-20260901.tsv..." -> "# of tab-parts should be 8, but is 1" -> "b'Error 1 for line 1:\n<!DOCTYPE html>'" -> exit 1 -- `gh run list` (workflow fetch-daily-corrections-from-cologne, last 20 scheduled): 11 failure / 9 success.
- **Failure scenario:** A day with zero submissions: Cologne serves a 404 HTML page, curl exit 0, file non-empty, cfr_adj.py exit 1, workflow status failure, email notification to maintainer; after weeks of red-on-quiet-days the maintainer stops reading them and a real outage of the Cologne host (identical symptom) goes unnoticed; no issue is created because the step never reaches :40-61.
- **Proposed fix:** Use `curl -fsS -o "$OUTPUT" "$URL"`; on HTTP 404 (`curl -w '%{http_code}'` or exit 22) write nothing, echo 'no corrections for `<date>`' and `exit 0` so the workflow is green; keep `exit 1` for any other status or for cfr_adj.py failure. Add `permissions: contents: write, issues: write` and `concurrency: fetch-daily` to the workflow.
- **Refuter votes:** [{"refuted": false, "confidence": "high", "reason": "CORRECTNESS: csl-corrections@e363439 fetch_yesterday_cfr.sh:3 `set -e`, :18 `if ! curl -o \"$OUTPUT\" \"$URL\"; then` (no -f, so a 404 HTML page is saved as the .tsv), :23-31 `[ -s ]` passes and cfr_adj.py exits 1 on `# of tab-parts should be 8`. gh run log 33582426561 (2026-09-02): 'File downloaded successfully, running cfr_adj.py... / # of tab-parts should be 8, but is 1 / b'Error 1 for line 1:\\n<!DOCTYPE html>' / exit code 1' — mechanism confirmed. Last 20 scheduled runs of fetch-daily-corrections-from-cologne.yml in my window: 10 failur

### B4. manual/pft/redo.sh regenerates the FUTURE table under the pft filename (copy-paste from fut), so tables_pft.txt can never be rebuilt from Huet data

- **Where:** [csl-inflect](https://github.com/sanskrit-lexicon/csl-inflect/tree/14cd802) · [verbs/pysanskritv2/manual/pft/redo.sh:2](https://github.com/sanskrit-lexicon/csl-inflect/blob/main/verbs/pysanskritv2/manual/pft/redo.sh#L2) · category `correctness` · severity **medium** · effort trivial · prior art: new (none)
- **Claim:** The periphrastic-future regeneration script reads the simple-future Huet extract and the fut exclusion list (which does not exist in pft/), so running it either fails (missing fut_exclude.txt) or, if copied, overwrites the pft table with future forms; the committed ../tables_pft.txt consumed by models/redo.sh is therefore unreproducible and the readme documents a different command than the script executes.
- **Evidence:** verbs/pysanskritv2/manual/pft/redo.sh:2@14cd802: `python huet_table.py 0 fut_exclude.txt ../../../../huetdata/mapextract/huet_conj_tables_fut.txt tables_fut_huet.txt` -- verbs/pysanskritv2/manual/pft/readme.txt@14cd802: `python huet_table.py 0 pft_exclude.txt ../../../../huetdata/mapextract/huet_conj_tables_pef.txt tables_pft_huet.txt` + `cp tables_pft_huet.txt ../tables_pft.txt` -- `git ls-files verbs/pysanskritv2/manual/pft/` has no fut_exclude.txt -- verbs/pysanskritv2/models/redo.sh:14@14cd802 consumes `../manual/tables_pft.txt` -- verbs/pysanskritv2/manual/redo.sh:1-8@14cd802 only descends into aor and prf (pft never run from the top-level driver) -- untouched since 1618fdd 2020-07-03; docs/PIPELINE_MANUAL.md mentions pft only as a tense-code spelling note (:258,:286), not this bug.
- **Failure scenario:** Maintainer updates huetdata (new SL_morph.xml), runs `sh redo.sh` in manual/pft to refresh periphrastic-future tables: python errors on missing fut_exclude.txt (no set -e, exits 0 for the shell); if they copy fut_exclude.txt in, tables_fut_huet.txt is produced and the `cp` to ../tables_pft.txt (present only in readme) is never done -- models/redo.sh keeps using the 2020 tables_pft.txt, so any Huet correction to periphrastic futures never reaches the sqlite tables or the web app.
- **Proposed fix:** Rewrite pft/redo.sh to the readme command: `python3 huet_table.py 0 pft_exclude.txt ../../../../huetdata/mapextract/huet_conj_tables_pef.txt tables_pft_huet.txt && cp tables_pft_huet.txt ../tables_pft.txt`; add pft (and the other per-tense dirs) to manual/redo.sh; note in PIPELINE_MANUAL.md that tables_pft.txt is regenerable.
- **Refuter votes:** [{"refuted": false, "confidence": "medium", "reason": "CORRECTNESS: csl-inflect@14cd802 verbs/pysanskritv2/manual/pft/redo.sh contains verbatim `echo \"construct tables_fut_huet.txt \"` / `python huet_table.py 0 fut_exclude.txt ../../../../huetdata/mapextract/huet_conj_tables_fut.txt tables_fut_huet.txt` (identical to fut/redo.sh); pft/ holds no fut_exclude.txt (git ls-files: huet_table.py, pft_exclude.txt, readme.txt, redo.sh, tables_pft_huet.txt), while pft/readme.txt documents the correct command with pft_exclude.txt and huet_conj_tables_pef.txt. models/redo.sh:14 consumes ../manual/tables_

### B5. models/redo.sh concatenates calc_models_*.txt after twelve unchecked root_model.py runs; whole csl-inflect regen chain lacks set -e, sqlite loaders rm/mv blindly

- **Where:** [csl-inflect](https://github.com/sanskrit-lexicon/csl-inflect/tree/14cd802) · [verbs/pysanskritv2/models/redo.sh:2,4-55,57](https://github.com/sanskrit-lexicon/csl-inflect/blob/main/verbs/pysanskritv2/models/redo.sh#L2) · category `silent-failure` · severity **medium** · effort medium · prior art: new (none)
- **Claim:** A single failing tense in the verb-model derivation drops that tense from calc_models.txt silently: the script deletes previous outputs, runs each tense without checking status, then globs whatever survived into the combined file that feeds the sqlite tables. No redo/redo_* script in huetdata/, nominals/, verbs/ or sqlite/ uses `set -e`, and the sqlite loaders `rm` the old db before `sqlite3 < .sql` and `mv` regardless of result, so a bad load leaves no database at all.
- **Evidence:** verbs/pysanskritv2/models/redo.sh:2@14cd802: `rm calc_models_*.txt` -- :4-55 twelve lines of `python3 root_model.py 5,<tense> ... calc_models_<tense>.txt` with no `||`/`set -e` -- :57 `cat calc_models_*.txt > calc_models.txt` -- `grep -rl 'set -e' --include='*.sh' huetdata nominals verbs sqlite` returns nothing -- sqlite/lgtab1/redo_lgtab1.sh@14cd802: `rm $dbfile; sqlite3 $dbfile < $dbname.sql; mv $dbfile ../db/` (unchecked) and :3 dead `smfile="../../outputs/nominals/stem_model_tab.txt"` (path does not exist in ls-files) -- sqlite/db/ contains only readme.md (databases are build products with no regen command in README) -- huetdata/redo_one.sh:24,30@14cd802 bare `python` (known ben/prf `||` omission at :10 is docs/PIPELINE_MANUAL.md:238,:280).
- **Failure scenario:** root_model.py crashes on the `ppf` tense after a huetdata refresh (e.g. new model code not in its dictionary): calc_models_ppf.txt is missing, `cat` builds calc_models.txt from 11 tenses, sqlite/vlgtab*/redo scripts load it, the web app answers periphrastic-perfect lookups with no results and nothing in the run output flagged it; if `sqlite3 < .sql` fails on a syntax error, `mv` moves a zero-table file into ../db/ and dal.php:79 echoes the sqlite path to the browser.
- **Proposed fix:** Add `set -euo pipefail` to every redo*.sh; replace the glob with an explicit ordered list of the twelve files and `test -s` each; in sqlite loaders build to a temp file and `mv` only on `sqlite3` success; delete the dead smfile line; document `sqlite/db/*.sqlite` regeneration order in README.
- **Refuter votes:** [{"refuted": false, "confidence": "medium", "reason": "CORRECTNESS: csl-inflect@14cd802 verbs/pysanskritv2/models/redo.sh:2 `rm calc_models_*.txt`, :4-55 twelve unchecked `python3 root_model.py 5,<tense> ...`, :57 `cat calc_models_*.txt > calc_models.txt` (a failed tense silently vanishes from the concatenation); `grep -rl 'set -e'` over huetdata nominals verbs sqlite web -> nothing; sqlite/lgtab1/redo_lgtab1.sh does `rm $dbfile` then `sqlite3 $dbfile < $dbname.sql` then `mv $dbfile ../db/` with no checks. PRIOR-ART partial: docs/PIPELINE_MANUAL.md section 5 already records the ben/prf bug ('n

### B6. PIPELINE_MANUAL/README claim 'no third-party packages, plain python3' but huetdata requires lxml and scripts call bare `python`; no requirements/pyproject, CI is dependabot only

- **Where:** [csl-inflect](https://github.com/sanskrit-lexicon/csl-inflect/tree/14cd802) · [docs/PIPELINE_MANUAL.md:224](https://github.com/sanskrit-lexicon/csl-inflect/blob/main/docs/PIPELINE_MANUAL.md#L224) · category `docs-drift` · severity **low** · effort trivial · prior art: new (none)
- **Claim:** The stated environment contract is wrong on both axes: the entry stage of the pipeline imports lxml (third-party) and uses str.maketrans (py3-only), while several redo scripts invoke `python` (unqualified, which on modern macOS/Debian is absent or python2); there is no requirements.txt, pyproject.toml or workflow that would catch either, so a fresh clone following the manual fails at step one.
- **Evidence:** docs/PIPELINE_MANUAL.md:224@14cd802: "python3 on PATH (plain scripts, no third-party packages, no build step)" -- README.md@14cd802 "no build step" -- huetdata/verbs_tp.py:20@14cd802: `from lxml import etree`; :18 `str.maketrans(slp_from,slp_to)` -- huetdata/redo_one.sh:24,30@14cd802 bare `python`; sqlite/vlgtab2/redo_distrib.sh:2 bare `python`; verbs/pysanskritv2/manual/pft/redo.sh:2 bare `python` -- `git ls-files | grep -Ei 'requirements|pyproject|setup\.(py|cfg)|Pipfile|environment\.yml'` returns nothing -- .github/ contains dependabot config only, no test/lint workflow -- minor: PIPELINE_MANUAL.md:245 '678 entries' for huet_mw_map_data vs huet_mw_map_data.py having 34 dict entries (678 is the .txt line count) -- CHANGELOG.md [0.1.0] 2026-07-15 only, Unreleased empty.
- **Failure scenario:** New maintainer on macOS 15 follows PIPELINE_MANUAL step 1 (`sh redo_one.sh` in huetdata): `python: command not found` for the bare invocations, and `ModuleNotFoundError: No module named 'lxml'` for the python3 ones; with no set -e the script exits 0 and downstream stages read empty mapextract files, propagating empty tables into nominals/verbs/sqlite.
- **Proposed fix:** Add requirements.txt (`lxml>=4`), a one-line `python3 -m pip install -r requirements.txt` in README/PIPELINE_MANUAL step 0, replace bare `python` with `python3` (3 files), correct :224 and :245, and add a minimal GitHub Actions workflow that runs `python3 -m py_compile` over the on-chain scripts plus `bash -n` over every redo*.sh.
- **Refuter votes:** [{"refuted": false, "confidence": "medium", "reason": "CORRECTNESS: csl-inflect@14cd802 docs/PIPELINE_MANUAL.md:224 `- **python3** on PATH (plain scripts, no third-party packages, no build step).` contradicted by huetdata/verbs_tp.py:20 `from lxml import etree` (verbs_tp.py:18 str.maketrans is py3-only) invoked from huetdata/redo_one.sh:24 `python ${extractpy} download/SL_morph.xml ...` and :30 `python huet_mw_map.py ...` with bare `python`, as do all manual/`<tense>`/redo.sh. No requirements/pyproject; .github has only dependabot*.yml; lxml is mentioned nowhere in docs/README/huetdata readmes. 

### B7. apidev utilities/transcoder/*.xml drifted from websan twin (missing ˚↔॰ rule, as_roman.xml fork) and is outside twin-drift.yml + apidev_copy.sh

- **Where:** [csl-apidev](https://github.com/sanskrit-lexicon/csl-apidev/tree/e5ed207) · [.github/workflows/twin-drift.yml:51-56](https://github.com/sanskrit-lexicon/csl-apidev/blob/main/.github/workflows/twin-drift.yml#L51) · category `correctness` · severity **medium** · effort trivial · prior art: extends (CODEBASE_ADVERSARIAL_AUDIT_2026-08 A25/G4 (twin drift, fixed for transcoder.php by H3634 28-08-2026); H3634 guard covers 4 PHP files only)
- **Claim:** H3634 made transcoder.php byte-identical between apidev and websan, but the FSM XML tables the PHP loads are not guarded and have already diverged: websan added the U+0970 abbreviation-sign rule to slp1_deva.xml/deva_slp1.xml (9e72f5e 2026-01-25) and reworked as_roman.xml (3cd4abe 2019-09-29); apidev still ships the 2018/2021/2022 versions. Executed on scratchpad copies with COLOGNE/iast/transcoder.py: `kf˚` -> websan कृ॰, apidev कृ˚.
- **Evidence:** csl-apidev/.github/workflows/twin-drift.yml:51-56@e5ed207: `twins=(\n "basicadjust.php:v02/makotemplates/web/webtc/basicadjust.php"\n "basicdisplay.php:..."\n "getword_data.php:..."\n "utilities/transcoder.php:v02/makotemplates/web/utilities/transcoder.php"\n)` — no utilities/transcoder/*.xml entry. csl-websanlexicon/v02/apidev_copy.sh@2750327 copies only basicadjust.php/basicdisplay.php/getword_data.php. diff apidev utilities/transcoder/slp1_deva.xml vs websan v02/makotemplates/web/utilities/transcoder/slp1_deva.xml: `162a163,166 > <e> <s>INIT,SKT</s> <in>˚</in> <out>॰</out> <cl>atom</cl> </e>`; deva_slp1.xml `200a201,204 > <e> <s>INIT</s> <in>॰</in> <out>˚</out> </e>`; as_roman.xml: websan has `a5→ã`, `i13→ī3 <!-- for gra -->`, apidev has `d4→d́`, `h4→h́`, `e14`. Git: apidev slp1_deva.xml 35cac03 2021-08-08, deva_slp1.xml 9fbf784 2022-04-25, as_roman.xml c686084 2018-04-17; websan slp1_deva.xml/deva_slp1.xml 9e72f5e 2026-01-25, as_roman.xml 3cd4abe 2019-09-29. 4 XMLs exist only in apidev (as_romanorig, beng_slp1, cyrillic_slp1, velthuis_slp1).
- **Failure scenario:** A user of the apidev API requests deva output for an entry whose SLP1 body contains the CDSL abbreviation mark `˚` (e.g. MW `kf˚`, PW compounds): the website renders कृ॰ (U+0970) while the API returns कृ˚ (raw U+02DA inside Devanagari); the same PHP code, same input, two outputs. twin-drift.yml passes green because it never compares the XML tables. `as_roman` (AS→IAST) output also differs for a5/i10/u10/i13/d4/h4 tokens between the two hosts.
- **Proposed fix:** 1) Copy websan v02/makotemplates/web/utilities/transcoder/{slp1_deva,deva_slp1,as_roman}.xml over apidev utilities/transcoder/ (sync PR). 2) Add to twin-drift.yml twins array every XML present in both dirs (13 identical + 3 drifted), or a directory-level `diff -r --exclude='*.ser' --exclude='*.py'` step. 3) Extend apidev_copy.sh to copy utilities/transcoder/*.xml. Decide separately whether the 4 apidev-only XMLs (beng/cyrillic/velthuis/as_romanorig) should be upstreamed to websan.
- **Refuter votes:** [{"refuted": false, "confidence": "medium", "reason": "CORRECTNESS: csl-apidev@e5ed207 .github/workflows/twin-drift.yml:51-56 twins array lists only basicadjust.php, basicdisplay.php, getword_data.php, utilities/transcoder.php; csl-websanlexicon/v02/apidev_copy.sh copies only the three PHP files. Verified diff: websan (2750327) slp1_deva.xml and deva_slp1.xml carry the `˚`<->U+0970 abbreviation-sign rule added in 9e72f5e (2026-01-25); apidev copies last touched 35cac03 (2021-08-08) lack it; as_roman.xml differs (e.g. websan `a5`->U+00E3, apidev `a5`->`a5`). apidev uses these tables live (api0_

### B8. csl-inflect ships its own Deva->SLP1 JS map (no ळ/ँ/digits/nukta) and a v00-lineage transcoder.php that silently returns input when the FSM is missing

- **Where:** [csl-inflect](https://github.com/sanskrit-lexicon/csl-inflect/tree/14cd802) · [web/main.js:35-60,104](https://github.com/sanskrit-lexicon/csl-inflect/blob/main/web/main.js#L35) · category `duplication` · severity **medium** · effort medium · prior art: extends (code_duplication_census.md (lists csl-inflect transcoder.php copy) ; CODEBASE_ADVERSARIAL_AUDIT W11/A17 (fail-silent transcoder.php, fixed in websan/apidev only))
- **Claim:** csl-inflect is a live web consumer that neither vendors sanskrit-util nor shares the websan transcoder: web/main.js hand-rolls devanagariToSLP1 (c8b1831 2026-07-09) from partial tables, and web/utilities/transcoder.php (a606f9f 2022-10-12, 449 lines, 169 diff lines vs websan v02) predates the H3634 fail-loud/APCu work. Executed (scratchpad/engines/cmp.js): `ळ` -> main.js `ळ` (untouched) vs SU `La`; `अँ` -> `aँ` vs `aM`; `यजमानोऽनु` -> `yajamAno'nu` (matches FSM); `राम१` -> `rAma१` both.
- **Evidence:** csl-inflect/web/main.js:57@14cd802 `function devanagariToSLP1(text) {` with tables at :35-50 lacking ळ, ँ, ॐ, ०-९, nukta; :55 `var DEVA_AVAGRAHA = "'";  // ऽ`; caller ~:104 `if (transLit === "DEVA") { word = devanagariToSLP1(word); transLit = "SLP2SLP"; }`. web/utilities/transcoder.php:41 `if (!file_exists($filein)) {return;}`; :270-278 `if(isset($transcoder_fsmarr[$fromto])) {\n  $fsm = $transcoder_fsmarr[$fromto];\n } {\n  transcoder_fsm($from,$to);\n  $fsm = $transcoder_fsmarr[$fromto];\n  if (!$fsm) {\n//   echo "could not find fsm\n";\n   return $line;` (note the `} {` — the second block is unconditional, so transcoder_fsm() re-runs on every call). websan v02 transcoder.php:351-352@2750327 by contrast: `error_log("transcoder: no FSM for $fromto; failing loud"); throw new RuntimeException(...)`. Callers: web/getWord.php:87-88 `transcoder_processElements($ans,"slp1",$filter,"SA")`.
- **Failure scenario:** User enters a Vedic form with ळ (e.g. अग्निमीळे) or candrabindu in the csl-inflect Devanagari box: the untouched Devanagari char is sent as `SLP2SLP` input, the SLP1 lookup fails with no error. Server side, a missing/renamed XML in web/utilities/transcoder/ (13 files, v00 lineage) makes getWord.php return raw SLP1 labelled as the requested script, silently; plus every request re-parses the XML because of the unconditional block at :272.
- **Proposed fix:** 1) Vendor js/sanskrit-util.global.js into csl-inflect/web/vendor/ and replace devanagariToSLP1 with `SanskritUtil.deva_to_slp1` (after finding 2 is settled — the avagraha/candrabindu semantics must match main.js's current `'` behaviour, so prefer the fsm-compat mode). 2) Replace web/utilities/transcoder.php + transcoder/ with the websan v02 copy (byte-identical to apidev post-H3634) and add csl-inflect to the twin-drift matrix, or make csl-inflect a git-subtree/submodule consumer of the websan utilities dir. 3) Fix the `} {` at :272 into `else {` in whichever copy survives.
- **Refuter votes:** [{"refuted": false, "confidence": "medium", "reason": "CORRECTNESS partial: csl-inflect@14cd802 web/main.js:35-55 tables lack ळ, ँ, ॐ, digits, nukta; :55 `var DEVA_AVAGRAHA = \"'\";`, :100-103 routes DEVA through devanagariToSLP1 then `transLit = \"SLP2SLP\"`; web/utilities transcoder.php:28-41 transcoder_fsm: `if (isset($transcoder_fsmarr[$fromto])) { return; } ... if (!file_exists($filein)) {return;}` — missing XML is a silent identity passthrough. The finder's sub-claim that the `} {` block re-parses XML every request is FALSE: transcoder_fsm caches in $transcoder_fsmarr and returns early o

### B9. CI 70% coverage gate measures exactly 2 statements; subprocess-driven tests are invisible to pytest-cov

- **Where:** [csl-pywork](https://github.com/sanskrit-lexicon/csl-pywork/tree/e460545) · [.github/workflows/ci.yml:59-67](https://github.com/sanskrit-lexicon/csl-pywork/blob/main/.github/workflows/ci.yml#L59) · category `ci` · severity **high** · effort medium · prior art: extends (csl-observatory/docs/CODEBASE_ADVERSARIAL_AUDIT_2026-08.md line 36 ("70% coverage CI gate effectively measures dictparms.py alone") — new: exact measurement + the subprocess mechanism that would keep updateByLine uncovered even if the omit were lifted)
- **Claim:** Running the exact CI command in a scratchpad copy reports `v02/dictparms.py 2 0 100% / TOTAL 2` — the `--fail-under=70` gate is vacuous (2 statements). updateByLine.py is under the omitted `v02/makotemplates/**` AND its 15 tests execute it via `subprocess.run([sys.executable, str(SCRIPT), ...])`, so pytest-cov cannot attribute any line to it; test_redo_xampp_selective.py likewise runs redo_xampp_selective.py (explicitly omitted) via subprocess. 261 tests pass in 6.8 s but the gate would stay green with every updateByLine assertion deleted.
- **Evidence:** ci.yml:59-67@e460545: `- run: pip install pytest pytest-cov` … `python -m pytest tests/ -v --cov=v02 --cov-report=term-missing --cov-config=.coveragerc` … `- name: Coverage gate (v02 directly-imported modules)` `run: python -m coverage report --fail-under=70`; .coveragerc:5-10@e460545: `omit = v02/makotemplates/** v02/distinctfiles/** v02/utilities/** v02/generate.py v02/redo_xampp_selective.py`; tests/test_updateByLine.py:14,26-28@e460545: `SCRIPT = Path(__file__).parent.parent / "v02" / "makotemplates" / "pywork" / "updateByLine.py"` … `result = subprocess.run([sys.executable, str(SCRIPT), str(fin), str(fchg), str(fout)], capture_output=True, ...)`; live scratchpad run output: `Name Stmts Miss Cover / v02/dictparms.py 2 0 100% / TOTAL 2 0 100% / 261 passed in 6.77s`
- **Failure scenario:** A regression in updateByLine.update() (e.g. `del` op off-by-one) with the test file deleted or skipped: coverage report still `TOTAL 2 0 100%`, `--fail-under=70` passes, CI green. Conversely, adding one untested 5-statement helper to dictparms.py drops coverage to 28% and fails the gate for an unrelated reason.
- **Proposed fix:** (1) Convert test_updateByLine.py to in-process: `sys.path.insert(0, v02/makotemplates/pywork); import updateByLine; updateByLine.update(fin, fchg, fout)` with `pytest.raises(SystemExit)` for the two error cases (updateByLine.py parses as plain Python — 0 Mako directives, AST-OK). (2) Narrow the omit: replace `v02/makotemplates/**` with an explicit list of Mako-templated files (hw.py, hwparse.py, make_xml.py, sqlite/sqlite.py, redo_*.sh) so the C-category pure-Python files (updateByLine.py, parseheadline.py, hw0.py, hw2.py, sqlite/sqlite_txt.py, webtc2/init_query.py) are measured. (3) Set fail-under to the honest number after (1)-(2), or drop the gate and name the job by what it does.
- **Refuter votes:** [{"refuted": false, "confidence": "high", "reason": "CORRECTNESS: csl-pywork@e460545 .github/workflows/ci.yml:59-67 runs pytest `--cov=v02 --cov-config=.coveragerc` then `coverage report --fail-under=70`; .coveragerc:5-10 omits makotemplates/**, distinctfiles/**, utilities/**, generate.py, redo_xampp_selective.py, tests/**, leaving only v02/dictparms.py (3 top-level nodes); tests/test_updateByLine.py:14,26-28 drive updateByLine.py via subprocess so nothing is traced. Live CI log of run 33247698814 (2026-08-29): `v02/dictparms.py 2 0 100%` / `TOTAL 2 0 100%` — the 70% gate is satisfied by two s

### B10. redo_postxml.sh executes twice per generate_dict.sh run (redo_xml.sh:21 and generate_dict.sh:143) — sqlite + query_dump rebuilt twice

- **Where:** [csl-pywork](https://github.com/sanskrit-lexicon/csl-pywork/tree/e460545) · [v02/generate_dict.sh:143](https://github.com/sanskrit-lexicon/csl-pywork/blob/main/v02/generate_dict.sh#L143) · category `duplication` · severity **medium** · effort trivial · prior art: extends (csl-observatory/docs/CODEBASE_ADVERSARIAL_AUDIT_2026-08.md line 34 (pipeline flow lists `redo_xml.sh (… → redo_postxml.sh) → redo_postxml.sh` without flagging it) and O2 (cites line 143 only for missing red-coloring) — the double execution itself is not recorded as a defect anywhere in the prior-art set (grep 'twice' empty))
- **Claim:** generate_dict.sh runs `sh redo_xml.sh`, whose last line is `sh redo_postxml.sh`, then unconditionally runs `sh redo_postxml.sh` again. Live acsj log shows two complete `BEGIN pywork/redo_postxml.sh` blocks, each removing/rebuilding acsj.sqlite (638 rows) and query_dump.txt (637 records) and re-copying acsjheader.xml. inventory.txt:6 even documents `; redo_xml.sh calls redo_postxml.sh`.
- **Evidence:** generate_dict.sh:122@e460545: `output=$(sh redo_xml.sh 2>&1)`; generate_dict.sh:143@e460545: `sh redo_postxml.sh`; makotemplates/pywork/redo_xml.sh:21@e460545: `sh redo_postxml.sh`; v02/inventory.txt:6@e460545: `; redo_xml.sh calls redo_postxml.sh`; scratchpad attempt2_venv.log: `BEGIN pywork/redo_postxml.sh` appears at lines 36 and 50, each followed by `remaking acsj.sqlite from ../acsj.xml with python...` … `638 rows written to acsj.sqlite` … `637 records written to query_dump.txt`
- **Failure scenario:** For mw (the largest dict) the sqlite build + query_dump + ab/auth tooltip stages are the dominant post-XML cost; every `generate_dict.sh mw` and every batch `redo_xampp_all.sh`/`redo_cologne_all.sh` pays that stage twice. Second run also masks a first-run failure: if sqlite.py fails on run 1 (P9 stale-sqlite path) the second run's output overwrites the diagnostic in the terminal scrollback.
- **Proposed fix:** Delete generate_dict.sh:143 (`sh redo_postxml.sh`) and the surrounding echo, keeping the call inside redo_xml.sh (or the reverse, and drop redo_xml.sh:21) — one-line change; verify with the acsj golden run that outputs are byte-identical (they are deterministic, see snapshot finding).
- **Refuter votes:** [{"refuted": false, "confidence": "high", "reason": "CORRECTNESS: csl-pywork@e460545 v02/generate_dict.sh:122 `output=$(sh redo_xml.sh 2>&1)` and, still in the pywork dir (:144 `cd $curdir` comes after), :143 `sh redo_postxml.sh`; v02/makotemplates/pywork/redo_xml.sh:21 unconditionally `sh redo_postxml.sh`; v02/inventory.txt:6 even comments '; redo_xml.sh calls redo_postxml.sh'. redo_postxml.sh rebuilds web/sqlite and the webtc2 query_dump, so both run twice. PRIOR-ART: audit P13/O2/G1 and H3631 cite :143 and redo_xml.sh:21 only for exit-code handling; GENERATION_MANUAL.md:88,:241 describe a s

### B11. Testable-seam inventory: 6 of 10 pywork sources import unrendered today; the 4 Mako-templated ones need a 1-command render fixture

- **Where:** [csl-pywork](https://github.com/sanskrit-lexicon/csl-pywork/tree/e460545) · [v02/makotemplates/pywork/make_xml.py:1319](https://github.com/sanskrit-lexicon/csl-pywork/blob/main/v02/makotemplates/pywork/make_xml.py#L1319) · category `testing` · severity **medium** · effort medium · prior art: extends (csl-observatory/docs/CODEBASE_ADVERSARIAL_AUDIT_2026-08.md line 36 ("zero tests cover the production core") — new: which units are importable as-is vs need rendering, the exact seams and fixture sizes)
- **Claim:** AST-parsing each pywork source: parseheadline.py, updateByLine.py, hw0.py, hw2.py, sqlite/sqlite_txt.py, webtc2/init_query.py contain 0 Mako directives and parse as Python → in-process pytest today with `sys.path.insert(0, 'v02/makotemplates/pywork')`. hwparse.py (3 directives, SyntaxError line 44), hw.py (17, line 24), sqlite/sqlite.py (8, line 28), make_xml.py (151, line 27) are templates and can only be imported from a rendered copy; rendering is one command that already tolerates a missing distinctfiles/`<dict>` dir (acsj has none and rendered fine): `python3 generate.py acsj inventory.txt makotemplates distinctfiles/acsj <tmp>` (generate_pywork.sh:26). Seams and fixtures: parseheadline.parseheadline(str) — pure, no file; hwparse.init_hwrecs(filein) — 3-line `<L>1<pc>1<k1>a<k2>a<ln1>1<ln2>2` file; updateByLine.update(fin,fchg,fout) — 3-line input + 2-line change (SystemExit on error); init_query.make(filein,fileout) — 3-line `<H1><h><key1>..</key1>..</h><body>..</body>..<L>..</L></H1>` file (0-match variant reproduces P10 NameError, any variant reproduces P11 trailing junk row); sqlite_txt (SCHEMA_MAP, table stcab) — 3 tab-separated rows; make_xml.make_xml(filein,filehw,fileout) — 3-entry orig .txt + matching 3-line hw file, output parsed with ET and validated against the rendered acsj.dtd; sqlite.py main via `sys.argv` monkeypatch on a 3-entry XML. Test-isolation hazard: `class HW: Ldict = {}` (hwparse) and `Entry.Ldict` (hw.py) are class-level mutable dicts shared across calls — fixture must reset them.
- **Evidence:** AST scan (read-only, clone @e460545): `parseheadline.py AST-OK mako_directives=0 · updateByLine.py AST-OK 0 · hwparse.py SyntaxError line 44 mako_directives=3 · hw.py SyntaxError line 24 17 · hw2.py AST-OK 0 · hw0.py AST-OK 0 · make_xml.py SyntaxError line 27 151 · sqlite/sqlite.py SyntaxError line 28 8 · sqlite/sqlite_txt.py AST-OK 0 · webtc2/init_query.py AST-OK 0`; make_xml.py:24@e460545: `from hwparse import init_hwrecs,HW`; make_xml.py:1319@e460545: `def make_xml(filein,filehw,fileout):`; generate_pywork.sh:26@e460545: `python3 generate.py "$dict" inventory.txt makotemplates distinctfiles/${dict} $outdir`; rendered tempparent/acsj/pywork/hwparse.py:13: `dictcode = 'acsj'` vs template `dictcode = '${dictlo}'`; live: `find distinctfiles/acsj` → `No such file or directory` yet render succeeded
- **Failure scenario:** Without a render fixture every attempt to unit-test make_xml/hw/sqlite hits `SyntaxError` at the first `%if dictlo in [...]` line, which is why the existing suite stops at dictparms/updateByLine; with it, a 3-entry fixture per branch (generic + kosha) catches regressions in `get_k1`, `construct_xmlstring_2`, `sort_lines`, and `insert_batch` duplicate handling in <1 s.
- **Proposed fix:** Add tests/conftest.py fixture `rendered_pywork(dict='acsj')` that runs generate.py (needs mako → add `pip install mako` to the test job, currently only smoke-import installs it) into tmp_path and yields the pywork dir on sys.path; add tests/test_parseheadline.py, test_init_query.py, test_sqlite_txt.py (unrendered, zero infra), then test_make_xml_kosha.py / test_make_xml_generic.py (mci) using the fixture; reset `HW.Ldict.clear()` / `Entry.Ldict.clear()` in fixture teardown.
- **Refuter votes:** [{"refuted": false, "confidence": "medium", "reason": "AST inventory of csl-pywork (e460545) verified: no conftest.py or render fixture; also absent at origin/main aefbdac (checked via gh api contents). Partial prior art: audit P10/P11 (fixture-related items) were fixed by PR #85 (H3638), and the local clone lags origin by #84/#85/#87, so the finder's fixture-specific sub-claims are stale; the top-level gap (no render test harness) survives.", "corrected_claim": "csl-pywork @ e460545 (origin/main aefbdac): no conftest.py and no Mako-render fixture exist anywhere in the tree (find -name 'confte

### B12. ci.yml header claims 'No external dependencies or test suite' while the same file installs Mako and runs 261 tests

- **Where:** [csl-pywork](https://github.com/sanskrit-lexicon/csl-pywork/tree/e460545) · [.github/workflows/ci.yml:5](https://github.com/sanskrit-lexicon/csl-pywork/blob/main/.github/workflows/ci.yml#L5) · category `docs-drift` · severity **low** · effort trivial · prior art: new (none)
- **Claim:** The workflow's own banner comment contradicts its jobs: line 5 says no dependencies/no test suite; line 51-67 is a pytest job (261 tests measured live), line 77-78 installs Mako. The job name `Unit tests — updateByLine.py + dictparms.py` and the step name `Coverage gate (v02 directly-imported modules)` are the only honest descriptions, and they are three screens down.
- **Evidence:** ci.yml:5@e460545: `# No external dependencies or test suite; CI validates syntax and core importability.`; ci.yml:51-52@e460545: `test-updateByLine:` `name: Unit tests — updateByLine.py + dictparms.py`; ci.yml:77-78@e460545: `- name: Install Mako (required by generate.py)` `run: pip install mako`
- **Failure scenario:** A maintainer skimming the header decides tests do not exist and adds a parallel test job or skips writing one; the header is the first thing GitHub shows in the workflow editor.
- **Proposed fix:** Replace lines 4-5 with: `# Jobs: ruff syntax, pytest (tests/, coverage of directly-imported v02 modules), smoke-import (needs mako), DTD check, YAML lint.`
- **Refuter votes:** [{"refuted": false, "confidence": "low", "reason": "ci.yml line 5 comment banner still describes a test set that does not match steps at lines 52-78, both locally (e460545) and at origin/main aefbdac. No prior art in audit or handoffs. Materiality is low (comment only, no behavioral effect), but it is correct at tip.", "corrected_claim": "csl-pywork .github/workflows/ci.yml @ e460545 (same at origin/main aefbdac): header comment (line 5) enumerates checks that no longer correspond to the jobs actually run at lines 52-78. Documentation drift only; no runtime impact. No prior art found (audit G1

### B13. csl-corrections PUBLIC repo tracks 398 correction TSVs containing 945 distinct submitter e-mail addresses; .gitignore pattern misses year subdirectories and gitupdate.sh does git add .

- **Where:** [csl-corrections](https://github.com/sanskrit-lexicon/csl-corrections/tree/e363439) · [.gitignore:app/correction_response/cfr-*.tsv pattern; app/gitupdate.sh git add . line; app/correction_form_response.php:6-19](https://github.com/sanskrit-lexicon/csl-corrections/blob/main/.gitignore) · category `security` · severity **high** · effort hard · prior art: new (none (grep of csl-observatory/docs/CODEBASE_ADVERSARIAL_AUDIT_2026-08.md for correction_response/entry_email/csl-corrections/app returned nothing))
- **Claim:** The intake endpoint appends the raw entry_email column to TSVs that are committed and pushed by an auto-commit script into a repository whose GitHub visibility is PUBLIC; the .gitignore rule is anchored to the flat directory and does not cover the year-partitioned files that actually hold the data.
- **Evidence:** .gitignore@e363439: `app/correction_response/cfr-*.tsv` (anchored, no `**`). app/gitupdate.sh@e363439: `git add .` / `git commit -m "correction form submitted at $DATE"` / `git push origin master`. app/correction_form_response.php@e363439:6-19 loops over `entry_dict,entry_L,entry_hw,entry_old,entry_new,entry_comment,entry_email` from `$_POST` and writes them verbatim (only \n -> ' LB ', \t -> '  '). Measured: `git ls-files 'app/correction_response/*.tsv' | wc -l` = 398 (under app/correction_response/2020/, 2021/, ... plus cfr_snowcrest.tsv, new_cfr.tsv); 9253 rows with '@' in column 8; `sort -u` of lower-cased column 8 = 945 distinct addresses. `gh repo view sanskrit-lexicon/csl-corrections --json visibility,isPrivate` -> PUBLIC / false. No README/CONTRIBUTING privacy or e-mail notice (grep returned nothing).
- **Failure scenario:** Any visitor submits the correction form with their e-mail (websan webtc/indexcaller.php:128 posts to https://www.sanskrit-lexicon.uni-koeln.de/scans/csl-corrections/app/correction_form_response.php); the next testgit.php/gitupdate.sh run commits app/correction_response/`<year>`/cfr-`<date>`.tsv and pushes it to the public GitHub repo, where the address is harvestable by anyone (and via git history even after later deletion). 945 people are already exposed; every new submission adds to it.
- **Proposed fix:** 1) Stop new leakage: change .gitignore to `app/correction_response/**/*.tsv` (or move the intake directory outside the git tree entirely, e.g. an absolute path in correction_form_response.php), and replace `git add .` in gitupdate.sh with an explicit path list. 2) Drop or hash the entry_email column at write time (store a one-way hash or nothing; the maintainer reply workflow, if any, needs a private channel). 3) Purge history: rewrite with git filter-repo removing column 8 from all tracked TSVs, force-push, and ask GitHub support to clear cached forks/PR refs; alternatively make the repo private. 4) Add a data-retention/privacy note to README.
- **Refuter votes:** [{"refuted": false, "confidence": "high", "reason": "Verified at csl-corrections e363439 (== origin): correction_form_response.php:6-7 captures entry_email, :28-32 appends to cfr-`<date>`.tsv; daily/`<date>`/cfr-`<date>`.tsv exists for 247 dates, latest 20260830, committed by github-actions[bot]; grep counts 203 emails in daily/ and 945 in historical TSVs. Repo is public. Finder named gitupdate.sh (last used 2023) as the vector; the live vector is the Actions daily cron. Prior art H271 covers correction intake, not email exposure. Materially a PII leak.", "corrected_claim": "csl-corrections @ e36343

### B14. correction_form_response.php is an unauthenticated CORS-* endpoint that appends unbounded request data to disk with no size, rate, CSRF or fopen-failure check and no security headers

- **Where:** [csl-corrections](https://github.com/sanskrit-lexicon/csl-corrections/tree/e363439) · [app/correction_form_response.php:2,6-19,25-32](https://github.com/sanskrit-lexicon/csl-corrections/blob/main/app/correction_form_response.php#L2) · category `security` · severity **medium** · effort medium · prior art: new (none (H1523 in this repo fixed only testgit.php CLI gating; observatory audit has no csl-corrections/app row))
- **Claim:** Any origin can POST arbitrary-length values into seven columns, each request appends one TSV line under a relative path with no write-error handling, and the response carries none of the security_headers.php policy that websan/apidev share.
- **Evidence:** app/correction_form_response.php@e363439:2 `header("Access-Control-Allow-Origin: *");` (no require of a security_headers.php; `grep -rn 'security_headers\|X-Frame\|Content-Security' app/` returns nothing). :6-19 `foreach (array("entry_dict",...,"entry_email") as $col) { $x = $_POST[$col]; $x = str_replace("\n"," LB ",$x); $x = str_replace("\t","  ",$x); $outar[] = $x; }` — no strlen cap, no is_string check (array value -> str_replace on array returns array, join() then emits 'Array'). :25 `$out = join("\t",$outar);` :28-32 `$fileout = "correction_response/cfr-$filedate.tsv"; $fp = fopen($fileout,"a"); fwrite($fp,"$out\n"); fclose($fp);` — fopen result unchecked. app/correction_form.php:115 `<form action="correction_form_response.php"` with no captcha/honeypot/token (grep -iE 'captcha|honeypot|token|csrf' -> only the action line).
- **Failure scenario:** (a) A script POSTs 10,000 requests with 1 MB entry_comment bodies from any origin: the TSV grows by ~10 GB, is then swept into git by gitupdate.sh (finding 1), bloating the public repo and the server disk. (b) The relative path resolves against the web server cwd; when the directory is unwritable or cwd differs, fopen() returns false, fwrite(false) emits a TypeError-free warning under PHP 8 (fopen false -> fwrite(): Argument #1 must be of type resource, false given -> TypeError, HTTP 500 with absolute path if display_errors is on), and the user's correction is lost while the JS caller still reports success. (c) Because the page and its response lack X-Frame-Options/CSP, the form can be framed by a third-party site.
- **Proposed fix:** Add `require_once` of a copy of csl-apidev security_headers.php; restrict Access-Control-Allow-Origin to https://www.sanskrit-lexicon.uni-koeln.de (the only legitimate caller per websan webtc/indexcaller.php:128, webtc1/index.php:76, webtc2/index.php:189); enforce is_string + mb_strlen caps per column (e.g. 32 for dict/L/hw, 4000 for old/new/comment, 254 for email); add a simple honeypot field and per-IP rate limit (or reuse Apache mod_ratelimit); use an absolute `__DIR__ . '/correction_response/'` path, check `$fp === false` and return HTTP 500 JSON on failure; return JSON `{status:...}` instead of silent 200.
- **Refuter votes:** [{"refuted": false, "confidence": "medium", "reason": "Finder misquoted the code: lines 8-19 (e363439) do apply isset() and preg_replace to fields before writing, so 'no sanitisation' is wrong; but the substance holds — fields are only regex-stripped of control chars/tabs, there is no length cap or origin check, and :2 sets `Access-Control-Allow-Origin: *`, so anyone can append arbitrary rows that are auto-committed. Partial prior art: H271.", "corrected_claim": "csl-corrections correction_form_response.php @ e363439: :2 `header('Access-Control-Allow-Origin: *')`; :8-19 isset()+preg_replace st

### B15. PHP 8 TypeError: `?key[]=x` makes Parm call trim() on an array, crashing every Parm-based entry point in both csl-apidev and csl-websanlexicon (confirmed under PHP 8.5.9)

- **Where:** [csl-apidev](https://github.com/sanskrit-lexicon/csl-apidev/tree/e5ed207) · [parm.php:153 (compute_text), 91 (caller); websan v02/makotemplates/web/webtc/parm.php:60](https://github.com/sanskrit-lexicon/csl-apidev/blob/main/parm.php#L153) · category `correctness` · severity **medium** · effort trivial · prior art: extends (csl-observatory CODEBASE_ADVERSARIAL_AUDIT_2026-08.md A15/A16 (type coercion) — those covered int/bool coercion, not array-typed request params; H3636 A10 added try/catch only to apidev getword.php/getsuggest.php/getword_xml.php/api0)
- **Claim:** init_inputs_key() returns $_REQUEST['key'] untouched when it is an array (str_replace on an array returns an array; the is_string guard only applies to the >200-char check), and the subsequent unconditional trim() throws a TypeError under PHP 8; entry points without a Throwable catch (apidev listview.php, getword_batch.php, dalglob.php, api1/salt_*; ALL websan endpoints) therefore fatal.
- **Evidence:** csl-apidev parm.php@e5ed207:153 `trim(` inside compute_text, called from :91; :105-119 init_inputs_key `$ans = str_replace($invalid_characters, "", $x); if (is_string($ans) && mb_strlen($ans) > 200)`. Local run (scratchpad copy, PHP 8.5.9, `$_REQUEST=['key'=>['agni']]`): `Fatal error: Uncaught TypeError: trim(): Argument #1 ($string) must be of type string, array given in .../parm.php:153 ... #0 parm.php(153): trim(Array) #1 parm.php(91): Parm->compute_text(Array) #2 Parm->__construct()`. csl-websanlexicon webtc/parm.php@2750327:58-61 `$this->keyin = $this->init_inputs_key(); if ($this->keyin) { $this->keyin = trim($this->keyin);` — same local run: `Uncaught TypeError: trim(): Argument #1 ($string) must be of type string, array given in .../webtc/parm.php:60`. Uncaught contexts: websan webtc/getword.php@2750327:14 `$temp = new GetwordClass();` (no try); apidev listview.php@e5ed207:86 `$getParms = new Parm();` inline in the HTML body (no try); apidev getword_batch.php:50 `$temp = new GetwordClass();` (no try). error_reporting(E_ALL & ~E_NOTICE & ~E_WARNING) at those entries does not mask fatals.
- **Failure scenario:** GET https://.../csl-websanlexicon/v02/webtc/getword.php?key[]=agni&dict=mw -> uncaught TypeError -> HTTP 500; with display_errors=On (default for php-cli, and any misconfigured vhost) the stack trace with absolute server paths (/var/www/.../webtc/parm.php) is returned to the client; with it Off the user gets a blank 500. Same for apidev listview.php?key[]=x (half-rendered HTML then fatal) and getword_batch.php. For apidev getword.php the H3636 catch turns it into `{"status":500,"error":"trim(): Argument #1 ..."}` — a 500 for what is a 400-class client error, which will pollute error-rate monitoring.
- **Proposed fix:** In both parm.php init_inputs_key(): `if (!is_string($x)) { return ''; }` before str_replace (apidev :105 area; websan :126-143), and guard the trim call sites (`if (is_string($this->keyin))`). Apply the same is_string guard to every other `$_REQUEST[...]` read that later hits a string function (apidev parm.php:57 dict strtolower, :70-area viewAs; websan parm.php:70 `$this->viewAs = $_REQUEST['viewAs']`). Optionally wrap `new Parm()` in listview.php/getword_batch.php with the same Throwable catch as getword.php and map TypeError to HTTP 400. Sync via apidev_copy.sh so both trees stay identical.
- **Refuter votes:** [{"refuted": false, "confidence": "high", "reason": "Verified at csl-apidev e5ed207: parm.php:84-91,:105-119,:152-153 call trim() on possibly-null $_GET values; reproduced TypeError with PHP 8.5.9 CLI in scratchpad copy. Unchanged at origin/main 58b54a0 (gh api contents). No prior art (H1695 covers iast case fold only).", "corrected_claim": "csl-apidev parm.php @ e5ed207 (unchanged at origin/main 58b54a0): trim() applied to unset request parameters at :84-91, :105-119, :152-153 raises `TypeError: trim(): Argument #1 ($string) must be of type string, null given` on PHP 8.x (reproduced with PHP 

### B16. README/docs drift: csl-apidev root has ~10 live PHP entry points but README documents 4 and api_trial.php (live CORS-*/JSONP router on REQUEST_URI) is mentioned in no Markdown file

- **Where:** [csl-apidev](https://github.com/sanskrit-lexicon/csl-apidev/tree/e5ed207) · [README.md:whole file; api_trial.php:1-52,162-176](https://github.com/sanskrit-lexicon/csl-apidev/blob/main/README.md) · category `docs-drift` · severity **low** · effort trivial · prior art: new (none (observatory audit G-series covered docs of salt_* and getword only; issue #75 tracks the app/ unified UI, not root inventory))
- **Claim:** The public entry-point inventory in README.md is stale relative to `git ls-files '*.php'` at the root, and one undocumented file is a fully-featured request router that copies query segments into $_REQUEST and re-dispatches.
- **Evidence:** `git ls-files '*.php' | grep -v /` @e5ed207 with security_headers.php included (i.e. live entries): api_trial.php, cleanurl.php, dalglob.php, getsuggest.php, getword.php, getword_batch.php, getword_xml.php, listhier.php, listview.php, servepdf.php. README.md@e5ed207 names only basicadjust.php, basicdisplay.php, getword_data.php, api1/salt_entries.php. Doc-mention counts (`grep -rl <name> --include=*.md | wc -l`): api_trial.php = 0, getword_xml.php = 2, listhier.php = 3, others >= 5. api_trial.php@e5ed207:52 `$uri = $_SERVER['REQUEST_URI']; $url = preg_replace('|^.*?/api/|','',$uri);` and :162-176 copies key/lnum/regex/input/output segments into `$_REQUEST` before instantiating classes.
- **Failure scenario:** A hardening pass (e.g. the CORS fix above, or a future callback-regex tightening) is driven from README's list and skips api_trial.php; the undocumented router keeps the old behaviour and a security regression is 'fixed' in 9 of 10 entries. Reviewers of issue #75 (app/ unified interface) cannot tell which root files are superseded.
- **Proposed fix:** Add a 'Entry points at repo root' table to README.md generated from `git ls-files '*.php' | xargs grep -l security_headers` with one line each (purpose, JSON/HTML/JSONP, CORS yes/no, status live/legacy); either document api_trial.php's URL grammar or move it to sample/ with a deprecation note; add a CI step that fails when a root *.php with security_headers.php is absent from README.
- **Refuter votes:** [{"refuted": false, "confidence": "low", "reason": "api_trial absent from README.md and all .md at e5ed207 and origin/main 58b54a0. But CODEBASE_ADVERSARIAL_AUDIT_2026-08.md line 63 (A18) already documents api_trial, so novelty is partial; materiality low.", "corrected_claim": "csl-apidev @ e5ed207: `api_trial/` is not mentioned in README.md or any repo .md (grep → 0 hits). Already catalogued in CODEBASE_ADVERSARIAL_AUDIT_2026-08.md:63 (A18); this finding only adds that README remains silent."}]

### B17. update-stardict sync hard-fails on any v02 dir absent from cologne-stardict registry — 3 consecutive failures since NYBJ landed 2026-08-11

- **Where:** [csl-orig](https://github.com/sanskrit-lexicon/csl-orig/tree/dee5a03) · [.github/workflows/update-stardict.yml:87-100](https://github.com/sanskrit-lexicon/csl-orig/blob/main/.github/workflows/update-stardict.yml#L87) · category `correctness` · severity **high** · effort medium · prior art: new (none)
- **Claim:** The post-merge stardict pipeline derives CHANGED_DICTS from every v02/`<dir>` touched and runs make_babylon.py on each with no registry guard; v02/nybj (added 2026-08-11), v02/pwkvn and v02/etymology_stats are not in cologne-stardict/dictdata.py, so make_babylon.py raises KeyError and the loop aborts before move_to_stardict.sh/commit/push — every push touching such a dir silently loses the stardict update for all other dicts in that push. Confirmed via gh: runs for 09c68d1, 0c8e72c, 485bf79 (all NYBJ) failed with `KeyError: 'nybj'`. Separately dictdata keys inm krm mci pe pui are built and committed but never deployed (absent from move_to_stardict.sh).
- **Evidence:** csl-orig .github/workflows/update-stardict.yml:87@dee5a03 `CHANGED_DICTS=$(echo "${{ steps.changed-files.outputs.all_changed_files }}" | tr ' ' '\n' | grep '^v02/' | awk -F'/' '{print $2}' | sort -u)`; :92-99 `for dict in $CHANGED_DICTS; do ... python3 make_babylon.py $dict 0`; cologne-stardict make_babylon.py:291@0590b83 `fout.write('\n#bookname=' + dictdata[dictId][0] + ...)`; `comm` of csl-orig v02 dirs vs dictdata keys → `etymology_stats nybj pwkvn`; dictdata keys not in move_to_stardict.sh → `inm krm mci pe pui`; gh run 31668532366 --log-failed: `Processing dictionary: nybj` / `KeyError: 'nybj'` / `##[error]Process completed with exit code 1`; gh run list: 2026-08-13 failure 485bf79, 2026-08-13 failure 0c8e72c, 2026-08-11 failure 09c68d1
- **Failure scenario:** Push to main touching v02/nybj/nybj.txt together with v02/mw/mw.txt → CHANGED_DICTS='mw nybj' → mw babylon regenerated, then `make_babylon.py nybj 0` KeyError → step exits 1 → no commit to cologne-stardict, no push to indic-dict/stardict-sanskrit → mw stardict stays stale with only an Actions red X as signal. Push touching v02/etymology_stats/README.md alone → FileNotFoundError on ../csl-orig/v02/etymology_stats/etymology_stats.txt → same abort.
- **Proposed fix:** In the loop, skip dicts not registered: `python3 -c "import dictdata,sys; sys.exit(0 if '$dict' in dictdata.dictdata else 1)" || { echo "::warning::$dict not in dictdata.py, skipping"; continue; }`; additionally filter `grep -E '^v02/[a-z0-9]+/[a-z0-9]+\.txt$'` so doc-only dirs never enter; register nybj/pwkvn in dictdata.py+params.py+move_to_stardict.sh or explicitly document the 5 built-but-undeployed dicts (inm krm mci pe pui).
- **Refuter votes:** [{"refuted": false, "confidence": "high", "reason": "Verified: gh run 31668532366 log shows `Processing dictionary: nybj` → `KeyError: 'nybj'` → exit 1; three failed runs (2026-08-11, 08-13 x2), last success 2026-08-04. cologne-stardict dictdata.py (0590b83, also origin/main via gh api) lacks nybj, pwkvn, etymology_stats; make_babylon.py:291 indexes `dictdata[dictId][0]`. csl-orig workflow (dee5a03; origin/main 30b2ae7 identical lines) :87 derives CHANGED_DICTS from any v02/`<dir>` change, so any new dictionary breaks the job. H3851 (open, claimed Sonnet 5, 01-09-2026) names the red branch but n

### B18. Mutable third-party action tj-actions/changed-files@v47 runs in the job holding cross-repo PAT_TOKEN

- **Where:** [csl-orig](https://github.com/sanskrit-lexicon/csl-orig/tree/dee5a03) · [.github/workflows/update-stardict.yml:28](https://github.com/sanskrit-lexicon/csl-orig/blob/main/.github/workflows/update-stardict.yml#L28) · category `security` · severity **high** · effort trivial · prior art: new (none (no hit for tj-actions\|PAT_TOKEN in csl-observatory/docs, reports, Uprava/handoffs, Uprava/docs))
- **Claim:** The only third-party action in the 10-repo set is pinned by mutable tag, in the same job that checks out sanskrit-lexicon/cologne-stardict and indic-dict/stardict-sanskrit with `secrets.PAT_TOKEN` (persisted by actions/checkout into .git/config) and pushes to both. tj-actions/changed-files was the vector of the March 2025 tag-rewrite supply-chain compromise; a re-pointed v47 tag would exfiltrate a PAT with write on two repos. The same PAT is used in regenerate-hwnorm1-sqlite.yml for four more checkouts.
- **Evidence:** csl-orig .github/workflows/update-stardict.yml:28@dee5a03 `uses: tj-actions/changed-files@v47`; :41,:51 `token: ${{ secrets.PAT_TOKEN }}` (cologne-stardict, indic-dict/stardict-sanskrit checkouts); :117 `git push origin main`; :127 push to stardict-sanskrit master; regenerate-hwnorm1-sqlite.yml:43,:51,:59,:67 `token: ${{ secrets.PAT_TOKEN }}`; .github/dependabot.yml github-actions weekly (updates tags, does not SHA-pin)
- **Failure scenario:** Upstream v47 tag force-moved to a malicious commit → next push to v02/** runs attacker code with PAT_TOKEN readable from ../cologne-stardict/.git/config and ../stardict-sanskrit/.git/config → attacker can push to two external repos and any other repo the PAT covers.
- **Proposed fix:** Pin to full commit SHA with version comment (dependabot keeps SHA pins current); replace changed-files with `git diff --name-only ${{ github.event.before }} ${{ github.sha }} -- v02/` (fetch-depth: 2 already set); move PAT-bearing checkouts to `persist-credentials: false` and pass the token only to the push step; scope PAT to the two target repos (fine-grained).
- **Refuter votes:** [{"refuted": false, "confidence": "medium", "reason": "Verified at dee5a03 and origin/main 30b2ae7: :28 `uses: tj-actions/changed-files@v47`; :41,:51 `token: ${{ secrets.PAT_TOKEN }}`; :114,:127 push to cologne-stardict main and indic-dict/stardict-sanskrit master; no `permissions:` block. dependabot.yml covers github-actions weekly but bumps tags, never SHA-pins (PRs #2880, #2862, #2858, #2850, #2851 all tag bumps; none for tj-actions). tj-actions/changed-files was the vector of the March-2025 supply-chain compromise (CVE-2025-30066), which makes tag-pinning here materially risky. No prior ar

### B19. COLOGNE iast/slp1_roman.xml declared 'source of truth' is a 2022 version behind the 2024 copies deployed in csl-apidev and csl-websanlexicon; install_local.sh would regress production

- **Where:** [COLOGNE](https://github.com/sanskrit-lexicon/COLOGNE/tree/ece273d) · [iast/readme.txt:1-12](https://github.com/sanskrit-lexicon/COLOGNE/blob/ece273d/iast/readme.txt#L1) · category `docs-drift` · severity **high** · effort trivial · prior art: extends (COLOGNE/ARCHITECTURE_REVIEW.md P1 transcoder + SHARED_CODE.md (cover transcoder.py copies only, not XML); backlog #483)
- **Claim:** readme.txt names iast/slp1_roman.xml as the source of truth that 'should agree with copies used by php programs', and install_local.sh copies it over the csl-apidev and csl-websanlexicon copies. But COLOGNE's XML last changed 2022-02-16 while both deployed copies changed 2024-07-03 and carry a different accent model (three generic entries n=116-118 mapping `/`,`^`,`\` to combining U+0301/U+0300/U+0331) versus COLOGNE's ~50 per-vowel precomposed entries. Running the documented install step silently reverts production accent transliteration.
- **Evidence:** COLOGNE iast/readme.txt@ece273d `The slp1_roman.xml file herein is the source of truth for the current Cologne dictionaries. This file should agree with copies used by php programs`; iast/install_local.sh `cp slp1_roman.xml /c/xampp/htdocs/cologne/csl-apidev/utilities/transcoder/` and `.../csl-websanlexicon/v02/makotemplates/web//utilities/transcoder/`; md5 COLOGNE/iast/slp1_roman.xml=cc1b5135… (git 2022-02-16) vs csl-apidev/utilities/transcoder/slp1_roman.xml@e5ed207 and csl-websanlexicon/v02/makotemplates/web/utilities/transcoder/slp1_roman.xml@2750327 both 45565b20… (2024-07-03); diff: deployed adds `<!-- 09-19-2022 … -->` `<e n='116'> <s>SKT</s> <in>/</in> <out>́</out> </e>` n=117 `^`→̀ n=118 `\`→̱; COLOGNE lines 16-66 per-vowel `<in>a/</in> <out>á</out>` etc.; roman_slp1.xml e1d45cfa… vs c6d52585….
- **Failure scenario:** Maintainer follows iast/readme.txt, runs `sh install_local.sh`, redeploys csl-apidev → accented input `a/` yields precomposed á while `/` after consonant clusters and `^`/`\` accents are no longer transliterated; roman→slp1 reverse mapping diverges from the 2024 table, breaking round-trip for Vedic accent marks in web search.
- **Proposed fix:** Copy the 2024-07-03 csl-apidev XML pair back into COLOGNE/iast (or re-point readme.txt to csl-apidev as source of truth and delete install_local.sh); add a CI drift check comparing md5 of iast/*.xml against the csl-apidev copies (csl-apidev already has twin-drift.yml as a pattern).
- **Refuter votes:** [{"refuted": false, "confidence": "medium", "reason": "Verified: COLOGNE iast/readme.txt and docs/TOOLING_MANUAL.md:36,:72,:309-313 declare iast/slp1_roman.xml the source of truth that must agree with csl-apidev and csl-websanlexicon copies; COLOGNE copy (ece273d) md5 cc1b5135…, 5431 B, last change b3f223a 2022-02-16; apidev (e5ed207) and websan (2750327) copies identical to each other, md5 45565b20…, 3270 B, last change 2024-07-03 (MWS#169: L → ḻ, apidev :58 `<in>L</in> <out>ḻ</out>` absent from COLOGNE). The declared source is the stale one. Prior art partial: COLOGNE #483 'Consolidate trans

### B20. csl-observatory README states a monthly auto-refresh while the cron is weekly, commits are mislabelled 'monthly refresh', and four legacy reports sit frozen at 2026-06-01 beside live ones

- **Where:** [csl-observatory](https://github.com/sanskrit-lexicon/csl-observatory/tree/d2dea7a) · [README.md:121](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/README.md#L121) · category `docs-drift` · severity **medium** · effort trivial · prior art: new (none (OBSERVATORY_ROADMAP.md D4 'Monthly refresh automation' active; HYPOTHESIS_VIZ_STANDARDS_SPEC_2026-07.md:138 says 'weekly refresh'))
- **Claim:** Stated cadence contradicts the workflow and the data: README:121 says monthly; refresh-observatory.yml runs every Monday and labels each commit 'monthly refresh', giving 8 'monthly' commits between 2026-07-20 and 2026-08-31. reports/dashboard.md, contributors.md, coverage.md, timeline.md come from refresh.yml whose schedule was removed (dispatch-only) and have not been regenerated since 2026-06-01, yet remain in reports/ with only a footnote in reports/README.md and a 'Snapshot date: 2026-06-01' header — readers of the dashboard get 3-month-old headline numbers presented as current.
- **Evidence:** csl-observatory README.md:121@d2dea7a `- **Auto**: monthly via GitHub Actions ([.github/workflows/refresh-observatory.yml])` (blame 2026-05-15); .github/workflows/refresh-observatory.yml:5-6 `# Weekly: every Monday at 03:00 UTC` / `- cron: '0 3 * * 1'` (cron line last changed 2026-06-16); :136 `git commit -m "data: monthly refresh ($(date -u +%Y-%m-%d))"`; `git log -- data/manifest.json`: 2026-08-31, 08-24, 08-17, 08-10, 08-03, 07-28, 07-20 all 'data: monthly refresh'; refresh.yml:3-6 `# Manual-only ... its schedule was removed`; reports last-commit: 2026-06-01 contributors.md coverage.md dashboard.md timeline.md; reports/dashboard.md:3 `**Snapshot date**: 2026-06-01`; reports/README.md:129-131 `_Other files in this directory (contributors.md, coverage.md, dashboard.md, timeline.md) are generated summary tables from the render_reports.py pipeline`
- **Failure scenario:** A reader or a paper citing reports/dashboard.md headline numbers gets the 2026-06-01 snapshot while README and the commit log claim automated refresh; a maintainer auditing cadence from commit messages counts 8 'monthly' refreshes in 6 weeks and cannot tell whether the schedule is weekly by design or a misfire.
- **Proposed fix:** README:121 → 'weekly (Monday 03:00 UTC)'; commit message → `data: weekly refresh`; either add the four render_reports.py outputs to the weekly job (refresh.yml steps 39-45) or move them to reports/legacy/ with a STALE banner and date; add a freshness assertion in data_index.py --check that flags any reports/*.md older than 2 cadences.
- **Refuter votes:** [{"refuted": false, "confidence": "medium", "reason": "Verified at d2dea7a (README identical at origin/main via gh api): README.md:89 and :120 say 'monthly'; .github/workflows/refresh-observatory.yml:5-6 `# Weekly: every Monday at 03:00 UTC` / `cron: '0 3 * * 1'`; :126-138 commit message says 'data: monthly refresh'; bot commits land weekly (98b84dd 08-31, 29cd7d9 08-24, f113604 08-17, bda09c5 08-10). reports/dashboard.md:3 snapshot 2026-06-01, last touched 1c5445a 2026-06-01; render_reports.py (writes it, :269) runs only in refresh.yml which is workflow_dispatch-only (refresh.yml:1-9). Partia

### B21. regenerate-hwnorm1-sqlite: PD download failure masked (curl without -f, mv \|\| true), LS-zip step unguarded, git diff --quiet misses untracked outputs

- **Where:** [csl-orig](https://github.com/sanskrit-lexicon/csl-orig/tree/dee5a03) · [.github/workflows/regenerate-hwnorm1-sqlite.yml:85-87](https://github.com/sanskrit-lexicon/csl-orig/blob/main/.github/workflows/regenerate-hwnorm1-sqlite.yml#L85) · category `silent-failure` · severity **medium** · effort trivial · prior art: extends (csl-observatory/docs/CODEBASE_ADVERSARIAL_AUDIT_2026-08.md §8 G1 (fail-closed pipeline) / G8; P15-class git-diff pattern)
- **Claim:** The weekly cross-repo regeneration (last 6 runs green) can succeed while inputs are missing: the PD text zip is fetched with `curl -L -o` (no -f, so an HTML 404 page is saved as pdtxt.zip), `unzip` of a non-zip fails but the following `mv txt/* . 2>/dev/null || true` and set -e semantics only fail on unzip if it is on its own line — the download step itself passes; three `git diff --quiet` change gates ignore untracked new files so a first-time output is never committed; the 'Create LS links zip files' step has no HAS_CHANGES guard unlike its siblings, so zips are rebuilt and released even when nothing changed.
- **Evidence:** csl-orig .github/workflows/regenerate-hwnorm1-sqlite.yml:85@dee5a03 `curl -L -o pdtxt.zip https://www.sanskrit-lexicon.uni-koeln.de/scans/PDScan/2020/downloads/pdtxt.zip`; :87 `mv txt/* . 2>/dev/null || true`; :113,:148,:181 `if git diff --quiet`; :210 step `Create LS links zip files` has no `if: env.HAS_CHANGES == 'true'` (siblings do); :249 `gh release create "$DATE_TAG" ... -R sanskrit-lexicon/csl-sqlite $ALL_ASSETS`; :292 `git push --force origin HEAD:gh-pages`; gh run list: 2026-08-30..07-29 six successes
- **Failure scenario:** uni-koeln host returns 404/HTML for pdtxt.zip → curl exits 0 saving HTML → unzip fails only if that exact line is the command (if piped/ORed it is swallowed) → `mv ... || true` hides the empty txt/ → redo_xampp_all.sh builds PD-less sqlite → `git diff --quiet` says no change for tracked files → weekly release still cut with `--force` gh-pages push, permanently overwriting the previous good pages tree with a PD-less build.
- **Proposed fix:** `curl -fsSL --retry 3 -o pdtxt.zip …` then `unzip -q pdtxt.zip && test -s pd.txt || exit 1`; replace `git diff --quiet` with `git status --porcelain -- <paths> | grep -q . && HAS_CHANGES=true`; add the `if: env.HAS_CHANGES == 'true'` guard to the LS-zip step; drop `--force` in favour of a non-force push to a fresh orphan or keep history.
- **Refuter votes:** [{"refuted": false, "confidence": "medium", "reason": "Verified at dee5a03 and origin/main 30b2ae7 (gh api raw): :113,:148,:181 `if git diff --quiet; then` set HAS_CHANGES=false (:116) and skip every downstream push step (`if: env.HAS_CHANGES == 'true'`). `git diff --quiet` ignores untracked files, so a run that only creates a brand-new file reports 'No changes'. PRIOR-ART: audit P15 (line 90) documented the identical bug in csl-pywork v02/regenerate-hwnorm1-sqlite.sh, and PR #85 (H3638) fixed pywork at origin/main to `if [ -z \"$(git status --porcelain)\" ]` (:79,:102,:118 via gh api) — the c

### B22. csl-observatory keeps three hand-rolled `gh api` retry clients alongside scripts/_gh.py whose docstring promises a single shared implementation

- **Where:** [csl-observatory](https://github.com/sanskrit-lexicon/csl-observatory/tree/d2dea7a) · [scripts/external_reach.py:211](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/scripts/external_reach.py#L211) · category `duplication` · severity **low** · effort medium · prior art: new (none (reports/code_duplication_census.md does not mention _gh/gh_json))
- **Claim:** scripts/_gh.py provides run_gh with caching, transient detection and bounded retry 'so the caching, error handling, and transient-failure retry live in one place rather than being copy-pasted per script', yet only 2 of 20 gh-api-using scripts import it; external_reach.py, refresh_repo_licenses.py and rh1_apply_license.py each reimplement the retry loop with different transient heuristics (substring list vs 'Not Found' check vs status 404), different backoff (2**attempt vs 2*(i+1)) and different return shapes ((data,warning) vs (lic,arch,err) vs (rc,out,err)).
- **Evidence:** csl-observatory scripts/_gh.py:3-5@d2dea7a `Used by the read-only metadata/workflow snapshot scripts so the caching, error handling, and transient-failure retry live in one place rather than being copy-pasted per script.`; importers: only scripts/repo_metadata_snapshot.py, scripts/workflow_health.py; scripts/external_reach.py:211 `def _gh_json(endpoint: str, args: list[str], retries: int = 2):` with own `_TRANSIENT` and `time.sleep(2 ** attempt)`; scripts/refresh_repo_licenses.py:28 `def gh_state(repo, tries=6):` … `time.sleep(2 * (i + 1))`; scripts/rh1_apply_license.py:46 `def gh_json(args, tries=5):` … `return 2, "", f"TRANSIENT:{last[:120]}"`; 20 scripts match `api.github.com|gh api`
- **Failure scenario:** A rate-limit (403 secondary limit, not in any of the three ad-hoc transient lists) hits external_reach.py --fetch → returns (None, msg) after 0 retries and the reach report is regenerated with missing rows, while workflow_health.py using _gh.py treats the same case differently; fixing retry semantics in _gh.py leaves the three copies unpatched.
- **Proposed fix:** Route the three functions through `_gh.run_gh` (add a `jq`/extra-args parameter and a 403-rate-limit token to `_TRANSIENT`), delete the local copies, and add a scripts/ self-check to reports/code_duplication_census.py so the observatory's own scripts appear in its duplication census.
- **Refuter votes:** [{"refuted": false, "confidence": "low", "reason": "Verified at d2dea7a: scripts/_gh.py:2-6 docstring claims the helper exists so retry/caching 'live in one place rather than being copy-pasted per script'; only repo_metadata_snapshot.py and workflow_health.py import it; 13 scripts spawn gh directly, three with their own retry loops (external_reach.py `_gh_json(endpoint, args, retries=2)` ~:211; refresh_repo_licenses.py `gh_state(repo, tries=6)` :28; rh1_apply_license.py `gh_json(args, tries=5)` :46). No prior art: reports/code_duplication_census.md, docs/*.md, CHANGELOG.md contain no `_gh.py\|

## 5. Critic corrections — read before launching anything below

The completeness critic (last agent, same model) re-read the cited lines at **origin** tips and disagreed with the synthesis on these points. Where it conflicts with §4 or §6, **§5 wins**.

### 5.1 Ranking objections

1. Rank 3 (csl-pywork generate_dict.sh unchecked redo_xml/redo_postxml) is already fixed at origin/main aefbdac: generate_dict.sh:142-143 `output=$(sh redo_xml.sh 2>&1)` / `redo_xml_status=$?`, :164 fail_exit, :166-168 `sh redo_postxml.sh` + fail_exit. Local e460545 :122/:143 evidence is stale (3 commits behind). Demote to a quick win scoped to the surviving duplicate pass: redo_postxml.sh runs twice (makotemplates/pywork/redo_xml.sh:28 at tip / :21 local, plus generate_dict.sh:166). Map's path 'v02/redo_xml.sh:21' does not exist; real path v02/makotemplates/pywork/redo_xml.sh.
2. Rank 4 (csl-orig check_generate_dict.sh 'cannot fail'): overstated. At origin/main 30b2ae7 :89 `|| true` is correct, but :92-100 grep red ANSI lines → `overall_exit=1` and :107 `exit $overall_exit`, so the script fails on colored STOP output (which tip redo_xml.sh:23 now emits). The real defect is narrower: a non-red, non-zero exit is swallowed. PR #2893 (OPEN, CONFLICTING since 2026-08-28T16:45:20Z) conflicts because #2894 added +20 lines to the same script — the mission should be 'rebase #2893' not 'write the gate'.
3. Rank 1 (csl-corrections PII) mission masks daily/ only; cfr_ab/cfr_ab.tsv (2,433 occurrences) is regenerated by scripts/update_cfr_ab.py and committed at workflow :33 every night, and the public issue at :40-58 carries `user=` derived from raw email (cfr_adj.py:69-76). Acceptance must include those two sinks or the nightly leak continues. Human_decision #1 quotes '1,148 addresses (203 daily / 945 older)' — measured: 232 unique (20 in daily/), 34,757 occurrences (e363439).
4. Rank 5 (csl-inflect SQL injection): confirmed, but dal.php line is :82 (`$result = $this->file_db->query($sql);`), not :83; add :111/:178/:409 as further unparameterised query sites (14cd802).
5. Rank 7 (apidev/websan transcoder drift): the map's slp1_roman.xml/roman_slp1.xml drift claim is FALSE — both byte-identical (3270 B / 2388 B). Real drift: as_roman.xml, deva_slp1.xml, slp1_deva.xml + 4 apidev-only tables; re-scope evidence accordingly (csl-apidev e5ed207 vs csl-websanlexicon 2750327).
6. Rank 9 (csl-inflect transcoder.php silent return): line is :41 `if (!file_exists($filein)) {return;}` not :40 (14cd802).
7. Rank 2 (cologne-stardict dictdata gap): confirmed at 0590b83 but acceptance counts etymology_stats as a missing dictionary — it is a directory of CSVs/scripts in csl-orig v02, not a dictionary; the gap is nybj + pwkvn only.
8. Refutation 'csl-atlas L and x both → ḷ (prior-art)' is WRONG: csl-atlas 3f9fe64 scripts/build-stardict-export.mjs:30 `x: "ḷ"` and :34 `L: "ḷ"` collapse vocalic ḷ and retroflex lateral ḷ into one glyph in the shipped StarDict export (package.json:58 wires it); CDSL convention per MWS#169 rule `<in>L</in><out>ḻ</out>` says L → ḻ. Prior art documenting the bug does not fix it — it belongs in the ranked list as a correctness item.
9. Refutation 'update_user_corrections.sh unconditional push — unreferenced' is weak: csl-corrections readme_howto.txt:121,:131-134 document it as the operator procedure (`sh update_user_corrections.sh ae` etc.), so it is human-run, not dead; keep as a quick win (add a diff check before push).
10. Refutation 'csl-json redo.sh python2' rests on a prior-art doc only; csl-json c824e1e redo.sh:16 `python2 make_babylon.py $Val 1`, :24 `python2 json_from_babylon.py $Val` still call an interpreter absent on modern hosts — it is a live defect unless the repo is declared retired; fold into COLOGNE #484's port-or-retire policy.
11. Refutation 'cologne-stardict indic_transliteration two scheme maps' undercounts: utils.py:10-11 build slp1_accented→deva and iast→deva, make_babylon_synonymic.py:23 builds a third slp1→deva; three copies of the same SchemeMap construction, still low materiality.
12. Human_decisions: #1 (filter-repo purge), #3 (apidev history rewrite), #6 (CDN adoption) are real irreversible human forks. #2 is mostly a fact question — COLOGNE iast/slp1_roman.xml is byte-identical to csl-apidev utilities/transcoder/svarita_circumflex/slp1_roman.xml (5431 B), so the fork reduces to 'promote svarita_circumflex to top-level or not'. #4 (lookup/ vs sample/dalglob1.php) and #7 (keydoc fast path behind devFlag at dal.php websan :93 / apidev :91) are agent-verifiable first (grep callers, run both paths) and should not be presented as human forks until an agent shows behavioural difference. #5 (issue445 serve_api.py) is not covered by #484 (body lacks issue445/serve_api) — a real but small fork. #8 (who runs the Cologne-server dry-run) is an assignment, not a decision — move to GTD @DO.
13. Map's org scope '77 repos' is wrong: 86 per `gh repo list sanskrit-lexicon --limit 100` (4 archived); the ranking is drawn from ~15 repos and should say so in the summary.

### 5.2 Gaps the map does not cover (each is a follow-up agent task)

- **cfr_ab/cfr_ab.tsv (2,433 raw email occurrences, header col 'Your Email Address') is rewritten and committed nightly, and the nightly public issue body carries email-derived user= — rank-1 mission masks only daily/**
  - Why: csl-corrections e363439 .github/workflows/fetch-daily-corrections-from-cologne.yml:27 `python scripts/update_cfr_ab.py`, :33 `git add daily/${{...}}/ cfr_ab/cfr_ab.tsv`, :40-58 creates public issue 'Daily Corrections - `<date>`'; cfr_adj.py:69-76 derives `user=` from raw email. Masking daily/ alone leaves the largest live sink untouched; the map's count '1,148 addresses (203 daily / 945 older)' is unsupported — measured 232 unique addresses (20 in daily/), 34,757 occurrences in tracked tsv (1,379 daily, 2,433 cfr_ab.tsv, 33,378 non-daily incl app/correction_response/2020/*.tsv).
  - How to close: Agent task (csl-corrections, read-only first): enumerate every tracked file with an email column (`git ls-files '*.tsv' | xargs grep -lE '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+'`), list every writer (update_cfr_ab.py, cfr_adj.py, workflow :33/:40-58, app/correction_form_response.php:28-32) and extend rank 1's mission + acceptance to: mask at ingest (correction_form_response.php), mask in update_cfr_ab.py, strip user= from the issue body; re-count unique addresses from git output and replace the 1,148 figure in human_decision #1.
- **Rank 3 (csl-pywork generate_dict.sh unchecked redo_xml/redo_postxml) is ALREADY FIXED at origin/main aefbdac (PR #84); local clone e460545 is 3 commits behind. Only the duplicate redo_postxml pass survives at tip**
  - Why: origin/main:v02/generate_dict.sh :142-143 `output=$(sh redo_xml.sh 2>&1)` / `redo_xml_status=$?`, :164 fail_exit on non-zero, :166-168 `sh redo_postxml.sh` gated by fail_exit. But origin/main:v02/makotemplates/pywork/redo_xml.sh:28 `sh redo_postxml.sh` (:30-32 exits 1 on failure) AND generate_dict.sh:166 runs redo_postxml.sh again — the duplicate pass the map attributes to 'v02/redo_xml.sh:21' (wrong path; real file v02/makotemplates/pywork/redo_xml.sh, :21 at local e460545, :28 at tip; origin inventory.txt:6 comment '; redo_xml.sh calls redo_postxml.sh' confirms intent). Same stale-clone hazard for csl-websanlexicon (2750327 vs 02b45aa, +3, on branch h3636-getword-data-twin), csl-apidev (e5ed207 vs 58b54a0, +4: 0.4.1/0.4.2, listhierClass.php, basicdisplay.php, dictinfo.php), csl-orig (dee5a03 vs 30b2ae7, +4: #2894 +20 lines to check_generate_dict.sh, #2895), csl-pyutil (c66ce13 vs 1e5e0b3, v0.23.0 — map says v0.22.0).
  - How to close: Agent task: for every ranked item touching csl-pywork/csl-websanlexicon/csl-apidev/csl-orig/csl-pyutil, re-anchor line numbers with `git show origin/<default>:<path>` (no checkout) and drop rank 3's 'unchecked' claim, re-scoping it to 'redo_postxml.sh runs twice per dict (redo_xml.sh:28 + generate_dict.sh:166) — remove one call, then verify with `git show origin/main:v02/makotemplates/pywork/redo_xml.sh`'.
- **Zero finder coverage of the org's largest code surfaces: PWG (840 py/62 js/35 sh), PWK (415 py), MWS (379 py), AP (125 py), GRA (101 py/29 php), WIL (58 py/37 php), MWinflect (52 py/5 php/18 sh), mw-dev (29 py/62 php/36 sh), hwnorm2 (33 py/19 sh), CORRECTIONS (93 py/5 php/21 sh), csl-westergaard (12 php/127 js), csl-whitroot (2 php/125 js), csl-santam (Perl), csl-kale, csl-lslink, csl-guides, csl-doc, csl-devanagari, csl-newsletter, cologne-skills, sanskrit-lexicon.github.io, alternateheadwords, BEN/BUR/MD/VCP/SKD/BHS/BOP**
  - Why: Org is 86 repos (`gh repo list sanskrit-lexicon --limit 100`; task said 77; 4 archived: santamlegacy, temp_corrections_acc, temp_corrections_ae, test_cologne_push; forks: csl-homepage, csl-websanlexicon). tests/ dirs exist only in csl-app (17), csl-atlas (24), csl-pyutil (31), csl-pywork (3), csl-standards (9). Workflows at local HEAD: PWG 71ea4fe ci.yml+dependabot; PWK f92d49f / MWS 0018775 / AP 0ad2895 / GRA 9bfc177 ci+codeql+semgrep+dependabot; WIL aa14f92, MWinflect 8ef6fc6, hwnorm2 7bb33a9, CORRECTIONS 1e04b23, csl-westergaard bca7860, csl-whitroot 4fbf30d, csl-kale eff3bb6, csl-lslink 65c7d08: dependabot-auto-merge.yml only; mw-dev 6fb10b1 + readme-guard.yml; csl-santam 17858dc + semgrep.yml. The map's clusters cannot claim org-wide code-quality ranking when >2,000 py files in five dictionary-work repos were never opened.
  - How to close: Agent task, one repo per pass, read-only: for PWG, PWK, MWS, AP, GRA, WIL, MWinflect, mw-dev, hwnorm2, CORRECTIONS run the same fail-closed grep set used on csl-pywork (`set -e` absence in *.sh, `$(... 2>&1)` with exit discarded, `|| true`, xrange/print-statement py2 residue, `curl` without -f, bare `git diff --quiet`) scoped with `git -C <repo> grep -n`, and report counts per repo with file:line; add a cluster 'dictionary-work repos' with its own ranked items.
- **Transliteration-table drift audited only for four PHP twins; XML transcoder tables drift between csl-apidev and csl-websanlexicon and are outside twin-drift.yml**
  - Why: csl-apidev e5ed207 .github/workflows/twin-drift.yml:51-56 twins = basicadjust.php, basicdisplay.php, getword_data.php, utilities/transcoder.php only. Byte-compare of top-level utilities/transcoder/*.xml (apidev 20 files) vs csl-websanlexicon 2750327 v02/makotemplates/web/utilities/transcoder/ (16 files): DIFF as_roman.xml (22935 vs 22829 B), deva_slp1.xml (10102 vs 10277 B), slp1_deva.xml (11112 vs 11305 B); apidev-only as_romanorig.xml, beng_slp1.xml, cyrillic_slp1.xml, velthuis_slp1.xml; slp1_roman.xml (3270 B) and roman_slp1.xml (2388 B) IDENTICAL. Finder note admits csl-inflect XML vs pywork tables never diffed; csl-inflect 14cd802 web/utilities/transcoder.php:41 `if (!file_exists($filein)) {return;}` returns silently on a missing table.
  - How to close: Agent task: extend twin-drift.yml's list (or add a second job) to diff utilities/transcoder/*.xml between csl-apidev and csl-websanlexicon, and produce a three-way diff (apidev / websan / csl-inflect web/utilities/transcoder/) of deva_slp1.xml, slp1_deva.xml, as_roman.xml with the exact `<entry>` rows that differ, so a human can pick a canonical copy per file.
- **csl-observatory has two refresh workflows and README/cron/commit-message disagree on cadence**
  - Why: csl-observatory d2dea7a: .github/workflows/refresh.yml:1 'Refresh data snapshot (legacy pipeline)', :6 comment 'was removed to avoid two crons racing to commit data/' (cron removed, workflow file kept); refresh-observatory.yml:6 `cron: '0 3 * * 1'` (weekly), :137 commit msg 'data: monthly refresh'; README.md:89 'Monthly auto-refresh (template; needs workflow token scope to push)', :120 'Auto: monthly'. Recent runs 09-01 all success. Map lists the doc-drift quick win but not the dead legacy workflow.
  - How to close: Agent task: delete or archive refresh.yml (no cron, superseded), align README.md:89/:120 and the :137 commit message to 'weekly (Mon 03:00 UTC)', and add a lint asserting one refresh cron per repo.
- **cologne-stardict has no CI at all and its dictionary registry lags csl-orig by two real dictionaries**
  - Why: cologne-stardict 0590b83: only workflow dependabot-auto-merge.yml; dictdata.py has 43 keys vs 46 v02 dirs in csl-orig dee5a03 — missing nybj, pwkvn (real dicts) and etymology_stats (a non-dictionary dir of CSVs/scripts — should be excluded, not added); make_babylon.py:291 `fout.write('\n#bookname=' + dictdata[dictId][0] + ...` raises KeyError for unknown dictId; move_to_stardict.sh lacks inm krm mci pe pui. Rank 2 is confirmed but its acceptance should not count etymology_stats as a missing dictionary.
  - How to close: Agent task: add a ci.yml to cologne-stardict that runs `python -c` style assertion (in a test file) that every csl-orig v02/`<dict>`/ with a `<dict>`.txt source has a dictdata.py entry and a move_to_stardict.sh line, with an explicit allowlist for etymology_stats; add nybj and pwkvn to dictdata.py + move_to_stardict.sh.
- **csl-orig update-stardict.yml uses a mutable major-tag third-party action with a PAT that can push to two external repos, and a malformed committer email**
  - Why: csl-orig dee5a03 .github/workflows/update-stardict.yml: `tj-actions/changed-files@v47` (mutable tag; this action had a supply-chain compromise in 2025), PAT_TOKEN used for two external checkouts, `git config --global user.email "the committer e-mail (its `@` is missing)"` (missing @), CHANGED_DICTS from `grep '^v02/'`; runs red 08-11/08-13. The map's quick_wins mention pinning but no ranked item scopes the PAT blast radius.
  - How to close: Agent task: pin tj-actions/changed-files to a full commit SHA, restrict PAT_TOKEN to a fine-grained token scoped to the two target repos (or replace with a GitHub App), fix the committer email, and add `git diff --stat` output to the job summary so red runs show cause.

### 5.3 Unverified claims

- csl-pywork coverage gate 'measures ~2 statements': .coveragerc omits makotemplates/**, distinctfiles/**, utilities/**, generate.py, redo_xampp_selective.py and tests/ = test_dictparms.py, test_redo_xampp_selective.py, test_updateByLine.py (subprocess) at e460545 — plausible but never measured; verify by copying v02/dictparms.py + tests/ + .coveragerc to scratchpad and running `coverage run -m pytest && coverage report` there (not in the clone).
- Map cites csl-pywork v02/json_from_babylon.py:44 — file does not exist in csl-pywork e460545; csl-json c824e1e has json_from_babylon.py (git ls-files line 46) and redo.sh:24 `python2 json_from_babylon.py $Val`, :16 `python2 make_babylon.py $Val 1`. The :44 claim is unverified against csl-json.
- Rank-4 line ':89' is CORRECT at origin/main 30b2ae7 (scripts/check_generate_dict.sh:89 `pipeline_output=$(cd "$PYWORK_V02" && sh generate_dict.sh "$dict" "$outdir_rel" 2>&1) || true`, 107 lines) and :69 at local dee5a03 (87 lines) — my earlier 'wrong line' objection is withdrawn; the map was reading tip. Whether PR #2893's `pipeline_status=$?` gate still applies cleanly after #2894's +20 lines is unverified (PR shows CONFLICTING, updated 2026-08-28T16:45:20Z).
- csl-corrections nightly failure cause: gh run list shows failure 09-02, 09-01, 08-30, 08-29 and success 08-31, 08-28 — logs not read; fetch_yesterday_cfr.sh:3 `set -e` + :18 `if ! curl -o "$OUTPUT" "$URL"; then` (no -f, so HTTP 404/500 bodies are saved as data) is a plausible but unconfirmed cause.
- Human_decision #2 canonical slp1_roman.xml: COLOGNE ece273d iast/slp1_roman.xml is byte-IDENTICAL (cmp, 5431 B) to csl-apidev e5ed207 utilities/transcoder/svarita_circumflex/slp1_roman.xml; the 3270 B websan/apidev top-level copy is a different table. Whether the svarita_circumflex variant is the intended production table (vs an experiment) is unverified — the fork is smaller than stated: 'promote svarita_circumflex/ to top-level or not', not 'COLOGNE vs apidev'.
- COLOGNE ece273d iast/transcoder.py:45-47 points transcoder_dir at `<parent>`/data/transcoder which has 0 tracked files (ARCHITECTURE_REVIEW.md:62-63 already records it) — whether any caller still invokes iast/transcoder.py is unverified (no grep of importers done).
- csl-inflect analyze_1cons missing ../../transcode: refuted on materiality; not re-checked whether any redo.sh in verbs/ still invokes it.
- Semgrep/CodeQL findings for PWK, MWS, AP, GRA, csl-santam (semgrep.yml/codeql.yml present at local HEAD): never read via `gh api /repos/.../code-scanning/alerts` — could quantify open alerts in the uncovered repos without opening any file.
- COLOGNE issue #484 (OPEN, 'cleanup: Decide port-or-retire policy for Python 2 and legacy scripts') body does NOT mention issue445 or serve_api (gh --json body test = false) — human_decision #5 is not already decided by #484; whether another issue covers issues/issue445/serve_api.py is unverified.
- hwnorm1 4a80a35 hwnorm1.py:250,:261,:281 use Python-2 print statements and is invoked from hwnorm1.sh and dhaval/redo.sh — whether any live workflow (csl-orig regenerate-hwnorm1-sqlite.yml) calls hwnorm1.py or only hwnorm1c is unverified.

### 5.4 Coverage by area

| Area | Covered | Note |
|---|---|---|
| csl-corrections nightly pipeline + PII | yes | e363439: workflow :5/:27/:33/:40-58, correction_form_response.php:2/:6-7/:28-32, cfr_adj.py:69-76, fetch_yesterday_cfr.sh:3/:18 verified; email counts in the map are wrong (232 unique / 34,757 occurrences); cfr_ab.tsv and public-issue leak uncovered by rank 1. |
| csl-pywork generation pipeline | yes | Verified at e460545 and origin/main aefbdac; rank 3 already fixed at tip (PR #84), duplicate redo_postxml pass remains (redo_xml.sh:28 + generate_dict.sh:166); ci.yml:5 header vs pytest --cov contradiction confirmed; coverage '2 statements' not measured. |
| csl-orig workflows (update-stardict, regenerate-hwnorm1-sqlite, check_generate_dict.sh) | yes | dee5a03 + origin/main 30b2ae7; :89 `\|\| true` correct at tip; script CAN exit 1 on red lines (:83/:100/:107) so 'cannot fail' is overstated; regenerate-hwnorm1-sqlite.yml `git diff --quiet` :113/:148/:181, apidev commit steps dead (csl-apidev/.gitignore:21-22). |
| cologne-stardict | yes | 0590b83: make_babylon.py:291 KeyError, dictdata 43 vs 46 dirs (nybj, pwkvn real; etymology_stats not a dict), move_to_stardict.sh gaps, no CI; utils.py:10-11 two SchemeMaps vs make_babylon_synonymic.py:23 third map — refutation 'two scheme maps' is wrong in count (three) but right in materiality. |
| csl-inflect web/PHP (SQL injection, transcoder) | yes | 14cd802: getWord.php:15-24/:139 string-interpolated SQL, dal.php:82/:111/:178/:409 ->query, zero prepare(); transcoder.php:41 silent return; pft/redo.sh copy of fut/redo.sh; main.js DEVA_TO_SLP1 :35-55 lacks digits/candrabindu. XML tables vs pywork never diffed. |
| csl-websanlexicon / csl-apidev PHP twins + transcoder XML | yes | twin-drift.yml:30-56 covers 4 PHP files only; XML drift measured (as_roman, deva_slp1, slp1_deva differ; 4 apidev-only; slp1_roman/roman_slp1 identical); local clones 3-4 commits behind origin, websan on a feature branch. |
| COLOGNE monorepo (iast/, stardict/, enhancements/, issues/) | yes | ece273d: iast/transcoder.py:45-47 dead data dir, stardict/transcoder.py:169 xrange, 6 py2 files of 128; issues/ bodies (issue445 serve_api.py) not read; #484 does not cover issue445. |
| csl-observatory | yes | d2dea7a: cadence drift README:89/:120 vs refresh-observatory.yml:6/:137; legacy refresh.yml kept without cron (:1/:6); script bodies not audited. |
| csl-pyutil / csl-json / hwnorm1 / sanskrit-util / csl-sqlite / csl-app / csl-atlas / csl-standards | **no** | Only spot checks: csl-json redo.sh:16/:24 python2 (c824e1e); hwnorm1.py:250/:261/:281 py2 print (4a80a35); csl-atlas build-stardict-export.mjs:30-36 L and x both → ḷ (3f9fe64, wired via package.json:58); csl-pyutil local c66ce13 is 2 behind origin v0.23.0; csl-app Dart, csl-sqlite, sanskrit-util bodies not audited. |
| Dictionary-work repos PWG, PWK, MWS, AP, GRA, WIL, MWinflect, mw-dev, hwnorm2, CORRECTIONS | **no** | >2,000 py + ~130 php + ~130 sh files with no finder pass; PWK/MWS/AP/GRA have ci+codeql+semgrep workflows whose alert lists were never read; WIL/MWinflect/hwnorm2/CORRECTIONS have dependabot-auto-merge.yml only. |
| JS-heavy repos csl-westergaard, csl-whitroot (~125 js each) and Perl csl-santam | **no** | dependabot-only workflows (csl-santam + semgrep.yml); no finder opened any file. |
| Docs/site repos csl-kale, csl-guides, csl-doc, csl-devanagari, csl-newsletter, cologne-skills, sanskrit-lexicon.github.io, csl-lslink, alternateheadwords, BEN/BUR/MD/VCP/SKD/BHS/BOP | **no** | Inventoried by workflow filename only (dependabot-auto-merge.yml); low code-quality weight but not zero (site build scripts). |
| Org inventory | yes | 86 repos via gh (task said 77); 4 archived; 2 forks; every local clone has ≥1 workflow (mostly dependabot-auto-merge.yml). |
| Secrets / dependency vulnerabilities | **no** | Finder secrets grep over 13 repos zero hits; semgrep not installed locally; no `gh api` read of dependabot/code-scanning alerts on any repo. |
| Origin freshness of evidence | **no** | No fetch permitted; csl-pywork, csl-websanlexicon, csl-apidev, csl-orig, csl-pyutil local HEADs are 2-4 commits behind origin — ranked line numbers were partially re-anchored via `git show origin/main:` for csl-orig and csl-pywork only. |

## 6. Ranked roadmap — 19 launchable items

Ordering rule: blast radius across all 46 dictionaries first, then user-facing security and privacy, then build-path correctness, then hygiene. Effort lights: 🟢1 trivial (<15 min) · 🟡2 medium (15 min–2 h) · 🔴3 hard (2 h+). Tier is the synthesiser's suggestion. **Apply §5 before minting**: rank 3 is demoted to a quick win, rank 1 must also cover `cfr_ab/cfr_ab.tsv` and the nightly issue body, rank 4 is "rebase #2893", rank 7 is re-scoped to `as_roman.xml` / `deva_slp1.xml` / `slp1_deva.xml` + four apidev-only tables.

### 6.1 Overview

| # | Item | Repos | Effort | Tier | Depends on |
|---|---|---|---|---|---|
| 1 | csl-corrections: stop committing correction-form emails, make the intake fail loud, fix the nightly fetch | csl-corrections | 🟡2 medium | Opus 5 | none (history purge is a separate human decision, see human_decisions[0]) |
| 2 | csl-orig update-stardict.yml: register nybj/pwkvn/etymology_stats or skip non-dictionary dirs, SHA-pin tj-actions, add permissions block, fix bot email | csl-orig, cologne-stardict | 🟡2 medium | Sonnet 5 | none |
| 3 | csl-pywork: finish fail-closed (redo_postxml.sh, sqlite/redo.sh, downloads/redo_all.sh) and delete the duplicate redo_postxml pass | csl-pywork | 🟡2 medium | Sonnet 5 | none; must land before rank 6 so the golden test's exit code is meaningful |
| 4 | csl-orig: rebase PR #2893 onto the #2894 hook rewrite and remove `\|\| true` from check_generate_dict.sh:89 | csl-orig, Uprava | 🟡2 medium | Sonnet 5 | G2 (done); rank 3 (same fail-closed semantics on the pywork side) |
| 5 | csl-inflect: prepared statements in web/dal.php for getWord.php and every query built from $_GET | csl-inflect | 🟡2 medium | Sonnet 5 | none |
| 6 | csl-pywork: Mako-render fixture + golden-master test on acsj, and an honest coverage gate | csl-pywork | 🔴3 hard | Opus 5 | rank 3 (sub-stage exit codes must be real for a golden run to fail honestly) |
| 7 | Extend twin-drift to utilities/transcoder/*.xml and apidev_copy.sh, then resync apidev's transliteration tables from websanlexicon | csl-websanlexicon, csl-apidev | 🟡2 medium | Sonnet 5 | G4 (done); G5 (done, transcoder fails loud on both sides) |
| 8 | csl-inflect: fail-closed regen chain, correct pft/redo.sh, python3 + lxml requirement, and a minimal CI | csl-inflect | 🟡2 medium | Sonnet 5 | none |
| 9 | csl-inflect web: port the fail-loud transcoder and close the Devanagari coverage gaps in main.js | csl-inflect | 🟡2 medium | Sonnet 5 | G5 (done — copy websan transcoder.php:40-44,189,195,348-353 pattern); rank 5 (same files, avoid conflicting PRs) |
| 10 | csl-orig regenerate-hwnorm1-sqlite.yml: detect untracked outputs (P15 twin) and remove dead csl-apidev move/commit steps | csl-orig | 🟢1 trivial | Grok 4.5 | none |
| 11 | REFRESH follow-through: lockfile for redo_xampp_selective.py, fix json_from_babylon.py escapes, correct the plan header | csl-pywork | 🟡2 medium | Sonnet 5 | rank 3 (the refresh must call the fail-closed generate_dict.sh) |
| 12 | COLOGNE correctness slice: transcoder path bug, redo_one.sh hard-coded paths, https curl, xmltag regex, shellcheck in CI | COLOGNE | 🟡2 medium | Sonnet 5 | none (slp1_roman.xml canonical choice is human_decisions[1]; do not touch iast/slp1_roman.xml here) |
| 13 | COLOGNE packaging and tests: pyproject with declared deps, fixtures/mini_dict.txt, port-or-retire the four Python 2 files | COLOGNE | 🔴3 hard | Opus 5 | rank 12 (same files; sequence to avoid conflicts); rank 6 pattern (fixture-driven golden test) is the template to copy |
| 14 | Shared text utilities: extract levenshtein/util_mw/util_dump_lines/unixify/compare into csl-pyutil textutil and repoint the copies | csl-pyutil, csl-pywork, COLOGNE, csl-orig (scripts only) | 🔴3 hard | Opus 5 | rank 6 (golden master) because csl-pywork copies sit on the build path; rank 13 (COLOGNE packaging) so COLOGNE can declare csl-pyutil as a dependency |
| 15 | Transliteration delegation: finish SH1/H3488 — five queued sites onto sanskrit-util plus the SAST gap list | BookIndex, csl-observatory, SamudraManthanam, RussianRamayana, IndologyScholars, sanskrit-util | 🟡2 medium | Sonnet 5 | H3488 claim state (release or finish); none technically |
| 16 | csl-corrections reporting: CO357 KeyError 'pwg', freshness stamps on SVGs, CI wiring for build_correction_loci.py | csl-corrections | 🟡2 medium | Sonnet 5 | rank 1 (same repo; avoid conflicting PRs) |
| 17 | csl-observatory: cadence docs match the weekly cron, dashboard snapshot refreshed on schedule, one _gh.py retry helper | csl-observatory | 🟡2 medium | Sonnet 5 | none |
| 18 | Cross-repo CI hygiene sweep: SHA-pin actions, add CI where none exists, .gitattributes, case-collision guard | csl-websanlexicon, csl-apidev, csl-inflect, cologne-stardict, csl-app, COLOGNE, csl-pywork, csl-pyutil | 🟡2 medium | Grok 4.5 | rank 2 (establishes the SHA-pin pattern); rank 8 (csl-inflect CI) |
| 19 | Uprava registry and docs hygiene for the Cologne lane: archive H3485, fill H3851, fix H3631 line, refresh census and stale .claim files | Uprava, csl-pywork (docs) | 🟢1 trivial | Grok 4.5 | rank 2 (H3851 content) and rank 4 (H3631 correction) |

### 6.2 Clusters

- **Fail-closed pipeline and workflows** — Every dictionary is rebuilt through generate_dict.sh → redo_xml.sh → redo_postxml.sh and published through csl-orig Actions; any swallowed exit or unregistered dictionary silently ships stale or missing output for all of them. First wave (G1/G2/G8/G11) landed; the residue is sub-stage exit codes, the duplicate postxml pass, the red stardict job, and the conflicting #2893 rebase. Items: G1-residual, B10, B17, B18, B21, B5, B3, REFRESH, PR2893, P9, P12, P13, HO3851
- **Tests on the real pipeline** — csl-pywork's coverage gate measures 2 statements because Mako templates are omitted and no render fixture exists; a golden-master run on one small dictionary (acsj) is the precondition for every make_xml.py / shared-package refactor further down the map. Items: B9, B11, B12, AR6, CL482
- **Intake and web-app security and privacy** — csl-corrections auto-commits user email addresses to a public repo nightly and silently loses submissions; csl-inflect concatenates $_GET into SQL; parm.php TypeErrors on PHP 8. These are the only user-data and injection surfaces in the estate. Items: B13, B14, B2, B3, B1, B15, B8
- **PHP twin, transliteration and API hygiene** — websan/apidev twins drift where twin-drift.yml does not look (XML tables), COLOGNE declares a stale slp1_roman.xml canonical, and three apidev issues (#16, #44, #153) wait on human or maintainer action. Guard rail: websanlexicon first, then apidev_copy.sh. Items: B7, B19, D6, AP16, AP44, G6-close, G9-dead-code, A18
- **Shared-package extraction and Python 2 retirement** — levenshtein.py (10 copies/3 versions), util_mw.py, util_dump_lines.py, unixify.py, compare.py and 12 transliteration sites are duplicated across repos; COLOGNE still holds 4 py2 files that fail py_compile. Extraction must wait for the golden-master test. Items: CE1, SH1, HO3488, AR1, CL483, AR2, CL485, AR4, AR5, AR8, AR9
- **Repo hygiene and CI** — Mutable action tags in PAT-holding jobs, absent CI in csl-inflect and cologne-stardict, missing .gitattributes normalisation, no case-collision guard, untracked 26 MB blobs, generated files without markers, and three Uprava registry rows that no longer match reality. Items: B18, PW52, PW74, CL480, CL481, CL487, AR7, CL486, CO357, CO358, B22, HO3485, H3631-archive-line
- **DX and docs** — Docs contradict code in five places (pywork ci.yml header, inflect PIPELINE_MANUAL no-deps claim, observatory monthly vs weekly cron, REFRESH plan header, census digentry line); each misleads the next agent for minutes to hours. Items: B6, B12, B20, CE3, CE2, B16, csl-orig-shallow-clone

### 6.3 Missions and acceptance (launch specs)

#### Rank 1 — csl-corrections: stop committing correction-form emails, make the intake fail loud, fix the nightly fetch (🟡2 medium, Opus 5)

- **Why now:** A public repo receives a github-actions[bot] commit every night containing entry_email values (247 daily/`<YYYYMMDD>`/cfr-*.tsv, latest 20260830; 203 emails in daily/, 945 in older TSVs). The same intake silently drops submissions (unchecked fopen/fwrite, iframe always shows thank-you) and the fetch job saved 404 HTML because curl lacked -f — 4 of the last 6 scheduled runs are red (02-09, 01-09, 30-08, 29-08).
- **Depends on:** none (history purge is a separate human decision, see human_decisions[0])
- **Evidence:** B13 csl-corrections@e363439 app/correction_form_response.php:6-7,28-32; B14 app/correction_form_response.php:2,8-19; B2 app/correction_form.php:111-116; B3 fetch_yesterday_cfr.sh:18 `if ! curl -o "$OUTPUT" "$URL"; then`; run 33582426561; gh run list fetch-daily-corrections-from-cologne.yml: failure 2026-09-02T02:13Z, 2026-09-01T02:53Z, 2026-08-30, 2026-08-29; git ls-files daily/**/*.tsv = 247
- **Mission:** One PR against csl-corrections (agent opens the PR, never closes issues). (1) In fetch_yesterday_cfr.sh change the curl call to `curl -fsS --retry 3 -o "$OUTPUT" "$URL"`, treat HTTP 404 (no corrections yesterday) as a clean exit 0 with a log line, and have cfr_adj.py refuse to write when the tab-part count is not 8. (2) Mask or drop the email column before anything is committed: in cfr_adj.py replace entry_email with a stable salted hash (or drop the column) so daily/ TSVs never carry addresses; keep the raw column only on the Cologne server side. (3) In app/correction_form_response.php wrap fopen/fwrite in checks with flock(LOCK_EX), return HTTP 500 plus a plain error body on failure, cap each field with mb_substr (e.g. 2000 chars) after the existing preg_replace, and replace `Access-Control-Allow-Origin: *` with the sanskrit-lexicon.uni-koeln.de origin. (4) In app/correction_form.php show the thank-you only when the iframe body reports success (postMessage or a status token), otherwise show a retry message. Do not rewrite git history in this PR; do not touch csl-orig v02 data. Note the Cologne deploy step for app/ in the PR body — deployment to the Cologne server is a maintainer act.
- **Acceptance:** `gh run list --workflow fetch-daily-corrections-from-cologne.yml --limit 3` shows green after merge; `git grep -c '@' -- daily/$(date +%Y%m%d)*/` returns 0 on the first post-merge daily commit; a local PHP 8 run of correction_form_response.php with an unwritable target file returns HTTP 500 (php -S smoke test in PR); `shellcheck fetch_yesterday_cfr.sh` clean.

#### Rank 2 — csl-orig update-stardict.yml: register nybj/pwkvn/etymology_stats or skip non-dictionary dirs, SHA-pin tj-actions, add permissions block, fix bot email (🟡2 medium, Sonnet 5)

- **Why now:** The workflow has been red since 2026-08-11 (failures 08-11, 08-13 x2; last green 2026-08-04 b7297b9) because make_babylon.py:291 `dictdata[dictId][0]` raises KeyError for 'nybj'; meanwhile the same job runs mutable tj-actions/changed-files@v47 with secrets.PAT_TOKEN pushing to cologne-stardict main and indic-dict/stardict-sanskrit master with no permissions block. This is the root cause H3851 was claimed for but never filled.
- **Depends on:** none
- **Evidence:** B17 csl-orig@dee5a03 .github/workflows/update-stardict.yml:87,92-101; cologne-stardict@0590b83 make_babylon.py:291; dictdata.py lacks nybj, pwkvn, etymology_stats; runs 31467700092, 31668264887, 31668532366; B18 update-stardict.yml:28 `uses: tj-actions/changed-files@v47`, :41,:51 PAT_TOKEN, :111-114, :124-127; C update-stardict.yml:68 `git config --global user.email "the committer e-mail (its `@` is missing)"`; B17 dictdata keys inm krm mci pe pui absent from move_to_stardict.sh; HO3851 template unfilled; gh run list csl-orig 'Generate and Sync Stardict Files': failure 2026-08-13 x2, 2026-08-11; success 2026-08-04
- **Mission:** Two PRs. (a) cologne-stardict: add nybj and pwkvn rows to dictdata.py (copy title/author fields from csl-orig v02/`<dict>`/README or the CDSL dictionary list), make make_babylon.py exit 2 with a clear message for an unknown dictId instead of KeyError, and either add inm/krm/mci/pe/pui to move_to_stardict.sh or document why they are excluded. (b) csl-orig: in update-stardict.yml filter CHANGED_DICTS to directories that contain `<dict>`.txt AND are listed in dictdata.py (or a maintained allowlist), so etymology_stats and similar non-dictionary dirs are skipped; replace `tj-actions/changed-files@v47` with the full 40-char commit SHA (comment the tag) or with `git diff --name-only ${{ github.event.before }} ${{ github.sha }}`; add a top-level `permissions: contents: read` and grant `contents: write` only to the push step; fix :68 to `[e-mail redacted]`. Never commit v02 dictionary source; touch only .github/ and scripts. Fill H3851 in Uprava with this root cause and link both PRs.
- **Acceptance:** `gh run list --repo sanskrit-lexicon/csl-orig --workflow 'Generate and Sync Stardict Files' --limit 1` shows success on a workflow_dispatch after merge; `python3 make_babylon.py nybj` exits 0 in a scratchpad copy; `grep -n 'changed-files@' .github/workflows/update-stardict.yml` shows a 40-hex SHA or no match; `yq '.permissions' update-stardict.yml` is non-null.

#### Rank 3 — csl-pywork: finish fail-closed (redo_postxml.sh, sqlite/redo.sh, downloads/redo_all.sh) and delete the duplicate redo_postxml pass (🟡2 medium, Sonnet 5)

- **Why now:** generate_dict.sh:122 already runs redo_xml.sh which itself calls `sh redo_postxml.sh` (redo_xml.sh:21), then :143 runs redo_postxml.sh again — every dictionary's sqlite/webtc2 outputs are built twice per generation (inventory.txt:6 documents it). Inside that pass redo_postxml.sh:11 `output=$(sh redo.sh 2>&1)` never checks $?, sqlite/redo.sh has no set -e (P9) and downloads/redo_all.sh:2 (P12/P13) can fail silently, so the G1 fail_exit guard does not reach the stages that produce the served databases.
- **Depends on:** none; must land before rank 6 so the golden test's exit code is meaningful
- **Evidence:** B10 csl-pywork@e460545 v02/generate_dict.sh:122 `output=$(sh redo_xml.sh 2>&1)`, :143 `sh redo_postxml.sh`; v02/redo_xml.sh:21; inventory.txt:6; G1 residual v02/redo_postxml.sh:11 `output=$(sh redo.sh 2>&1)`; sqlite/redo.sh no set -e (P9); downloads/redo_all.sh:2 (P12/P13); G1 done part: PR #84 redo_xml.sh:11-33, generate_dict.sh:39-45 fail_exit, xmlvalidate.py:25
- **Mission:** One PR against csl-pywork touching v02/makotemplates and v02/ scripts only. Delete generate_dict.sh:143 (keep :122); in redo_postxml.sh capture `rc=$?` after each `output=$(sh ... 2>&1)` and call the existing fail_exit pattern from generate_dict.sh with the captured output when rc≠0; add `set -eu` (not -o pipefail where pipes to grep are expected to be empty) to sqlite/redo.sh and downloads/redo_all.sh templates and check every sqlite3/sort/gzip step; run the existing xml-parse.yml matrix dictionary (acsj) via `python3 generate.py acsj inventory.txt makotemplates distinctfiles/acsj <scratch>` in a scratchpad copy — never inside the clone — to confirm the rendered scripts still parse (`sh -n`). Do not change make_xml.py or any Python beyond exit-code handling. Record in the PR that Cologne regeneration is needed for live effect.
- **Acceptance:** `grep -c 'sh redo_postxml.sh' v02/generate_dict.sh` == 0 after merge and `grep -c 'sh redo_postxml.sh' v02/redo_xml.sh` == 1; xml-parse.yml matrix green; a deliberate `exit 3` injected into the rendered sqlite/redo.sh in a scratchpad run makes generate_dict.sh exit non-zero with the captured output printed.

#### Rank 4 — csl-orig: rebase PR #2893 onto the #2894 hook rewrite and remove `\|\| true` from check_generate_dict.sh:89 (🟡2 medium, Sonnet 5)

- **Why now:** PR #2893 (h3631-g1-fail-closed) is OPEN and CONFLICTING against main since the #2894 rewrite; scripts/check_generate_dict.sh:89 still ends in `|| true`, so the hook path G2 hardened trusts a check that cannot fail. The H3631 archive line in Uprava falsely records #2893 as merged, hiding the gap from the next planner.
- **Depends on:** G2 (done); rank 3 (same fail-closed semantics on the pywork side)
- **Evidence:** gh pr view 2893: state OPEN, mergeable CONFLICTING, updatedAt 2026-08-28T16:45:20Z; A G1 csl-orig scripts/check_generate_dict.sh:89 `|| true`; A H3631 archive line claims #2893 merged; G2 .githooks/pre-commit:29,48,58,73; install-hooks.sh:42
- **Mission:** Open a fresh branch from origin/main in a new worktree (never checkout inside the guarded clone), cherry-pick only the check_generate_dict.sh hunk from #2893, drop `|| true` at :89 so a failed generation returns non-zero, keep the #2894 pre-commit structure untouched, and open a new PR superseding #2893 (link it; do not close #2893 — maintainers close). In Uprava correct the H3631 archive row to say '#2893 superseded by #`<new>`'. Never commit v02 dictionary source; csl-orig data merges are batched monthly by maintainers, this PR is scripts-only.
- **Acceptance:** `grep -n '|| true' scripts/check_generate_dict.sh` returns nothing; `gh pr view <new> --json mergeable` is MERGEABLE; guard-backstop.yml green on the PR; `bash scripts/check_generate_dict.sh` in a scratchpad copy with a forced failing generate returns exit 1.

#### Rank 5 — csl-inflect: prepared statements in web/dal.php for getWord.php and every query built from $_GET (🟡2 medium, Sonnet 5)

- **Why now:** web/getWord.php:139 builds `select * from `$table` where `key`="$key"` from raw $_GET['word'] (:15-24) and web/dal.php:83 executes it with `$this->file_db->query($sql)`; no prepare/bind exists anywhere in web/. Impact is bounded (read-only sqlite, table whitelist at dal.php:29) but it enables full-table dumps, error leakage and DoS on the public inflection service.
- **Depends on:** none
- **Evidence:** B1 csl-inflect@14cd802 web/getWord.php:15-24,139; web/dal.php:29,83
- **Mission:** One PR against csl-inflect web/ only. Add a `get_by_key($table, $key)` method to dal.php that validates $table against the existing whitelist (dal.php:29), then uses `$this->file_db->prepare('SELECT * FROM "' . $table . '" WHERE "key" = :key')` with bindValue; replace the string-built SQL at getWord.php:139 and any sibling call sites (grep `->query(` under web/). Return HTTP 400 for an empty or over-long word (cap 200 chars) before touching the DB. Keep output format byte-identical for valid words (capture 5 sample responses before/after in the PR). Do not touch verbs/ or nominals/ generators.
- **Acceptance:** `grep -rn '->query(' web/` returns only the prepared path; `php -l web/dal.php web/getWord.php` clean; `curl -s 'http://localhost:8080/getWord.php?word=a%22%20or%201%3D1%20--&...'` returns 400 or an empty result, never a table dump; sample responses for 5 valid words unchanged (diff empty).

#### Rank 6 — csl-pywork: Mako-render fixture + golden-master test on acsj, and an honest coverage gate (🔴3 hard, Opus 5)

- **Why now:** ci.yml:59-67 with .coveragerc:5-10 (omit makotemplates/**, distinctfiles/**, utilities/**, generate.py, redo_xampp_selective.py) makes the coverage gate measure 2 statements (CI run 33247698814 `TOTAL 2 0 100%`); tests/test_updateByLine.py:26-28 drives via subprocess. Without a rendered-template fixture no make_xml.py, hwparse.py or shared-package refactor (ranks 11-12) can be verified — this is the gate the map's dependency rule names.
- **Depends on:** rank 3 (sub-stage exit codes must be real for a golden run to fail honestly)
- **Evidence:** B9 csl-pywork@e460545 .github/workflows/ci.yml:59-67; .coveragerc:5-10; run 33247698814; tests/test_updateByLine.py:26-28; B11 no conftest.py; parseheadline.py, updateByLine.py, hw0.py, hw2.py, sqlite_txt.py, init_query.py importable unrendered; hwparse.py/hw.py/sqlite.py/make_xml.py need `python3 generate.py acsj inventory.txt makotemplates distinctfiles/acsj <tmp>`; class-level Ldict dicts must be reset; B12 ci.yml:5 header 'No external dependencies or test suite'; AR6/CL482 no tests/, no fixtures/mini_dict.txt (COLOGNE side, same pattern)
- **Mission:** One PR against csl-pywork. Add tests/conftest.py with a session fixture that renders acsj into a tmp dir via `python3 generate.py acsj inventory.txt makotemplates distinctfiles/acsj <tmp>` and puts `<tmp>`/pywork on sys.path, resetting class-level Ldict state between tests; add tests/fixtures/acsj_mini.txt (first ~200 lines of a real acsj.txt copied once, never a csl-orig checkout); write a golden test that runs the rendered make_xml.py + xmlvalidate.py on the fixture and compares the produced XML and sqlite row counts to a committed golden file; change .coveragerc to include the rendered pywork dir (use `--cov=<tmp>/pywork` via `[paths]` mapping) and set `--cov-fail-under` to the measured real number (expect 20-40%) rather than 100; fix ci.yml:5 header. Do not refactor make_xml.py in this PR — the golden must first pass on unchanged code.
- **Acceptance:** ci.yml coverage step reports TOTAL statements > 500 (not 2); `pytest tests/ -q` green in CI; a one-character change to the rendered make_xml.py output template in a scratch branch turns the golden test red.

#### Rank 7 — Extend twin-drift to utilities/transcoder/*.xml and apidev_copy.sh, then resync apidev's transliteration tables from websanlexicon (🟡2 medium, Sonnet 5)

- **Why now:** twin-drift.yml:51-56 lists four PHP files only; apidev's slp1_deva.xml/deva_slp1.xml (35cac03, 2021) lack the ˚↔U+0970 rule websan added in 9e72f5e (2026-01-25) and as_roman.xml diverges (a5). apidev_copy.sh copies only 3 PHP files, so the sync tool itself cannot carry the tables. Users of the API and the website already transliterate differently for the same input.
- **Depends on:** G4 (done); G5 (done, transcoder fails loud on both sides)
- **Evidence:** B7 csl-apidev@e5ed207 .github/workflows/twin-drift.yml:51-56; utilities/transcoder/slp1_deva.xml, deva_slp1.xml (35cac03 2021); as_roman.xml diverges; apidev_copy.sh copies 3 PHP files; websan@2750327 9e72f5e 2026-01-25 ˚↔U+0970 rule
- **Mission:** Websanlexicon edits first, then apidev_copy.sh — never edit apidev's copies by hand. (1) In csl-websanlexicon nothing changes unless the as_roman.xml divergence turns out to be an apidev-side fix worth porting back; diff the two files and decide per rule, documenting each in the PR. (2) In csl-apidev extend apidev_copy.sh to copy utilities/transcoder/*.xml from the websan checkout, run it, and commit the resulting XML resync; extend twin-drift.yml:51-56 to loop over `utilities/transcoder/*.xml` in addition to the four PHP files. Run the existing transliteration smoke (api_trial or a 10-string round-trip) before and after and paste the diff. Agents open PRs and never close issues.
- **Acceptance:** `diff -r csl-websanlexicon/v02/makotemplates/web/utilities/transcoder csl-apidev/utilities/transcoder` empty for *.xml after merge; twin-drift.yml drill (change one XML byte in a scratch branch) goes red; `grep -c 'transcoder/\*.xml\|transcoder/.*xml' .github/workflows/twin-drift.yml` ≥ 1.

#### Rank 8 — csl-inflect: fail-closed regen chain, correct pft/redo.sh, python3 + lxml requirement, and a minimal CI (🟡2 medium, Sonnet 5)

- **Why now:** models/redo.sh:2 `rm calc_models_*.txt` then :4-55 twelve unchecked root_model.py runs then :57 `cat calc_models_*.txt > calc_models.txt` — any one failure produces a silently truncated model table; sqlite/lgtab1/redo_lgtab1.sh rm/sqlite3/mv are unchecked; no `set -e` anywhere. manual/pft/redo.sh:2 is an unedited copy of fut/redo.sh (refers to a fut_exclude.txt that does not exist). docs/PIPELINE_MANUAL.md:224 says 'no third-party packages' while huetdata/verbs_tp.py:20 imports lxml; scripts call bare `python`. The repo has no CI at all.
- **Depends on:** none
- **Evidence:** B5 csl-inflect@14cd802 verbs/pysanskritv2/models/redo.sh:2,4-55,57; sqlite/lgtab1/redo_lgtab1.sh; B4 verbs/pysanskritv2/manual/pft/redo.sh:2 vs fut/redo.sh; readme.txt correct command; B6 docs/PIPELINE_MANUAL.md:224; huetdata/verbs_tp.py:20 `from lxml import etree`; huetdata/redo_one.sh:24,30 bare `python`; C csl-inflect no CI
- **Mission:** One PR. Add `set -euo pipefail` to models/redo.sh, every manual/`<tense>`/redo.sh, huetdata/redo_one.sh and sqlite/lgtab1/redo_lgtab1.sh; in models/redo.sh write each root_model.py output to a temp file and only `cat` into calc_models.txt after all twelve succeed; fix manual/pft/redo.sh to the command readme.txt documents (pft inputs, no fut_exclude.txt); replace bare `python` with `python3`; add requirements.txt (lxml) and correct PIPELINE_MANUAL.md:224; add .github/workflows/ci.yml running `python3 -m py_compile` over *.py, `shellcheck -S warning` over the redo scripts and `php -l` over web/*.php. Do not run the regen chain in the clone — if a smoke is needed copy models/ into the scratchpad.
- **Acceptance:** `gh run list --repo sanskrit-lexicon/csl-inflect --limit 1` green; `grep -L 'set -e' $(git ls-files '*/redo*.sh')` empty; `diff manual/pft/redo.sh manual/fut/redo.sh` non-empty and pft/redo.sh mentions no fut_exclude; `shellcheck` clean at warning level.

#### Rank 9 — csl-inflect web: port the fail-loud transcoder and close the Devanagari coverage gaps in main.js (🟡2 medium, Sonnet 5)

- **Why now:** web/utilities/transcoder.php:40 is the pre-G5 twin — a missing XML table silently returns identity output; web/main.js:35-55 devanagariToSLP1 omits ळ, ँ, ॐ, nukta consonants and Devanagari digits while :100-103 routes DEVA input through it, so users typing those characters get wrong lookups without an error.
- **Depends on:** G5 (done — copy websan transcoder.php:40-44,189,195,348-353 pattern); rank 5 (same files, avoid conflicting PRs)
- **Evidence:** B8 csl-inflect@14cd802 web/main.js:35-55,100-103; web/utilities/transcoder.php:40; G5 websan@2750327 transcoder.php:40-44,189,195,348-353
- **Mission:** One PR after rank 5 merges. Port the websan G5 change into web/utilities/transcoder.php (throw on missing/invalid XML, no identity fallback); extend the devanagariToSLP1 map in main.js with ळ→L, ँ→~, ॐ→oM, nukta forms (क़ ख़ ग़ ज़ ड़ ढ़ फ़ य़) and ०-९ digits, matching the slp1_deva.xml table used server-side; add a 20-string round-trip test (Node script under web/test/) run by the CI added in rank 8.
- **Acceptance:** CI green including the new round-trip script; `grep -c 'ळ' web/main.js` ≥ 1; renaming slp1_deva.xml in a scratchpad copy makes transcoder.php return HTTP 500 with a message rather than the input string.

#### Rank 10 — csl-orig regenerate-hwnorm1-sqlite.yml: detect untracked outputs (P15 twin) and remove dead csl-apidev move/commit steps (🟢1 trivial, Grok 4.5)

- **Why now:** Lines 113, 148, 181 use `if git diff --quiet; then` → HAS_CHANGES=false at :116 whenever the only change is a new untracked file, so a freshly generated sqlite is silently not committed; csl-pywork fixed the identical bug in PR #85 with `[ -z "$(git status --porcelain)" ]`. The workflow also still carries 'Move/Commit csl-apidev' steps that do nothing since hwnorm1c.sqlite went untracked (AP44).
- **Depends on:** none
- **Evidence:** B21 csl-orig@dee5a03 .github/workflows/regenerate-hwnorm1-sqlite.yml:113,116,148,181; G8 csl-pywork PR #85 regenerate-hwnorm1-sqlite.sh:79,102,118; AP44 csl-orig regenerate-hwnorm1-sqlite.yml dead 'Move/Commit csl-apidev' steps; hwnorm1c.sqlite untracked at apidev 23c6d1a
- **Mission:** One PR touching .github/workflows/regenerate-hwnorm1-sqlite.yml only: replace the three `git diff --quiet` checks with `[ -z "$(git status --porcelain -- <output paths>)" ]`, delete the csl-apidev move/commit steps (link issue #44 in the PR body; do not close it), keep the documented force-push at :292 untouched. Never commit v02 dictionary source.
- **Acceptance:** `grep -c 'git diff --quiet' .github/workflows/regenerate-hwnorm1-sqlite.yml` == 0; `grep -ci 'csl-apidev' ...yml` == 0 or only in comments; workflow_dispatch run green and commits a regenerated sqlite when one is produced.

#### Rank 11 — REFRESH follow-through: lockfile for redo_xampp_selective.py, fix json_from_babylon.py escapes, correct the plan header (🟡2 medium, Sonnet 5)

- **Why now:** v02/redo_xampp_selective.py (7bd24d1) shipped with argparse/--dry-run/--no-push but no lockfile, so two overlapping refreshes on the Cologne box would race; json_from_babylon.py:44 has invalid escape sequences that Python 3.12+ warns on and 3.14 will reject; the plan doc header still says 'no csl-pywork files changed'. The live p95 win from G3/D4 waits on this refresh being run at Cologne (human decision on the rehearsal).
- **Depends on:** rank 3 (the refresh must call the fail-closed generate_dict.sh)
- **Evidence:** A REFRESH csl-pywork v02/redo_xampp_selective.py (7bd24d1) no lockfile; DECISIONS_NEEDED C2 dry-run not done; plan header stale; json_from_babylon.py:44 invalid escapes; issue #53 open
- **Mission:** One PR: add an fcntl.flock-based lock (path configurable, default /tmp/redo_xampp_selective.lock) around the whole run with a clear 'another refresh is running' exit 3; raw-string the regexes at json_from_babylon.py:44; update the plan doc header to list the changed files; add a `--list-only` that prints the dictionaries that would be rebuilt. Do not execute the script against any clone — dry-run only in a scratchpad copy with a fake inventory. Reference issue #53, do not close it.
- **Acceptance:** `python3 -W error -c 'import ast,sys; ast.parse(open("v02/json_from_babylon.py").read())'` exits 0; two concurrent `python3 redo_xampp_selective.py --dry-run` in a scratchpad copy: second exits 3; ci.yml green.

#### Rank 12 — COLOGNE correctness slice: transcoder path bug, redo_one.sh hard-coded paths, https curl, xmltag regex, shellcheck in CI (🟡2 medium, Sonnet 5)

- **Why now:** iast/transcoder.py:45-47 points at the wrong directory; stardict/transcoder.py duplicates it and still uses xrange at :169,:340; xmltag/redo_one.sh:4 and eascii/redo_one.sh:4 hard-code `../../../cologne/csl-orig/...`; dictionary_init.sh:41,44,47 curl over plain http; xmltag.py:35 parses XML with `<(.*?)>` and make_md.py:57-58 uses string replace. These are the tools maintainers reach for on every dictionary correction pass; each currently fails or silently mis-parses outside one historical directory layout.
- **Depends on:** none (slp1_roman.xml canonical choice is human_decisions[1]; do not touch iast/slp1_roman.xml here)
- **Evidence:** AR2/CL485 COLOGNE@ece273d iast/transcoder.py:45-47; stardict/transcoder.py:41-42,169,340; issue #485; AR7/CL486 xmltag/redo_one.sh:4, eascii/redo_one.sh:4; dictionary_init.sh:41,44,47; issue #486; AR8 xmltag.py:35 `<(.*?)>`; make_md.py:57-58; C xmltag/catall.sh, eascii/catall.sh enumerate 36 dicts vs redo_all.sh 45 (omits ap pd pwkvn lrv abch acph acsj fri); xmltag.py gather() unreachable print after return; easummary.py bare except:
- **Mission:** One PR against COLOGNE. Fix iast/transcoder.py:45-47 to resolve the XML dir relative to __file__; make stardict/transcoder.py import from iast (or vice versa) and replace xrange; parametrise CSL_ORIG in xmltag/redo_one.sh and eascii/redo_one.sh (`: "${CSL_ORIG:?set CSL_ORIG}"`); https in dictionary_init.sh; replace the `<(.*?)>` regex at xmltag.py:35 with xml.etree or lxml parsing where the input is well-formed and keep the regex only behind a documented fallback; drop the unreachable print in gather() and the bare except in easummary.py; make catall.sh enumerate from redo_all.sh's list; add `shellcheck -S warning` to the existing ruff CI job. Reference issues #485/#486/#480, never close them.
- **Acceptance:** CI green with shellcheck step present; `CSL_ORIG=<scratch> sh -n xmltag/redo_one.sh` clean; `python3 -m py_compile stardict/transcoder.py iast/transcoder.py` exit 0; `grep -n 'http://' dictionary_init.sh` empty.

#### Rank 13 — COLOGNE packaging and tests: pyproject with declared deps, fixtures/mini_dict.txt, port-or-retire the four Python 2 files (🔴3 hard, Opus 5)

- **Why now:** No pyproject/requirements while TOOLING_MANUAL.md:108,111 lists lxml, indic_transliteration, flask, flask_restx, flask_cors; CI runs ruff E9/F63/F7 only with no tests/; four py2 files fail py_compile and are ruff-excluded, so the taxonomy's 'port or retire' verdict is unenforced and any future edit to them is unverifiable.
- **Depends on:** rank 12 (same files; sequence to avoid conflicts); rank 6 pattern (fixture-driven golden test) is the template to copy
- **Evidence:** AR4 TOOLING_MANUAL.md:108,111; AR6/CL482 ruff E9/F63/F7 only; no tests/, no fixtures/mini_dict.txt; AR1/CL483 4 py2 files fail py_compile; ruff excludes; AR5 no cologne_tools package; issue #481 (248 rows); AR9 no argparse: updateByLine.py:159-161, make_md.py:121; C ruff.toml (E9,F63,F7) vs pre-commit (adds F8) disagree
- **Mission:** One PR. Add pyproject.toml declaring lxml, indic_transliteration, flask, flask-restx, flask-cors as optional extras (`[project.optional-dependencies] web=[...]`), align ruff.toml with pre-commit (E9,F63,F7,F8); add tests/fixtures/mini_dict.txt (200 lines copied from one csl-orig dictionary, named on purpose, never a whole file) and pytest tests for xmltag.py, updateByLine.py and make_md.py on that fixture; for each of the 4 py2 files either port (2to3 + test) or move to attic/ with a README tombstone per the CL483 taxonomy, and remove their ruff excludes; give updateByLine.py and make_md.py argparse entry points without changing positional defaults. Do not create the cologne_tools package yet (AR5 depends on human_decisions[4] for issue445 and on rank 6/14).
- **Acceptance:** `pytest tests/ -q` green in CI; `python3 -m py_compile $(git ls-files '*.py')` exit 0 with no excludes in ruff.toml; `pip install -e .[web]` succeeds in a fresh venv; ruff.toml and .pre-commit-config.yaml select the same rule set (diff of sorted rule lists empty).

#### Rank 14 — Shared text utilities: extract levenshtein/util_mw/util_dump_lines/unixify/compare into csl-pyutil textutil and repoint the copies (🔴3 hard, Opus 5)

- **Why now:** levenshtein.py exists in 10 copies with 3 divergent versions, util_mw.py 7/2, util_dump_lines.py 7/4, unixify.py 2/2, compare.py 4/4; csl-pyutil 0.22.0 has no textutil module. Every bug fix today lands in one copy and drifts from the rest — the same failure mode the twin-drift guard was built to stop on the PHP side.
- **Depends on:** rank 6 (golden master) because csl-pywork copies sit on the build path; rank 13 (COLOGNE packaging) so COLOGNE can declare csl-pyutil as a dependency
- **Evidence:** CE1 levenshtein.py 10 copies/3 versions, util_mw.py 7/2, util_dump_lines.py 7/4, unixify.py 2/2, compare.py 4/4; csl-pyutil@c66ce13 0.22.0 no textutil; C csl-pyutil pyproject 0.22.0 vs latest tag v0.9.0; requires-python>=3.8 but CI tests 3.x only
- **Mission:** Three PRs in order. (1) csl-pyutil: add csl_pyutil/textutil/{levenshtein,util_mw,util_dump_lines,unixify,compare}.py taking the newest version of each as base, with a unit test per divergent behaviour found in the older copies (document which copy had which behaviour), bump to 0.23.0 and tag it (tags lag: latest v0.9.0). (2) csl-pywork: replace each copy with `from csl_pyutil.textutil import ...` shims that keep the file paths (Mako-rendered scripts import by name), run the rank-6 golden test. (3) COLOGNE and csl-orig scripts/: same shim replacement. Dict-build transcoder.py copies stay by design (SH1). Never commit csl-orig v02 dictionary source.
- **Acceptance:** `git grep -l 'def levenshtein' -- ':!*.md'` across the four repos returns only csl-pyutil after all three PRs; pywork golden test green; `pip index versions csl-pyutil` (or `git tag`) shows v0.23.0.

#### Rank 15 — Transliteration delegation: finish SH1/H3488 — five queued sites onto sanskrit-util plus the SAST gap list (🟡2 medium, Sonnet 5)

- **Why now:** 7 of 19 transliteration sites delegate to sanskrit-util; five named sites still carry private copies (BookIndex linguistics.js, csl-observatory obs_t_translit_check.py, SamudraManthanam slug.py/html_to_canonical.py, RussianRamayana transliterate_filenames.py, IndologyScholars keyword_filtering.py). H3488 is claimed by OxAlpha (2026-08-31) with deliverables 2-3 undone; the claim should be honoured or released, not duplicated.
- **Depends on:** H3488 claim state (release or finish); none technically
- **Evidence:** SH1 7/19 sites delegate; queued list; HO3488 H3488 claimed OxAlpha 2026-08-31; deliverable 1 landed via H3634/H3636/H3637; deliverables 2-3 undone
- **Mission:** Under H3488 (do not mint a duplicate): one PR per repo replacing the private transliteration copy with a sanskrit-util import (JS: the published package; Python: `from sanskrit_util import ...`), each PR carrying a 10-string before/after equality check in its body; then write the SAST gap list (which repos still lack CodeQL/ruff security rules) as the H3488 deliverable 3 into the existing csl-observatory reports/ tree. Dict-build transcoder.py copies are kept by design.
- **Acceptance:** `git grep -l 'slp1\|iast' -- '*.py' '*.js'` in each of the five repos shows no private mapping tables; each repo's CI green; the SAST gap report file exists and lists every sanskrit-lexicon repo with a yes/no column.

#### Rank 16 — csl-corrections reporting: CO357 KeyError 'pwg', freshness stamps on SVGs, CI wiring for build_correction_loci.py (🟡2 medium, Sonnet 5)

- **Why now:** build_correction_viz.py:102-103 raises KeyError 'pwg' because dict_entry_counts.tsv lacks a pwg row, so the correction visualisation cannot regenerate; SVGs carry no freshness stamp; build_correction_loci.py's TSV was refreshed by hand (PRs #356/#381) but nothing in CI keeps it current. Issues #357/#358 open.
- **Depends on:** rank 1 (same repo; avoid conflicting PRs)
- **Evidence:** CO357 build_correction_viz.py:102-103 KeyError 'pwg'; dict_entry_counts.tsv lacks pwg; SVGs no stamp; issue #357; CO358 TSV refreshed PR #356/#381; no CI for build_correction_loci.py; issue #358
- **Mission:** One PR: add the pwg row to dict_entry_counts.tsv (count from csl-orig v02/pwg/pwg.txt `<L>` lines, read-only, cite the sha); make build_correction_viz.py skip-with-warning on a missing dictionary instead of KeyError; stamp each SVG with `<desc>generated YYYY-MM-DD from <sha></desc>`; add a weekly workflow that runs build_correction_loci.py and build_correction_viz.py and opens a PR (peter-evans/create-pull-request pinned by SHA) when outputs change. Reference #357/#358, never close them.
- **Acceptance:** `python3 build_correction_viz.py` exits 0 in a scratchpad copy; `grep -l '<desc>generated' *.svg | wc -l` equals the SVG count; the new workflow's first dispatch run is green.

#### Rank 17 — csl-observatory: cadence docs match the weekly cron, dashboard snapshot refreshed on schedule, one _gh.py retry helper (🟡2 medium, Sonnet 5)

- **Why now:** README.md:89,:120 say 'monthly' and the commit message at refresh-observatory.yml:137 says 'data: monthly refresh' while :5-6 runs `cron: '0 3 * * 1'` weekly; reports/dashboard.md:3 is a 2026-06-01 snapshot because render_reports.py:269 runs only in the dispatch-only refresh.yml, and CALL_COLOGNE_2026-06-27.md:12,58-60 cites that stale number. scripts/_gh.py's docstring promises a shared client while three scripts carry private retry loops.
- **Depends on:** none
- **Evidence:** B20 csl-observatory@d2dea7a README.md:89,120; .github/workflows/refresh-observatory.yml:5-6,137; reports/dashboard.md:3; scripts/render_reports.py:269; CALL_COLOGNE_2026-06-27.md:12,58-60; B22 scripts/_gh.py:2-6; external_reach.py ~:211 `_gh_json(..., retries=2)`; refresh_repo_licenses.py:28 `gh_state(repo, tries=6)`; rh1_apply_license.py:46 `gh_json(args, tries=5)`
- **Mission:** One PR: change README.md:89,120 and the :137 commit message to 'weekly'; add the render_reports.py step to refresh-observatory.yml after data refresh so dashboard.md regenerates weekly; add a 'snapshot as of' footnote where CALL_COLOGNE cites the dashboard; move the three retry loops into _gh.py `gh_json(args, tries=5, backoff=2.0)` and import it from the three scripts. Read-only gh only.
- **Acceptance:** `grep -n monthly README.md .github/workflows/*.yml` empty; next scheduled run commits a dashboard.md whose :3 date equals the run date; `grep -rn 'tries=\|retries=' scripts/*.py` shows only _gh.py.

#### Rank 18 — Cross-repo CI hygiene sweep: SHA-pin actions, add CI where none exists, .gitattributes, case-collision guard (🟡2 medium, Grok 4.5)

- **Why now:** Beyond the PAT-holding job fixed in rank 2, every repo pins actions by mutable major tag (websan checkout@v4 x5, setup-python@v5; csl-app subosito/flutter-action@v2); cologne-stardict has no CI, tags or CHANGELOG and commits production/*.babylon; csl-apidev .gitattributes lacks `* text=auto` and lists `*.txt` twice; COLOGNE has none; no repo guards against case-colliding filenames (PW74) on the Cologne Linux box vs macOS/Windows clones.
- **Depends on:** rank 2 (establishes the SHA-pin pattern); rank 8 (csl-inflect CI)
- **Evidence:** C csl-websanlexicon checkout@v4 x5, setup-python@v5; csl-app subosito/flutter-action@v2 unpinned; cologne-stardict no CI/tags/CHANGELOG, production/*.babylon committed; PW52 csl-apidev .gitattributes lacks `* text=auto`, `*.txt` twice; COLOGNE has none; issue #52; PW74 `git ls-files | sort -f | uniq -di` guard; issue #74; C csl-pyutil requires-python>=3.8 but CI tests 3.x only
- **Mission:** One sweep PR per repo, each self-contained: pin every `uses:` to a 40-char SHA with the tag in a trailing comment (dependabot keeps bumping); add `* text=auto` and dedupe .gitattributes (apidev), add a .gitattributes to COLOGNE; add a `case-collision` job `test -z "$(git ls-files | sort -f | uniq -di)"` to each repo's CI; give cologne-stardict a py_compile + `python3 make_babylon.py --help` CI and a CHANGELOG stub, and note the committed .babylon size in the PR (do not delete them — data purge is not in scope); csl-pyutil CI matrix 3.8/3.12. Reference #52/#74, never close them.
- **Acceptance:** `grep -rn 'uses: .*@v[0-9]' .github/workflows/` empty in every touched repo; case-collision job present and green in each; cologne-stardict has a green run.

#### Rank 19 — Uprava registry and docs hygiene for the Cologne lane: archive H3485, fill H3851, fix H3631 line, refresh census and stale .claim files (🟢1 trivial, Grok 4.5)

- **Why now:** Uprava README.md:164 still lists H3485 active/claimed though its scope shipped under H3633 (PR #153/#87); H3851 is claimed by Sonnet 5 with an unfilled template while rank 2 now holds its root cause; the H3631 archive line says #2893 merged (it is OPEN+CONFLICTING); H3637/H3640 .claim files are stale; the pywork census says the template lags deployment (CE2 measurement artefact) and misnames the canonical digentry (CE3). Each stale row misroutes the next planner.
- **Depends on:** rank 2 (H3851 content) and rank 4 (H3631 correction)
- **Evidence:** HO3485 Uprava README.md:164; shipped under H3633 PR #153/#87; HO3851 template unfilled; update-stardict.yml red since 2026-08-11; A G1 H3631 archive line claims #2893 merged; gh pr view 2893 OPEN CONFLICTING; CE2 census 'template lags deployment' is a measurement artefact; CE3 census lines 21/40 stale vs ffe847f digentry.py
- **Mission:** In Uprava (direct push to main per house rule): archive H3485 with a pointer to H3633; fill H3851 --mission with the rank-2 root cause; correct the H3631 archive row; remove stale .claim files for H3637/H3640 after confirming their PRs merged (gh pr view, read-only). In csl-pywork docs: fix census lines 21/40 (digentry.py canonical since ffe847f) and replace the CE2 'lags' sentence with the modal-cohort finding (webbackup/ + per-dict issue dirs).
- **Acceptance:** `python tools/handoff.py show H3485` reports archived; `python tools/handoff.py show H3851` has a non-template mission; `grep -n '2893' handoffs/archive/H3631*.md` says superseded/open; `ls handoffs/*.claim` lists no H3637/H3640.

## 7. Quick wins — 21 one-sitting fixes

| # | Fix | Repo | File | How |
|---|---|---|---|---|
| 1 | curl -f in the nightly corrections fetch (stop saving 404 HTML) | csl-corrections | fetch_yesterday_cfr.sh:18 | Replace `if ! curl -o "$OUTPUT" "$URL"; then` with `if ! curl -fsS --retry 3 -o "$OUTPUT" "$URL"; then` and add a branch that exits 0 with an echo when the HTTP status is 404 (`curl -s -o /dev/null -w '%{http_code}'`). Folds into ranked #1 if that PR is open. |
| 2 | Delete the duplicate redo_postxml.sh invocation | csl-pywork | v02/generate_dict.sh:143 | Remove line 143 `sh redo_postxml.sh` — redo_xml.sh:21 already runs it inside the :122 call; update inventory.txt:6 wording. Folds into ranked #3. |
| 3 | SHA-pin tj-actions/changed-files and add a permissions block | csl-orig | .github/workflows/update-stardict.yml:28 | Replace `@v47` with the 40-char SHA of the v47 release (`gh api repos/tj-actions/changed-files/git/ref/tags/v47`) plus `# v47` comment; add top-level `permissions: contents: read`. Folds into ranked #2. |
| 4 | Fix bot email in stardict workflow | csl-orig | .github/workflows/update-stardict.yml:68 | `git config --global user.email "the committer e-mail (its `@` is missing)"` → `"[e-mail redacted]"`. |
| 5 | git status --porcelain instead of git diff --quiet (untracked outputs) | csl-orig | .github/workflows/regenerate-hwnorm1-sqlite.yml:113,148,181 | Replace each `if git diff --quiet; then` with `if [ -z "$(git status --porcelain)" ]; then` (pattern from csl-pywork PR #85 regenerate-hwnorm1-sqlite.sh:79). Folds into ranked #10. |
| 6 | is_string guard before trim() on request params (PHP 8 TypeError) | csl-websanlexicon then csl-apidev via apidev_copy.sh | v02/makotemplates/web/webtc/parm.php:60 (websan); parm.php:84-91,105-119,152-153 (apidev, via copy) | Wrap each `trim($_GET[...])` / `trim($_REQUEST[...])` as `is_string($v) ? trim($v) : ''`. Edit websanlexicon first, then run apidev_copy.sh and commit apidev; twin-drift.yml verifies. |
| 7 | Correct the ci.yml header comment | csl-pywork | .github/workflows/ci.yml:5 | Replace 'No external dependencies or test suite' with 'Runs ruff + pytest (tests/) and a coverage gate — see jobs below'. |
| 8 | pft/redo.sh is a copy of fut/redo.sh | csl-inflect | verbs/pysanskritv2/manual/pft/redo.sh:2 | Replace line 2 with the pft command documented in manual/pft/readme.txt (pft inputs; no fut_exclude.txt). |
| 9 | python → python3 and declare lxml | csl-inflect | verbs/pysanskritv2/huetdata/redo_one.sh:24,30; docs/PIPELINE_MANUAL.md:224 | `python ` → `python3 ` in redo_one.sh and each manual/`<tense>`/redo.sh; add requirements.txt containing `lxml`; change PIPELINE_MANUAL.md:224 to name lxml as the one third-party dependency (huetdata/verbs_tp.py:20). |
| 10 | Observatory cadence wording: weekly, not monthly | csl-observatory | README.md:89,120; .github/workflows/refresh-observatory.yml:137 | Replace 'monthly' with 'weekly (Mondays 03:00 UTC, cron 0 3 * * 1)' in README and the commit message string at :137. |
| 11 | Remove dead unused_get3b | csl-apidev (via websan first if the twin has it) | dalglobClass.php:284 | Delete the `unused_get3b` function; grep confirms no callers. If websan's dalglobClass.php carries the same function, delete there first and apidev_copy.sh. |
| 12 | .gitattributes normalisation | csl-apidev, COLOGNE | .gitattributes | apidev: add `* text=auto` as line 1, remove the duplicate `*.txt` line. COLOGNE: create .gitattributes with `* text=auto` and `*.sh text eol=lf`. Reference issue #52. |
| 13 | Case-collision CI guard | csl-pywork (then each repo in ranked #18) | .github/workflows/ci.yml (new job) | Add job `case-collision: run: test -z "$(git ls-files \| sort -f \| uniq -di)"`. Reference issue #74. |
| 14 | Generated-file markers and linguist-generated | COLOGNE | .gitattributes; TOOLING_MANUAL.md:255-262,291-296 | Add `<generated-dir>/** linguist-generated=true` lines for the 86 generated files enumerated at TOOLING_MANUAL.md:255-262,291-296. Reference issue #480. |
| 15 | issues/README.md and ruff exclude | COLOGNE | issues/README.md (new); ruff.toml | Create issues/README.md ('per-issue scratch trees, not maintained tooling; see TOOLING_MANUAL.md') and add `issues/` to ruff.toml exclude. Reference issue #481. |
| 16 | CODEOWNERS from the TOOLING_MANUAL verdicts | COLOGNE | .github/CODEOWNERS (new) | Encode the owner verdicts at TOOLING_MANUAL.md:465,479,523,539 as CODEOWNERS rows (one path pattern per line). Reference issue #487. |
| 17 | pwg row for dict_entry_counts.tsv | csl-corrections | dict_entry_counts.tsv | Append a `pwg\t<count>` row (count = number of `<L>` lines in csl-orig v02/pwg/pwg.txt at dee5a03, read once) so build_correction_viz.py:102-103 stops raising KeyError. Reference issue #357. |
| 18 | catall.sh dictionary lists drift from redo_all.sh | COLOGNE | xmltag/catall.sh; eascii/catall.sh | Add the 9 missing dictionaries (ap pd pwkvn lrv abch acph acsj fri and the remaining one from redo_all.sh) or source the list from redo_all.sh. Reference issue #480. |
| 19 | Fix invalid escape sequences | csl-pywork | v02/json_from_babylon.py:44 | Prefix the regex literal with `r` (raw string) so Python 3.12+ stops warning and 3.14 does not reject. |
| 20 | Shallow-clone guidance for the 910 MiB csl-orig .git | csl-orig | README.md | Add a 'Cloning' section: `git clone --filter=blob:none --sparse https://github.com/sanskrit-lexicon/csl-orig && git sparse-checkout set v02/<dict>`. |
| 21 | Archive H3485 and remove stale .claim files | Uprava | README.md:164; handoffs/*.claim (H3637, H3640) | `python tools/handoff.py archive H3485 --note 'shipped under H3633 PR #153/#87'`; delete H3637/H3640 .claim files after `gh pr view` confirms their PRs merged. |

## 8. Decisions a human should decide — 8

Each is a real fork: options are physical actions with their cost, followed by the synthesiser's recommendation and what would reverse it. Agents must not take any of these on their own (history rewrites, production transliteration changes, visibility flips, CDN/DNS, production flags).

### 8.1 What happens to the 1,148 correction-form email addresses already committed to the public csl-corrections repository (203 in daily/, 945 in older TSVs)?

1. Purge them from history: run git filter-repo over app/correction_response and daily/ to strip or hash the entry_email column in every commit, force-push, and ask every collaborator to re-clone; cost is roughly one hour of maintainer time, a broken history for any fork, and it must be done after ranked #1 stops new addresses arriving — otherwise the purge is undone the next night.
2. Keep history but make the repository private: flip visibility on GitHub, then republish only the masked TSVs from a fresh public mirror; cost is losing public issue links and Actions minutes on a private repo, and the addresses remain in the private history forever.
3. Do nothing beyond ranked #1: stop new addresses from landing and accept that the historical ones stay reachable to anyone who clones; zero cost today, ongoing exposure of ~1,100 addresses submitted by dictionary users who expected to reach the maintainers, not the public.

- **Facts:** Live vector is the daily Actions cron (github-actions[bot]), not gitupdate.sh (last run 2023); 247 daily/`<YYYYMMDD>`/cfr-*.tsv, latest 20260830; correction_form_response.php:6-7 captures entry_email and :28-32 appends it. History rewrite is prohibited for agents (HARD RULE 1) and irreversible for forks.
- **Recommendation:** Merge ranked #1 first, then purge the email column from history with git filter-repo and force-push, because the repository's purpose (public correction tracking) is preserved while the only user PII in the estate is removed; reversal is not possible after the force-push, so a tarball backup of the pre-purge repo should be kept privately for one year.

### 8.2 Which slp1_roman.xml is the canonical transliteration table: COLOGNE iast/slp1_roman.xml (5431 B, b3f223a 2022-02-16, declared source of truth in iast/readme.txt and TOOLING_MANUAL.md:36,72,309-321) or the identical csl-apidev/csl-websanlexicon copies (3270 B, 2024-07-03, carrying the MWS#169 `<in>L</in> <out>ḻ</out>` rule at apidev :58)?

1. Declare the websanlexicon copy canonical: replace COLOGNE iast/slp1_roman.xml with the 3270 B file, rewrite readme.txt and TOOLING_MANUAL.md:309-321 to point at csl-websanlexicon, and make COLOGNE's install_local.sh copy from there; cost is losing whatever the 2 KB of extra 2022 rules did (a diff review of ~40 rules, one hour), gain is that production and tooling agree and the twin-drift guard covers the file.
2. Declare the COLOGNE file canonical: port the MWS#169 ḻ rule into it, then push it to websanlexicon and apidev_copy.sh to apidev; cost is a production transliteration change on the live site that nobody has tested since 2022, and a Cologne redeploy.
3. Keep both and document the split: mark the COLOGNE file 'offline tooling only' and forbid install_local.sh from touching web copies; cost is permanent drift and a trap for the next maintainer who reads 'source of truth' in the manual.

- **Facts:** B19: sizes 5431 B vs 3270 B; dates 2022-02-16 vs 2024-07-03; apidev and websan copies byte-identical; install_local.sh would regress production if run today; issue #483 / AR P1 already flag the readme claim.
- **Recommendation:** Adopt the websanlexicon copy as canonical and rewrite COLOGNE's readme and manual to say so, after an agent posts a rule-by-rule diff of the two files in the issue #483 thread for the maintainer to eyeball; what would reverse it is any rule in the 2022 file that a maintainer confirms is still needed offline, in which case that rule is ported forward into websan first and flows to apidev via apidev_copy.sh.

### 8.3 Should csl-apidev's git history be rewritten to drop the eight or more 26 MB hwnorm1c.sqlite blobs (.git is 223 MB; the file has been untracked since 23c6d1a), as issue #44 asks?

1. Rewrite with git filter-repo --path hwnorm1c.sqlite --invert-paths and force-push main; cost is every clone and fork must re-clone (the Cologne server's checkout included, so it needs a coordinated redeploy window), gain is a ~200 MB smaller clone for every CI run and contributor.
2. Leave history as is and only add shallow-clone guidance (`--filter=blob:none`) to the README and CI checkout; cost is nothing today, the 223 MB stays for anyone doing a full clone.
3. Defer until the csl-corrections purge (human_decisions[0]) is scheduled and do both force-pushes in one announced maintenance window.

- **Facts:** AP44: hwnorm1c.sqlite untracked at 23c6d1a; .git 223 M with ≥8 blobs of 26 MB; csl-orig regenerate-hwnorm1-sqlite.yml still carries dead 'Move/Commit csl-apidev' steps (removed in ranked #10); issue #44 open. History rewrite is outside agent authority.
- **Recommendation:** Add shallow-clone guidance now and do the filter-repo rewrite only inside the same maintenance window as the csl-corrections purge if that purge is approved, because two announced force-pushes cost the same coordination as one; if the corrections purge is declined, leave apidev history alone — 223 MB is an inconvenience, not a risk.

### 8.4 Does the shipped csl-apidev lookup/ (PRs #63/#130) supersede sample/dalglob1.php, closing the dalGlob refactor that issue #16 tracks?

1. Declare lookup/ the supported entry point: delete sample/dalglob1.php in a PR, add a README line pointing to lookup/, and let the maintainer close #16; cost is any external script still calling sample/dalglob1.php breaks (no known callers, but the API is public).
2. Keep both: mark sample/dalglob1.php deprecated in a header comment with a removal date one year out; cost is one more file for twin-drift to ignore and the refactor issue stays open.
3. Reopen the refactor: have an agent port sample/dalglob1.php onto dalglobClass.php's prepared-statement path (G9) so both entry points share one implementation; cost is a medium PR for an entry point nobody may use.

- **Facts:** AP16: lookup/ shipped via PRs #63/#130; issue #16 still open; G9 moved dalglobClass.php to prepared statements (PR #157); residual dead unused_get3b at dalglobClass.php:284.
- **Recommendation:** Mark sample/dalglob1.php deprecated with a one-year removal date and point README at lookup/, because the public API surface should not lose an entry point without notice; reversal is trivial (delete the comment) if the maintainer confirms no external callers exist, in which case delete it outright.

### 8.5 For each COLOGNE issues/`<n>`/ path that ships runnable code — issue445/serve_api.py (Flask, CORS(app) at :12, f-string SQL at :46-48, debug=True at :94) foremost — promote it into maintained tooling or sunset it into an attic?

1. Sunset: move issues/issue445/serve_api.py and its siblings under attic/issues/ with a README tombstone, remove them from ruff scope, and never run them; cost is that any maintainer who relied on the local Flask viewer must reinstall it from the attic.
2. Promote issue445 only: move serve_api.py to tools/serve_api.py, fix the CORS wildcard, parametrise the SQL, drop debug=True, add it to the AR4 pyproject web extra and CI; cost is a medium PR and a permanent maintenance obligation for a local-only viewer.
3. Promote everything under issues/ that imports flask or sqlite3 after a per-file review; cost is several days and it turns scratch code into supported API surface.

- **Facts:** AR3/CL484: issues/issue445/serve_api.py:12,46-48,94; issue #484 asks for a promote-or-sunset verdict per path; no other issue dir is known to serve HTTP. CL481 adds issues/ to ruff exclude regardless.
- **Recommendation:** Sunset every issues/ path into attic/ except issue445, which is promoted as tools/serve_api.py with its three defects fixed, because it is the only one that serves data and the fixes are small; reversal is a git mv back if a maintainer names another issues/ script they still run.

### 8.6 Should the dictionary site adopt a CDN (D5) for static assets and sqlite downloads, or keep everything served from the Cologne box?

1. Adopt Cloudflare (or the university's CDN) in front of sanskrit-lexicon.uni-koeln.de for static and downloads/ only, leaving PHP dynamic paths uncached; cost is DNS control and a university-IT ticket, gain is offloading the multi-GB sqlite downloads that dominate egress.
2. Do nothing: D1/D2/D3 (cache headers, defer, APCu) already shipped and the p95 win from G3/D4 arrives with the next regeneration; revisit only if the Cologne box shows egress saturation.

- **Facts:** D5 is a product decision with no tracked issue; D1 .htaccess:12-29 headers, D2 defer, D3 APCu already live; G3/D4 query index waits on Cologne regeneration (REFRESH).
- **Recommendation:** Do nothing until the REFRESH regeneration lands and one week of post-regeneration server logs exists, because the largest measured latency cost (query p95) is already fixed in code and CDN adoption needs university DNS authority no agent holds; reverse if the logs show downloads/ egress above the box's capacity.

### 8.7 Should the keydoc fast path in dal.php (websan :93, apidev :91), currently gated on `$this->devFlag`, be enabled in production at Cologne?

1. Enable it: flip the gate to default-on behind a config constant, deploy via the normal websan-then-apidev_copy.sh route and a Cologne redeploy, and measure p95 on 100 real queries before and after; cost is one deploy window and the risk that keydoc output differs from the slow path for some dictionaries (needs a 10-dictionary equality check first).
2. Leave it dev-only: no production change, no measurement; cost is an unmeasured latency win left on the table.

- **Facts:** D6: dal.php:93 (websan) / :91 (apidev) fast path gated on devFlag; no handoff exists; enabling requires Cologne server access an agent does not have.
- **Recommendation:** Have an agent first produce a 10-dictionary equality report (fast vs slow path, 50 headwords each, read-only against a local build) and only then let a maintainer flip the gate in a deploy window, because the win is plausible but unmeasured and the equality check is cheap; a single mismatch in the report reverses the recommendation until the fast path is fixed.

### 8.8 Who runs the Cologne-server dry-run rehearsal of v02/redo_xampp_selective.py (REFRESH, DECISIONS_NEEDED C2) that gates the live G3/D4 query-index win?

1. A maintainer with SSH to the Cologne box runs `python3 redo_xampp_selective.py --dry-run` after ranked #11 adds the lockfile, pastes the output into csl-pywork issue #53, and if clean runs the real refresh for one dictionary (acsj) then all; cost is an hour of maintainer time and a short window of double CPU on the box.
2. Delay until the golden-master test (ranked #6) exists so the refresh can be run against a CI-verified generate_dict.sh; cost is the p95 win stays unshipped for another cycle.

- **Facts:** REFRESH: script shipped 7bd24d1 with --dry-run/--no-push; no lockfile yet (ranked #11); Cologne server access is outside agent authority; issue #53 open; G3/D4 p95 162.8→45.5 ms measured locally only.
- **Recommendation:** Run the dry-run at Cologne as soon as ranked #3 (fail-closed sub-stages) and #11 (lockfile) merge, before the golden test, because the dry run itself changes nothing and the fail-closed scripts already make a bad real run stop rather than ship; reverse to the delay option if the dry-run output lists more than the expected changed dictionaries.

**Correction to 8.1 (critic):** the counts quoted in the question come from the finder; the critic's re-count at e363439 is **232 unique addresses, 34,757 occurrences** across tracked TSVs (1,379 in daily/, 2,433 in cfr_ab/cfr_ab.tsv, 33,378 in older files including app/correction_response/2020/*.tsv). The decision stands; the number to quote is 232 unique. **Correction to 8.2 (critic):** COLOGNE `iast/slp1_roman.xml` is byte-identical to csl-apidev `utilities/transcoder/svarita_circumflex/slp1_roman.xml`, so the fork is "promote the svarita_circumflex variant to top-level or not", not "COLOGNE vs apidev".

## 9. Dropped after refutation — 18

| Finding | Repo | File | Why dropped |
|---|---|---|---|
| csl-json/redo.sh drives py3-only make_babylon.py with python2 and no set -e -- babylon regeneration silently skipped, stale JSON emitted | csl-json | redo.sh:16,24 | PRIOR-ART: REFRESH_SCRIPT_MODERNIZATION_PLAN.md:30,:32,:40,:132 already records csl-json redo.sh as a python2 residual with the same fix; the canonical driver c |
| update_user_corrections.sh commits and pushes csl-orig unconditionally after generate_dict.sh with no exit-code check | csl-corrections | update_user_corrections.sh:1-19 | CORRECTNESS holds (csl-corrections@e363439 update_user_corrections.sh: 19 lines, no set -e, cd/generate_dict.sh/xmlchk/git add/commit/push chained unconditional |
| sanskrit-util deva_to_slp1/slp1_to_devanagari silently diverge from the site FSM (avagraha dropped, ँ→M, Devanagari digits and ॰ not transco | sanskrit-util | sanskrit_util/__init__.py:150,176-177,222-223,240 | CORRECTNESS holds (sanskrit-util@a2c4714 py/sanskrit_util/__init__.py:150 `_DV_MARK = {'ं': 'ṃ', 'ः': 'ḥ', 'ँ': 'ṃ'}`, :176-177/:222-223 avagraha `pass`; js/san |
| csl-inflect analyze_{1cons,an,f}.py import transcoder from ../../transcode which does not exist in the repo — redo_1cons_analyze.sh cannot r | csl-inflect | nominals/pysanskritv2/analysis/analyze_1cons.py:10-12 | CORRECTNESS holds (csl-inflect@14cd802 nominals/pysanskritv2/analysis/analyze_1cons.py:10-12 `sys.path.append('../../transcode')` / `import transcoder` / `trans |
| cologne-stardict runs a third transliteration engine (indic_transliteration) with two inconsistent scheme maps (slp1_accented vs slp1) and a | cologne-stardict | utils.py:6,10-11,37-45 | CORRECTNESS holds (cologne-stardict@0590b83 utils.py:6,10 `SchemeMap(SCHEMES['slp1_accented'], SCHEMES['devanagari'])`, :37-45 applyaccent; make_babylon_synonym |
| csl-atlas hand-rolled SLP1->IAST tables in 3 scripts map both `L` and `x` to ḷ, contradicting the sanskrit-util it already vendors | csl-atlas | scripts/build-stardict-export.mjs:30-41 | CORRECTNESS holds (csl-atlas@3f9fe64 scripts/build-stardict-export.mjs:30-41 `x: "ḷ"` and `L: "ḷ"`, scripts/lib/dharmamitra_infer.py:54 and scripts/import-dharm |
| levenshtein re-implemented in 5 live copies across 4 repos; csl-pyutil 0.22.0 still exports none (SHARED_CODE §9 lift target unmet) | csl-pyutil | csl_pyutil/__init__.py:63-64 | PRIOR-ART: the same defect and the same fix are already recorded — SHARED_CODE.md:38 (item 9 'Generic helpers (levenshtein ...) copied, low priority, lift into  |
| Python transcoder.py lineage at HEAD: COLOGNE/iast == apidev simpleslp == corrections/issue87 (byte-identical); COLOGNE/stardict and csl-pyw | COLOGNE | iast/transcoder.py:49-53,60-68 | CORRECTNESS: COLOGNE@ece273d iast/transcoder.py is byte-identical to csl-apidev/simple-search/simpleslp/transcoder.py and csl-corrections/issues/issue87/transco |
| acsj is a viable golden-master: 50 KB input, 0.8 s full pipeline, 7 byte-deterministic artifacts across runs; CI's only generation check is  | csl-pywork | .github/workflows/xml-parse.yml:92-100 | CORRECTNESS: csl-pywork@e460545 .github/workflows/xml-parse.yml:92 `sh generate_dict.sh mci tempparent/mci 2>&1 \| tee build.log \|\| true` followed at :93-100  |
| Newcomer first run without mako: exit 0 and five misleading follow-on messages (generate_web usage text, cd/redo_*.sh not found) after the r | csl-pywork | v02/generate_dict.sh:60-65 | CORRECTNESS partial: csl-pywork@e460545 v02/generate_dict.sh:60 `fullpath=\`readlink -f $outdir\`` and :62/:65 pass it to csl-websanlexicon/v02/generate_web.sh, |
| GENERATION_MANUAL §3 lists PHP as a generation prerequisite; a full generate_dict.sh run invokes no php | csl-pywork | docs/GENERATION_MANUAL.md:113 | CORRECTNESS: csl-pywork/v02/redo_postxml.sh:46-55 (e460545) invokes whitmwtab/westmwtab redo.sh which runs `php dbinit.php`; GENERATION_MANUAL.md:113 PHP statem |
| Shipped web/webtc2/query_dump.txt ends with the literal junk row `prevkey :: keysanskrit<TAB>keydata` — live-confirmed for acsj (639 lines f | csl-pywork | v02/makotemplates/pywork/webtc2/init_query.py:58 | CORRECTNESS-AT-TIP: fixed upstream by PR #85 (fcef81d, H3638, 28-08-2026); origin/main aefbdac no longer emits the row. Local clone e460545 is stale. PRIOR-ART: |
| phpquery/phpQuery-onefile.php v0.9.5 is dead vendored code with PHP-8-fatal create_function()/each() calls, still shipped and CI-skipped | csl-apidev | phpquery/phpQuery-onefile.php:6 (@version 0.9.5), 1035 (create_function) | PRIOR-ART/MATERIALITY: WEB_BACKEND_MANUAL.md:63 already marks phpQuery 'do not build on'; the audit notes it; the library is dead code not invoked by any live e |
| Unconditional `Access-Control-Allow-Origin: *` on HTML, non-JSONP and PDF responses in both websan and apidev | csl-websanlexicon | v02/makotemplates/web/webtc/getword.php:11; webtc2/query.php:8; csl-apidev getword.php:11, listview.php:4, servepdf.php:19, cleanurl.php:114, api_trial.php:~10 | MATERIALITY: endpoints serve anonymous, public dictionary data with no cookies/auth; wildcard CORS is the intended design for a public API; finder concedes noth |
| app/entry.php honours `?fixtures=1` in production and serves the tracked app/fixtures/fixtures.json instead of live data | csl-apidev | app/entry.php:94-100 (entry_fixtures), 96 | CORRECTNESS: entry.php:267-268 (2750327) `$is_canonical_view = (count($blocks) > 0 && $dictparam === '' && !$fixtures)` — fixtures view is non-canonical and rec |
| apidev_copy.sh drift: websan servepdfClass.php dereferences $imageParms[$dictinfo->dictupper] unguarded while the apidev twin has an isset g | csl-websanlexicon | v02/makotemplates/web/webtc/servepdfClass.php:45; webtc2/query_gather.php, query_multi.php, querylistview.php, webtc/disp.php, webtc/getwordviewmodel.php (inventory.txt category D); js/orphus.customized.js:368 | Multiple sub-claims false at 2750327: generate.py:170-176 'D' means delete-from-output (`if os.path.exists(newfile): os.remove(newfile)`), not dead code; webtc2 |
| COLOGNE CI lint cannot see the transliterator: iast/ and stardict/ excluded as 'Python 2 legacy' and undefined-name rules disabled, while st | COLOGNE | ruff.toml:2-14 | CORRECTNESS holds (ruff.toml:2,:6 at ece273d exclude iast/ as 'Python 2 legacy' while iast/slp1_iast.py and iast/transcoder.py py_compile OK on py3 and TOOLING_ |
| csl-orig is a 910 MiB / 2249-commit repo with no shallow or partial-clone guidance anywhere contributors read | csl-orig | README.md:1 | CORRECTNESS holds (dee5a03: .git 910M, size-pack 909.11 MiB, 12959 objects, 2249 commits, 397 tracked files; no 'shallow\|depth\|blob:none\|filter\|sparse' in R |

**Critic dissent on two drops:** csl-atlas `scripts/build-stardict-export.mjs:30,34` collapsing `x` and `L` into one ḷ glyph is a shipped correctness bug (prior art documents it, nothing fixes it) and belongs in §6; the `update_user_corrections.sh` unconditional-push drop was judged weak. Both are re-listed as follow-ups in §11.

## 10. Method and what was not covered

1. **Prior art first.** 50 items collected from the August audit, GENERATION_MANUAL/DECISIONS_NEEDED, REFRESH, SHARED_CODE, CROSS_REPO_DECISIONS, ARCHITECTURE_REVIEW, the open issues of COLOGNE/csl-pywork/csl-apidev/csl-corrections and the Cologne-lane handoffs; four verifier agents rated each `done / partial / open / superseded` from the tips, with evidence.
2. **Finders on the residue only.** Five merged finder lenses: inflect + corrections + downstream exporters; shared transliteration; pywork tests and DX; PHP surfaces; COLOGNE + csl-orig hygiene. 40 raw findings.
3. **Refutation.** Two batched refuters, each finding judged through three lenses (correctness, materiality, prior-art); 22 kept, 18 dropped.
4. **Synthesis + critic.** One synthesiser (xhigh effort) produced §2, §6, §7, §8; one critic re-read at origin tips and produced §5.

Finder coverage notes (verbatim intent):

- **inflect-corrections-downstream** (heads: csl-inflect=14cd802 csl-corrections=e363439 cologne-stardict=0590b83 csl-json=c824e1e csl-app=a830144 csl-homepage=4165574 csl-sqlite=65e9b8d hwnorm1=4a80a35 csl-pywork=e460545 csl-orig=dee5a03 sanskrit-util=a2c4714) — Read-only audit; nothing executed, no file modified, no git state changed, gh used only for `run list`/`run view --log-failed`/`issue list`. NOT covered: (a) csl-inflect duplicated-transcoder claim vs sanskrit-util is unverifiable because sanskrit-util@a2c4714 `git ls-files` contains no transcoder XML/FSM tables at all -- csl-inflect's 13 web/utilities XML tables are self-contained, not a fork of sanskrit-util (task premise unsupported); duplication vs csl-pywork's own transcoder tables not diffed. (b) Task hypothesis "Python 2 generators on live path" was checked and is WRONG for cologne-stardict/make_babylon.py (f-strings, indic_transliteration -> py3) and csl-json/json_from_babylon.py (py3-clean); the py2 residue is only in csl-json/redo.sh invocations and hwnorm1/hwnorm1.py (off the canonical sanhw1/redo.sh chain). (c) csl-sqlite@65e9b8d holds 7 non-code files (README, LICENSE, CLAUDE.md, workflows) -- nothing to audit; generator lives in csl-pywork regenerate-hwnorm1-sqlite.sh (known P15). (d) csl-app Dart/Flutter source not audited (build/CI only, per scope); deploy.yml uses unpinned `subosito/flutter-action@v2` channel stable by explicit comment -- noted, not ranked. (e) csl-orig dictionary data not descended; csl-corrections local ` M data/derived/correction_loci.tsv` ignored, HEAD audited; build_correction_loci.py/build_correction_viz.py not re-read beyond known #357/#358. (f) csl-inflect nominals/pysanskritv2/analysis/*.py py2 prints (analyze_f.py:167) are off the redo chain -- not ranked. (g) ~37 open auto-created "Daily Corrections - `<date>`" issues (#313..#382) never closed by any automation -- noted as process debt, not ranked. (h) Web-app runtime behaviour (PHP version on the Cologne host) not observable locally; PHP 8 fatals are inferred from source (dal.php:225/303/342 `$item00 = item0[0];` bare-constant, correction_form_response.php fwrite(false)).
- **shared-transliteration** (heads: sanskrit-util=a2c4714 csl-pyutil=c66ce13 csl-pywork=e460545 csl-apidev=e5ed207 csl-websanlexicon=2750327 csl-orig=dee5a03 COLOGNE=ece273d csl-inflect=14cd802 csl-corrections=e363439 cologne-stardict=0590b83 csl-observatory=d2dea7a csl-atlas=3f9fe64 Uprava=98c37cd2f) — Not inspected: csl-orig/v02/*/ dictionary data (only two greps on csl-orig/v02/mw/mw.txt for `<k1>` lines containing `~` and `'`); csl-pywork/csl-websanlexicon distinctfiles OUTPUT trees (only distinctfiles/mw/pywork/transcoder.py + mwauth/tooltip.py as build INPUT for the lineage map); issues/ workspaces except csl-corrections/issues/issue87/transcoder.py (byte-compared only); webbackup snapshots; v00/v01 trees beyond diff baselines; Uprava handoff bodies beyond titles + keyword grep of H3485/H3486/H3488/H3634/H3851; csl-inflect web/utilities/transcoder/*.xml functional diff vs websan (only file count + line count); body of levenshtein in csl-atlas/scripts/forensic/f0_shared_headword_typos.py (BSD sed error; existence + commit date confirmed); COLOGNE/enhancements/* beyond issue10/levenshtein.py; GRA transcoder3.js (outside named scope); no gh reads used; no runtime tests of transcoder.php (PHP not executed) — PHP claims are static reads; Python/JS engine comparisons executed only on scratchpad copies (scratchpad/engines/cmp.py, cmp2.py, cmp.js). Prior-art docs grepped for avagraha/candrabindu/U+0970/slp1_deva.xml/deva_slp1.xml/devanagariToSLP1/analyze_1cons/v0.5.1/.ser: zero hits, so findings 1-6 are new relative to those docs.
- **pywork-tests-dx** (heads: csl-pywork=e460545 csl-orig=dee5a03 csl-websanlexicon=2750327 csl-observatory=d2dea7a) — Read in full: csl-pywork tests/* (3 files), .coveragerc, ci.yml, xml-parse.yml, v02/generate.py, generate_dict.sh, generate_orig.sh, generate_pywork.sh, generate_ab_bib_ls.sh, inventory.txt, inventory_orig.txt, makotemplates/pywork/{hw.py,hwparse.py,parseheadline.py,updateByLine.py,redo_hw.sh,redo_xml.sh,redo_postxml.sh,sqlite/redo.sh,sqlite/sqlite.py,sqlite/sqlite_txt.py,webtc2/redo.sh,webtc2/init_query.py}, make_xml.py lines 1-30 + 1290-1391 only (per-dict dig_to_xml bodies lines ~30-1290 NOT read), docs/GENERATION_MANUAL.md, CLAUDE.md; csl-websanlexicon v02/generate_web.sh + generate.py head (first 150 lines) + WEB_FRONTEND_MANUAL.md by grep only. Live DX runs done ONLY in scratchpad copies (/private/tmp/claude-502/-Users-mac-Documents-GitHub/c058c65f-6aa6-473d-a501-ac5fdcd1991a/scratchpad/dx/{csl-pywork,csl-websanlexicon,csl-orig/v02/acsj}, venv with mako 1.4.1, pytest, pytest-cov; logs attempt1_nomako.log, attempt2_venv.log, attempt3_venv.log): generate_dict.sh acsj run 3x (once without mako, twice with), pytest+coverage run once. NOT done: any run for mci or a generic-branch dict (only the kosha branch abch/acph/acsj/nmmb was exercised live); hw2.py/hw0.py/digentry.py/xmlvalidate.py/downloads templates/v00 not read; csl-websanlexicon generate.py beyond line 150 and its makotemplates/web PHP not read; prior-art docs other than CODEBASE_ADVERSARIAL_AUDIT_2026-08.md and GENERATION_MANUAL.md only grepped for redo_postxml/readlink/golden/snapshot/mako/subprocess/acsj; no gh reads needed. Note csl-websanlexicon HEAD (2750327) differs from the audit pin 45b0d30. No clone file modified; no state-changing git.
- **php-surfaces** (heads: csl-websanlexicon=2750327, csl-apidev=e5ed207, csl-corrections=e363439, csl-observatory=d2dea7a) — NOT examined: semgrep (not installed on this Mac; skipped, non-blocking). No requests sent to production; all runtime evidence is from local PHP 8.5.9 (/opt/homebrew/bin/php) executing copies under scratchpad/php-surfaces/ (apidev parm.php, websan webtc/parm.php with Mako `${...}`/`<%doc>` stripped by sed). csl-apidev simple-search/, htmlwork/, sample/, pwkvn/, monier1list/, lookup/ PHP not audited (legacy/scratch per WEB_BACKEND_MANUAL). phpQuery internals read only for @version and create_function/each grep; no CVE lookup performed (no web access used). lookup/dictmeta.js <-> app/dictmeta.php sync NOT verified (first key-extraction regex failed; the JS is an array-of-arrays literal, a proper diff was not re-run) — no drift claimed. Dynamic-property (PHP 8.2) regex scan was noisy (comment false positives) and the local run of Parm+DictInfo+GetwordClass emitted no Deprecated lines, so no dynamic-property finding is reported. GetwordClass/Dal runtime paths beyond Parm construction untested locally (no sqlite data). websan v00 and generated distinctfiles outputs not read. csl-corrections outside app/ (data/, tools/) not examined. Server config (Apache vhost, php.ini display_errors, whether app/correction_response/ is web-readable) is unknowable from the clones — the path-leak scenario is stated conditionally. Issues #4/#16/#44 (salt id minting) deliberately not re-audited.
- **cologne-orig-hygiene-meta** (heads: COLOGNE=ece273d csl-orig=dee5a03 csl-pywork=e460545 csl-websanlexicon=2750327(branch h3636-getword-data-twin; origin/main=02b45aa) csl-apidev=e5ed207 sanskrit-util=a2c4714 csl-pyutil=c66ce13 csl-inflect=14cd802 csl-corrections=e363439 cologne-stardict=0590b83 csl-observatory=d2dea7a csl-atlas=3f9fe6) — NOT inspected: csl-orig v02/* dictionary data except `head -3 v02/mw/mw.txt` and directory listings (v02/nybj, v02/pwkvn, v02/etymology_stats) — rule 3; COLOGNE issues/**, localinstall/, docs prose beyond readme.txt/README/ARCHITECTURE_REVIEW/cleanup backlog; COLOGNE v00 lanes; csl-pywork v02/*.sh internals beyond names (generate_dict.sh no-set-e is known O2/G1); csl-websanlexicon PHP/Mako code (only workflows + transcoder XML twins); csl-atlas 127 scripts/*.mjs and csl-standards 26 scripts/*.mjs bodies (only workflows, package.json scripts, verify.mjs:51 audit line); csl-observatory observatory/fetch.py body beyond except-line grep, 80 scripts bodies beyond gh-client duplicates; no scripts were executed (only python3 ast parse of COLOGNE/stardict/transcoder.py in place, read-only); gh used read-only (run list / run view --log-failed on csl-orig). Secrets grep (ghp_/github_pat_/AKIA/PRIVATE KEY/xox/sk- + literal password/token assignments) over all 13 repos excluding .git,v02,node_modules,MD,issues: zero hits. Python 3.8 compatibility of csl-pyutil (requires-python>=3.8, CI tests only '3.x') not verifiable locally (no 3.8 interpreter); grep for PEP604/match without future-import found none.

HYGIENE TABLE (repo | python: requires vs CI | ruff/pre-commit | .gitattributes | action pins | CodeQL/Semgrep | tags / CHANGELOG head vs last commit | LICENSE | notes):
- csl-pywork | none / CI 3.11 + matrix 3.10-3.12 | pre-commit yes, no ruff.toml | `* text=auto` +tsv/sh lf | checkout@v7 setup-python@v7 fetch-metadata@v3 codeql@v4 (uniform) | CodeQL yes | v0.2.2 / 2026-07-31 vs 2026-08-20 (no changelog-lint) | GPL | generate_dict.sh no set -e (known O2)
- csl-websanlexicon | none / CI 3.12 | pre-commit yes | text=auto | checkout@v4 x5 + @v5, setup-python@v5, fetch-metadata@v2, upload-sarif@v3 (oldest pins in set; same on origin/main) | CodeQL + Semgrep | v0.3.0 / 2026-07-28 vs 2026-08-28 | GPL | local clone on feature branch
- csl-apidev | none / CI 3.11 + 3.12 | pre-commit yes | txt/tsv/sh lf, no text=auto | checkout@v7 setup-python@v7 fetch-metadata@v3 upload-sarif@v4 | Semgrep (no CodeQL) | v0.4.2 / 2026-07-29 vs 2026-08-28 (has changelog-lint.yml) | GPL | twin-drift.yml present
- csl-orig | none / CI 3.10 x2 + 3.x | pre-commit yes (opt-in hook chain dormant in clone: no .git/hooks/pre-commit, hooksPath unset) | text=auto | checkout@v7 setup-python@v7 upload-artifact@v7 fetch-metadata@v3 + tj-actions/changed-files@v47 (only third-party mutable tag in set) | none | NO tags / 2026-06-30 vs 2026-08-26 (no changelog-lint) | CC-BY-SA 4.0 | .git 910 MiB, 2249 commits, no shallow/partial-clone guidance in README/CONTRIBUTING/CLAUDE
- sanskrit-util | none / CI 3.11 + 3.x | pre-commit yes | `* text=auto eol=lf` | checkout@v5+@v7, setup-python@v6+@v7 mixed | none | v0.9.0 / 2026-08-24 = last commit | MIT | changelog-lint.yml
- csl-pyutil | requires-python>=3.8 / CI 3.x only (floor never tested) | pre-commit yes, pyproject | none | checkout@v7 setup-python@v7 | none | v0.9.0 / 2026-08-21 vs 2026-08-27 | MIT | pyproject version 0.22.0 vs latest tag v0.9.0 (tag/version drift)
- csl-inflect | none / no CI (only dependabot-auto-merge.yml) | none | text=auto eol=lf | fetch-metadata@v3 | none | v0.1.0 / 2026-07-15 vs 2026-07-27 | LGPL | no test/lint workflow at all
- csl-corrections | none / CI 3.11 | pre-commit yes | text=auto | checkout@v7 setup-python@v7 | none | v1.0.1 / 2026-08-30 vs 2026-08-31 | GPL | changelog-lint.yml
- COLOGNE | none / CI 3.11 | ruff.toml (select E9,F63,F7; excludes enhancements/ iast/ stardict/) + pre-commit ruff --select E9,F63,F7,F8 (CI and pre-commit disagree on F8) | NO .gitattributes | checkout@v7 setup-python@v7 codeql@v4 | CodeQL yes | v0.1.0 / 2026-08-30 = last commit | GPL | 2 py2-syntax files remain (known P1)
- cologne-stardict | none / NO CI (only dependabot-auto-merge.yml) | pre-commit yes | text=auto | fetch-metadata@v3 | none | NO tags, NO CHANGELOG / last commit 2026-08-04 | MIT | production/*.babylon (43) committed; driven remotely by csl-orig update-stardict.yml
- (sub-scope 4) csl-observatory: no pyproject/requirements; CI py 3.11 x2 + 3.12 x5 mixed; checkout@v5+@v7, setup-python@v6+@v7; PAT TOOLING_AUDIT_TOKEN fails-loud if unset (dict-audit.yml:48, tooling-audit.yml:44); refresh uses GITHUB_TOKEN only; check_coverage.py guard present. csl-atlas: verify.mjs:51 runs `npm audit --omit=dev --audit-level=high` inside `npm run verify` which test.yml:24/build-and-deploy.yml:30 call — DEPENDENCY_SECURITY.md:5 claim holds; CodeQL weekly; requirements-review.txt pins csl-pyutil by commit. csl-standards: ci.yml runs npm test + every validate-* gate named in README:90-97; node 24; dependabot-automerge-sync.yml exits 0 with ::warning when ORG_AUTOMERGE_TOKEN unset (documented inert).

Minor observations not promoted (evidence available): csl-orig update-stardict.yml:68 `git config --global user.email "the committer e-mail (its `@` is missing)"` (missing @); COLOGNE xmltag/catall.sh and eascii/catall.sh enumerate 36 dicts while xmltag/redo_all.sh (2026-05-04) enumerates 45 → committed all_xmltags.txt has 36 `; tags for` headers, omitting ap pd pwkvn lrv abch acph acsj fri (extends #480), eascii/catall.sh labels sections "; tags for" (copy-paste); xmltag.py gather() has unreachable `print(... %(n,fileout))` after `return d` with n undefined; easummary.py write_ea bare `except:`; csl-orig regenerate-hwnorm1-sqlite.yml last 6 scheduled runs all success.

Not covered, by construction: csl-orig `v02/` dictionary data was not inspected; no semgrep/CodeQL alerts were read; no production requests were made; the org's largest code surfaces (PWG, PWK, MWS, AP, GRA, WIL, MWinflect, mw-dev, hwnorm2, CORRECTIONS and the JS-heavy csl-westergaard/csl-whitroot) had **zero** finder coverage — see §5.2. The medium-size run used 5 merged finders instead of the 14 planned for the first (aborted) ultracode run, and 2 batched refuters instead of three independent refuters per finding.

## 11. Follow-ups this map creates (agent tasks, not yet minted)

1. Re-scope rank 1 per §5.1 item 3 (mask at ingest, mask in `update_cfr_ab.py`, strip `user=` from the nightly issue body) and re-count the addresses from git output — then mint it.
2. Fold rank 3 into the quick-wins list (only the duplicate `redo_postxml.sh` pass survives at tip).
3. Add a ranked item for csl-atlas `build-stardict-export.mjs:30,34` (ḷ collapse) — correctness, 🟢1 trivial.
4. Run the fail-closed grep set over the ten uncovered dictionary-work repos (§5.2 gap 3) and add a "dictionary-work repos" cluster.
5. Three-way diff of `deva_slp1.xml` / `slp1_deva.xml` / `as_roman.xml` across apidev, websan and csl-inflect (§5.2 gap 4) so a human can pick a canonical copy per file.
6. Measure the csl-pywork coverage gate in a scratch copy instead of asserting "2 statements" (§5.3).
7. Read the code-scanning alerts of PWK/MWS/AP/GRA/csl-santam via the GitHub API (no file opened) to quantify the uncovered repos.

## 12. Provenance

- Workflow run `wf_455b00eb-e36`, script `cologne-map-medium.js`, args `date=02-09-2026`; journal retained in the session transcript directory.
- All 13 subagents and the orchestrating session: Claude Fable 5.1 (`claude-fable-5-1`); no model overrides.
- Elapsed: workflow 59м 1с measured; no tok/s figure is reported (interactive run, no worker log).
- A first attempt as a 66-agent ultracode workflow (`wf_e2513e4b-6ff`) hit the session limit before producing output; its prior-art scouting was reused as the 50-item input list.

_Dr. Mārcis Gasūns_
